# 2026-08-18 工作日志

## AIOps 平台全量综合测试审核（代码审计）→ 产出 1.md
- 以交付总监角色建团队 `software-aiops-audit`，委派 QA 工程师 严过关 做代码级综合测试审核（不部署）。
- 产出：`/Users/mssc/Documents/Code/agent/aiops/1.md`（配套 R1/R2 报告，不重复）。
- 核心结论：测试就绪度 = 风险高。
  - 仍存在 **3 个安全 BLOCKER**：S1 orchestrator 无全局认证中间件（绑 0.0.0.0:8080）、S2 Go AuthMiddleware 不查用户状态（删/禁用用户 JWT 仍可用）、S3 ProxyAI 转发高危路径无角色门控（普通用户可读审计/触发执行）。
  - 仍存在 **约 20 个 MAJOR**：命令执行白名单过宽(S10)、SSRF(S12)、报告路径穿越(S13)、硬编码弱凭据(S17)、无 NetworkPolicy(S18)、WAL 重启丢未 ack 数据(D4)、背压无 dropped 计数(D5)、/health 恒 200(R1) 等。
  - **复核纠正 R2 不实结论**：R5「audit_logs 表全仓无」错误——`ai-orchestrator/migrations/0001_business_tables.sql:22` 已建表，`db.py` 启动执行迁移。
  - 测试体系：query-go(~127) / orchestrator(~320) 业务单元较充分；**event-collector 0 测试、前端 0 提交测试、ingest WAL 0 测试**——安全/数据管道/前端回归守护缺失。
- 优先修复顺序：安全 BLOCKER → D4/D5 数据丢失 → RBAC 收敛 → 凭据/网络 → 可靠性 → 补测试。

## AIOps 平台可落地修复方案（代码级）→ 产出 2.md
- 建团队 `software-aiops-fixplan`，委派架构师 高见远 基于 1.md 产出 `/Users/mssc/Documents/Code/agent/aiops/2.md`。
- 覆盖 1.md 登记册**全部 35 项**（3 BLOCKER + 21 MAJOR + 11 MINOR/其他），每条均给真实代码级精确修改点（文件:函数:行号 + 改法/伪代码），非泛泛而谈。
- 与既有 `AIOPS_FIX_PLAN_2026-08-18.md`（G/H 系列高层设计）配套：本方案补齐精确修改点，并新增 S8(用户无法禁用)/S9(GET /settings/llm 公开)/P1-3②(SaveLLMSettings 先改后校验) 三项既有方案未单列项。
- 结构：修复总策略 + 任务分解(Mermaid 依赖图 T01–T06) + 逐条修复 + 回归验证清单 + 风险与 6 项待明确决策(S7 角色模型/S17 密钥注入/S18 NetworkPolicy 范围/S19-S20 权限边界/S22 JWT 刷新/R5 建表实测) + 修改文件清单。
- 关键 BLOCKER 修复思路：S1 orchestrator 挂全局 AuthMiddleware(公开白名单/Internal-Token/JWT 三态)；S2 Go AuthMiddleware 查库校验用户存在+status==1；S3 ProxyAI 转发前对 /ops/*、/ipmi/*、/ai/nl2sql/execute 等高危及执行路径加 isAdmin 门控。
- 未改任何仓库源码，仅产出方案。
