# AIOps 全功能及真实性验收执行记录

执行日期：2026-08-27

## 1. 执行边界

- 依据《AIOps前端全功能及真实性验收测试方案_细化排查版_最终版》执行。
- 方案中识别到 113 个唯一测试编号；本记录只记录本轮实际执行结果，不将未执行项标记为通过。
- AI Chat、AI 调查新建/执行及涉及创建、删除、审批、扩缩容、授权的动作均按用户指令暂缓或未执行。
- DeepFlow Agent 熔断保持临时关闭，未重新开启。
- 未修改 DeepFlow 源码；只修改自研 AIOps 代码、部署配置、ClickHouse schema 和验证脚本。
- 两份用户验收文档未纳入本轮 Git 提交：
  - `AIOps前端全功能及真实性验收测试方案_细化排查版_最终版.md`
  - `部署验证.md`

## 2. 本轮修复与部署

### 追加修复：服务观测时间窗口

复测发现服务页面切换到“近 1 小时”后，服务列表仍显示近 24 小时计数。根因是前端只向拓扑接口传递 `minutes`，`/services` 发现与指标聚合接口固定使用 24 小时条件。

修复内容：

- `ResourceScope` 增加时间窗口，服务发现与服务指标聚合均使用 `start_time >= now() - INTERVAL N MINUTE`。
- `/api/v1/services` 校验并接收 `minutes` 参数，默认仍为 1440 分钟。
- 前端服务列表和拓扑节点详情均透传当前时间窗口。
- 增加服务 repository、handler 的时间窗口和非法参数回归测试。

真实回归结果：同一服务在近 24 小时窗口显示约 364 次，切换近 1 小时后显示约 53 次；底层 ClickHouse 同一时点窗口对比为近 1 小时约 39 次、近 24 小时约 350 次，数据随实时采集继续增长，窗口变化方向和数量级一致。服务详情在近 1 小时窗口显示约 52 次调用。连续两个自动刷新周期后，时间窗口和指标选择保持不变。

### Trace 查询架构

Trace 列表已从 `trace_spans` 明细实时全量 `GROUP BY trace_id` 改为 Trace Summary/Index 查询路径：

```text
trace_spans 明细
      ├─ trace_summary_index：候选 Trace 排序/分页
      └─ trace_summary_state FINAL：Trace 摘要读取

Trace 详情仍查询 trace_spans 明细
```

已增加并部署：

- `trace_summary_state`
- `trace_summary_index`
- 增量物化视图与历史回填/索引重建脚本
- Trace 列表分页和详情回归测试

### 日志真实性

- 日志原始列表改为真实 VictoriaLogs 查询，支持有界 `limit`。
- 非结构化日志按显式字段或保守消息规则推断标准级别；未知级别保持未知，不伪造为错误。
- 日志趋势、服务和级别聚合在新路由下直接使用 VictoriaLogs stats 查询。
- legacy ClickHouse `log_records` 路径保留，但不再把空表结果伪装成新路由真实日志。

### Grafana 上游错误隔离

- Grafana 上游 401/403 映射为明确的 `grafana authentication failed`，不再误判为 AIOps 平台 JWT 失效并跳回登录页。
- Grafana 健康检查真实返回正常；Grafana 搜索仍因上游要求认证而阻断，未绕过认证配置。

## 3. 真实数据核验

本轮从真实运行环境读取到的关键数据：

| 数据源 | 实际结果 | 判定 |
|---|---:|---|
| Platform ClickHouse `trace_spans` | 约 8.87M 行/24h 租户范围 | 真实数据存在 |
| Platform ClickHouse `trace_summary_index` | 约 8.76M 行 | Summary/Index 已接入 |
| Platform ClickHouse `trace_summary_state` | 约 8.76M 行 | Summary 已接入 |
| DeepFlow ClickHouse `flow_log.l7_flow_log` | 4,450,749 行 | DeepFlow 原始数据真实存在 |
| Platform ClickHouse `alert_events` | 115 行 | 告警事件真实存在 |
| Platform ClickHouse `k8s_events` | 42 行 | K8s 事件真实存在 |
| VictoriaLogs | 可查询真实日志；页面显示分页及聚合 | 日志链路真实接入 |
| Platform ClickHouse `log_records` | 0 行 | legacy 表为空；不代表 VictoriaLogs 为空 |
| Platform ClickHouse `service_topology` | 0 行 | 当前没有持久化关系数据；页面显示真实 0 关系 |

Trace 延迟抽样查询得到：8,874,298 条 span，P95 约 48.49ms，平均约 11.67ms，最大约 17.68s。Overview 页面在完整请求返回后显示真实 P95；首次短时间观察到的 loading/0 属于请求尚未完成，不作为数据缺失判定。

## 4. 前端验收结果

### 已实际验证通过

- 登录成功、错误密码失败、退出后受保护路由不可访问、未知路由显示 404。
- Overview 显示真实服务数、调用量、错误率、P95、CPU、内存、磁盘等数据。
- Service 列表显示真实服务调用量、错误量和延迟；服务详情可读取真实 Trace/Span。
- Service 拓扑显示真实节点；当前真实关系数为 0，未构造虚假边。
- Trace 列表显示真实记录；加载更多后分页范围扩大；Trace 详情显示真实 Span、服务、错误和瀑布信息。
- Logs 原始列表、错误筛选、趋势/服务聚合和 VictoriaLogs 来源标识均已验证。
- Alert Events 显示真实告警；严重级别筛选返回对应真实记录。
- Alert Rules 显示真实规则；规则详情读取验证通过。
- Investigation 中既有失败 Run 可读取；页面明确显示暂无真实 ToolRun，不伪造工具活动。
- K8s 资源列表显示真实 Deployment/Pod 等资源；破坏性执行按钮保持审批前禁用，未执行变更。
- VM 页面明确显示 KubeVirt 未安装；没有把未安装伪装为空 VM 列表。
- Capacity 页面显示真实 CPU、内存、磁盘及阈值/预测状态。
- Hardware 页面明确显示暂无节点健康、IPMI 传感器和 SEL 数据；当前环境未配置 IPMI。
- Grafana 健康检查显示真实版本和健康状态；上游未认证时页面保持登录态并显示明确错误。
- Changes、Reports、Approvals 页面空状态表达清晰，未伪造记录。
- Users 页面显示真实 admin 用户；未执行新增、编辑、删除。
- Settings 的 AI 配置状态、集群列表和集群详情只读展示已验证。

### 真实空数据或环境前置条件不足

- `service_topology` 当前为空，Service 拓扑显示 0 关系；Trace 数据以单 Span 为主，未人为补边。
- KubeVirt 未安装，因此 VM 相关资源无法执行真实 VM 验收。
- 硬件/IPMI/SEL 数据源未配置，因此硬件健康项只能验证 fail-closed 空状态。
- 集群详情节点/Namespace/Event 查询因注册集群缺少 kubeconfig 明确返回：`cluster has no kubeconfig, cannot query ...`。这是安全边界的 fail-closed 行为，不使用隐式 in-cluster fallback。

### 阻断或暂缓项

- Grafana 搜索：DeepFlow Grafana API 对未认证请求返回 401；需要配置服务账号/API token 或明确启用匿名 Viewer。当前未修改认证授权配置。
- AI Chat、KGPT、AI 调查新建/ToolRun/Evidence/RCA：按用户指令暂不验证；既有调查已确认暂无真实 ToolRun，不能判为通过。
- 涉及创建、删除、审批、扩缩容和授权的动作：未执行，等待逐项明确授权。
- 集群节点/Namespace/Event 的真实详情：需要注册可用 kubeconfig/credential identity；当前不绕过授权边界。

## 5. 自动化验证

以下命令在本轮修复后通过：

```text
ai-apm-query-go: go test ./...
observability-frontend: npm run test:run
observability-frontend: npm run build
bash deploy/scripts/test-deployment-contracts.sh
bash -n deploy/scripts/backfill-trace-summaries.sh
bash -n deploy/scripts/rebuild-trace-summary-index.sh
git diff --check
```

前端测试结果：14 个测试文件、24 个测试通过。后端 Go 全量测试通过。部署契约测试通过。

已部署并完成 rollout：

- query-api：`query-api:git-a0f4190-observability-fixes-v2`
- frontend：`observability-frontend:git-a0f4190-trace-summary-index-v10`
- 追加时间窗口修复版本：`query-api:git-73444aa786d7`、`observability-frontend:git-73444aa786d7`

## 6. Git 一致性

- 本地分支：`main`
- GitHub `origin/main` 当前保持上一批次提交：`8abd5a7`
- 本地 `HEAD` 为追加修复提交：`73444aa`
- 追加修复按“五个故障后同步 GitHub”约定暂未推送；当前环境已运行 `git-73444aa786d7` 精确版本。
- 提交：`fix: route traces through summary index and restore real observability reads`
- 工作区只保留两份未跟踪用户验收文档；实现文件已提交并推送。

## 7. 当前结论

Trace P0 的根因修复已完成：Trace 列表不再对数百万 Span 做不可扩展的全量高基数聚合，已切换到 Summary/Index 查询模型；真实日志接入和 Grafana 上游错误隔离也已完成并部署验证。

追加发现的服务观测时间窗口问题已修复并部署，服务列表、服务详情和自动刷新回归通过。

本轮不能宣布 113 项全部通过，因为 AI Chat、需要明确授权的变更动作、Grafana 上游认证、KubeVirt/IPMI 及缺少集群 kubeconfig 的详情查询仍分别属于暂缓、未授权或环境前置条件阻断项。
