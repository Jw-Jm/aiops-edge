# AIOps 智能可观测平台 — 全面使用与改进建议报告

> 测试时间：2026-08-12 10:00–10:40（北京时间）
> 测试环境：http://localhost:30253（admin/admin123，演示环境）
> 测试方式：GUI 黑盒逐页操作 + 真实 LLM 自然语言对话 + 后端 API/数据源核验 + 源码逻辑交叉分析
> 测试人角色：产品经理

---

## 一、总体结论

平台已具备"**指标 + 日志 + 链路 + 告警 + AI 诊断**"的完整产品骨架，**AICHAT 真实 LLM 的根因分析输出质量非常高**（数据引用准确、逻辑链条清晰、能诚实说明数据缺口），是本产品最有价值的模块。

但同时存在 **3 个 P0 级功能不可用问题**（告警规则无法创建、AI 处置闭环 403 失效、命令面板死链）和一批**跨页面数据口径不一致**问题，直接削弱了"全链路可观测 + AI 闭环处置"的核心卖点。

**一句话评价**：产品概念与 AI 分析能力突出，但"数据一致性"与"关键闭环可用性"未达到可直接交付的水平。

---

## 二、功能使用情况总览

| 模块 | 页面 | 结论 | 关键问题 |
|---|---|---|---|
| 总览 | 工作台首页 | ✅ 可用 | 告警数量口径不一致（222 vs 3）、健康度与告警矛盾 |
| 可观测 | 服务全景 | ⚠️ 数据异常 | 列表全部指标为 0，与总览 3126 次调用矛盾 |
| 可观测 | 链路追踪 | ✅ 可用 | 全部为 1 服务 1 span，无跨服务链路 |
| 可观测 | 日志与指标 | ⚠️ 数据质量 | 日志被 deepflow 流记录刷屏，无 ERROR 级应用日志 |
| 告警 | 告警事件 | ✅ 可用 | RCA「重新分析」不生效（恒返回缓存） |
| 告警 | 告警规则 | 🔴 P0 不可用 | **新建规则 100% 失败**（字段契约不匹配） |
| 智能运维 | AI 对话 | ✅ 优秀 | 真实 LLM 输出质量高；处置执行闭环 403 失效 |
| 智能运维 | AI 工具 | ⚠️ 部分 | NL2SQL 生成的 SQL 不准确 |
| 容量 | 容量预测 | ⚠️ 数据过期 | "当前值"是 63h 前的数据；ETT 未展示 |
| 报告 | 报告中心 | ✅ 可用 | 预览摘要过薄 |
| 管理 | 用户管理 | ⚠️ 有 Bug | 新增用户成功后弹窗不关闭 |
| 管理 | 系统设置 | ✅ 可用 | 审计日志操作人显示原始 ID |
| 全局 | ⌘K 命令面板 | 🔴 死链 | 监控面板 → /monitor 无路由，静默跳回首页 |
| 全局 | AI 浮动 Dock | ✅ 可用 | 快捷提问正常 |

---

## 三、逐模块使用详情与发现

### 3.1 工作台首页（/overview）

**表现正常的部分**：登录后直达首页，服务数量 12、调用总量 3,126、P95 延迟 29.4ms、AI 快问快答入口均正常展示。

**发现的问题**：

1. **告警数量口径严重不一致（P1）**
   - 首页"当前态势"显示 **严重 222**，"告警按服务分布"显示 kubernetes 严重 222；
   - 但侧边栏告警徽标显示 **3**，告警事件页只有 **3 条**（按规则聚合）。
   - 根因：首页统计的是内存中原始 firing 事件数（ClickHouse alert_events 表有 243 条，其中 240 条 firing critical），告警事件页按规则聚合为 3 条。同一批告警在首页显示"222"、在事件页显示"3"，用户无法对齐，**数字信任感被破坏**。
2. **系统健康度 100/100 与 222 条严重告警并存（P2）**：健康度仅按"近 24h 请求错误率"加权计算（当前错误率 0%），完全不纳入告警/可用性因素。严重告警满屏却显示"健康 100"，逻辑上会让用户困惑甚至误判。
3. **AI 快问快答交互语义混乱（P2）**：输入框旁有"分析根因/集群巡检"按钮，用户输入自定义问题后点"分析根因"，实际发出的是固定预设问题"分析 prod 集群故障根因"，**用户输入被忽略**（源码确认按钮硬编码 navigate('/ai/chat?q=分析 prod 集群故障根因')）。建议改为"分析根因 = 对当前输入提问"，或将按钮改为纯快捷入口标签。

### 3.2 服务全景（/observability/service）

1. **拓扑视图与列表节点数不一致（P2）**：拓扑显示"9 节点 · 13 关系"，服务列表显示 **12 个服务**。来源不同（拓扑来自 ClickHouse 聚合 + MySQL 拓扑关系 13 条；列表来自服务目录 + 拓扑节点合并），用户难以理解差异。
2. **服务列表全部指标为 0（P1 数据一致性）**：12 个服务的调用量/错误数/错误率/平均延迟全部为 0.00%，但总览页显示调用总量 3,126、P95 29.4ms，且 ClickHouse trace_spans 实际有 3,126 条数据（deepflow-server 448 次调用等）。
   - 根因：服务列表请求 `topology/global?minutes=60`（近 1 小时窗口），而全部 trace 数据为 08-11 02:00-02:08 的种子数据（约 32h 前），1 小时窗口内无数据 → 全零。总览用 `date >= today()-1`（24h 窗口）→ 有数据。
   - **影响**：核心页面"服务全景"展示全零，直接击穿"RED 指标实时监控"卖点。
3. **服务详情抽屉数据为空（P2）**：点击服务后健康度显示 "-- 未知"、无上下游调用、调用链/Span 明细均"暂无数据"。与列表全零同因。

### 3.3 链路追踪（/observability/trace）

- 列表、搜索（按 trace_id）、分页、刷新均正常；点击 trace 弹出详情，展示 trace 摘要与 span 信息。
- **所有 trace 均为"1 服务 / 1 span"（P2）**：没有任何跨服务调用链，瀑布图退化为单 span。与 README 宣称的"分布式调用链路瀑布图，毫秒级定位慢调用"有差距——demo 数据未构造跨服务链路。

### 3.4 日志与指标（/observability/log）

1. **日志检索可用但内容质量差（P1 数据质量）**：默认结果几乎全部是 `ingest -> deepflow-clickhouse /?query=SELECT...` 这类 **deepflow 流记录 SQL 文本**，被当作"日志"入库展示；没有真实应用日志、没有任何 ERROR 级日志。搜索 "error" 命中也是因为 SQL 文本里含 `client_error/server_error` 关键字。
   - 根因：log_records 表被 ingest 服务的 deepflow 流日志写满（3,189 条，同一 8 分钟窗口），真实应用日志缺失。
2. **超长 SQL 文本撑破行布局（P3）**：消息列出现数百字符的 URL 编码 SQL，整行无法阅读，存在横向溢出/换行错乱风险（截图证据 t07_log.png）。
3. **聚合统计正常**：按服务聚合条数（deepflow-clickhouse 164 条）能正确工作。

### 3.5 告警事件（/alerts/events）

- 当前告警 3 条（Pod 频繁重启 3673 次 / Deployment 不可用 344 次 / Pod OOMKilled 79 次），历史告警为空；严重度筛选、分页正常。
- **注意**：测试期间 Pod 频繁重启告警次数从 3653 持续增长到 3673，说明告警仍在后台活跃触发（存在告警循环触发，P2，可能造成告警风暴）。
- **RCA「重新分析」按钮不生效（P1）**：点击"根因分析"弹窗展示告警详情 + 预置结论"【处置方案】建议排查 deepflow-grafana 的变更事件和指标趋势"；点击"重新分析"后**结果完全不变**（等待 20s+ 仍为同一句），疑似直接读取 checkpoint 缓存而未真正触发 LLM 重算。结论还指向 deepflow-grafana，与"kubernetes Pod 频繁重启"告警相关性存疑。

### 3.6 告警规则（/alerts/rules）

1. **🔴 P0：新建规则 100% 失败**
   - 表单必填校验正常（空提交会提示），但填写完整后提交，后端恒返回 `name is required`，即使规则名称已填写。
   - 根因（源码确认）：前端 `createAlertRule({ rule_name, service_name, metric, threshold, severity })`，而后端 `AlertRule` 结构体字段为 `name` / `service`（`json:"name"` / `json:"service"`）。**字段名契约不匹配**，前端从未发送过后端要求的 `name/service`。
   - 影响：告警规则这一"6 类策略"核心能力的**创建入口完全不可用**，只能查看/删除内置规则。同时后端还要求 service 必填，前端该字段却非必填（即使修好字段名仍会触发"service is required"）。
2. **"历史告警"按钮跳转后不按规则过滤（P1）**：点击规则行的"历史告警"跳转到 `/alerts/events?rule=k8s-deployment-unavailable`，但事件页**完全忽略 `?rule=` 参数**（源码确认 load() 未读取 searchParams），展示全部 3 条告警。
3. 规则详情抽屉展示完整配置，正常。

### 3.7 AI 对话（/ai/chat）— 重点模块

**交互流程**：会话列表（新建/清空/单删）→ 快捷提问 → SSE 流式输出（步骤提示"案例匹配"等）→ AI 分析报告 → "🛠️ 处置建议 · 待确认"卡片（确认执行/驳回/自定义命令）→ 多轮追问 → 自动发起下一轮。整体交互合理、流畅。

**真实 LLM 输出评估（详见第五节）**：质量高、数据引用准确、会诚实声明数据缺口。

**发现的问题**：

1. **🔴 P0：处置建议"确认执行"端到端失效（含 403 与无反馈两层问题）**
   - 点击"确认执行"后**界面无任何反馈**（无"执行中/已执行/失败"提示），审计日志中也无记录。
   - 根因链路：
     a. 前端 `POST /api/v1/ai/suggestion/execute`；
     b. **nginx 将 `/api/v1/ai/` 前缀直接代理到 ai-orchestrator，绕过 query-api**；
     c. orchestrator 的 `_require_approver()` 要求 `X-Internal-Token`，该 token 只有 query-api 的 `ProxyAI` 会注入，直接访问时缺失 → 返回 403"请求来源不可信（内部 token 校验失败）"（已实测复现）；
     d. 前端 `handleExecute` 把错误写入 message.content，但 **suggestion 类型卡片渲染时忽略 content**，用户什么都看不到。
   - 影响：产品核心闭环"AI 诊断 → 生成方案 → 人工审批 → 安全执行"的**执行一环完全断裂**，只能看方案、无法执行。
2. **AI 分析文本在聊天流中重复渲染（P2）**：每条 AI 回复出现两次——先是一条完整"## 分析报告"消息，随后"处置建议"卡片内又嵌入同一段全文（卡片 `plan` 字段 = 完整分析文本）。视觉冗余，浪费屏幕空间，建议卡片只放"方案要点 + 命令"。
3. **无命令时的空卡（P3）**：第一轮分析生成处置建议时脚本为 `(无命令)`，卡片出现"确认执行"按钮却无可执行内容，交互上让人困惑。建议无命令时隐藏执行按钮或引导"向 AI 索取命令"。
4. **风险评分未展示（P3）**：SSE 已返回 `risk_score/risk_reason`，但本次对话中卡片未显示风险分值（代码仅在分值非 0 时渲染）。
5. **长耗时无取消/超时提示（P3）**：每次分析 40-90s，有 AbortController（120s 超时）但界面无"取消"按钮，超时也只弹一行小字。

### 3.8 AI 工具（/ai/tools）

1. **NL2SQL 生成的 SQL 不准确（P1）**：输入"查询近24小时调用量最高的5个服务及其错误率"，生成：
   ```sql
   SELECT service_name, countIf(is_error = 1) AS errors, count() AS calls
   FROM observability.trace_spans
   GROUP BY service_name ORDER BY errors DESC LIMIT 100
   ```
   问题：① 无时间过滤（丢了"近24小时"）；② `ORDER BY errors DESC` 应为 `calls DESC`（用户要"调用量最高"）；③ `LIMIT 100` 应为 5；④ 无错误率计算（只有错误计数）。SQL 虽可执行，但与用户意图偏离较大。
2. MCP 工具列表（8 个：query_metrics/query_traces/k8sgpt_diagnose/execute_shell 等）与技能目录（8 项）展示完整、分类清晰，作为只读清单正常。

### 3.9 容量预测（/capacity）

1. **"当前值"为过期数据（P1 数据质量）**：CPU 当前值 54.88%、内存 56.69%、磁盘 34.40% 均展示为"当前值"，但 VictoriaMetrics 中 node-exporter 指标**最后样本在约 63 小时前**（实测 `node_cpu_seconds_total` 即时查询为空、query_range 末端时间戳 ≈ 63h 前）。即容量页展示的"当前"实际是 2.5 天前的状态。
2. **ETT（触达阈值时间）缺失（P1 产品缺口）**：API 已返回 `ewma.ett_seconds=5700`（约 1.6 小时后触达阈值）等预测数据，但**前端完全没有渲染 ETT**，而 README 把"预测资源触达阈值的时间（ETT），提前扩容避免故障"列为容量预测的核心卖点。功能在 API 层存在，UI 层缺失。
3. **环比变化恒为 0.00%（P2）**：三个指标环比全部 0.00%，与 API 返回 `change_pct: 0` 一致。数据稀疏/缺口导致对比计算退化为 0，展示意义不大。

### 3.10 报告中心（/report）

- 报告列表（巡检报告）、预览抽屉、下载（.md）均正常。
- **报告摘要过薄（P3）**：预览内容仅一行 `**目标**: | **问题**: ...`，没有分析结论/风险/建议摘要，价值有限。建议把 AI 报告的关键结论写入 summary 字段。

### 3.11 用户管理（/admin/users）

1. **🔴 P1：新增用户成功后弹窗不关闭**：创建用户成功（列表可见、MySQL 确认落库），但"新增用户"模态框**保持打开**，需手动取消。已复现两次。（源码 `setOpen(false)` 在 `.then` 中，实际未生效/被覆盖，待进一步定位。）
2. 编辑、删除（带确认气泡）均正常。

### 3.12 系统设置（/admin/settings）

- AI 模型配置：DeepSeek / deepseek-v4-flash / 已配置，密钥脱敏展示，含"测试当前配置/获取模型/保存"；实测"测试当前配置"返回 `Connection successful, model=deepseek-v4-flash`。
- 纳管集群：kubernetes-cluster（v1.35.6+orb1，1 节点，active），同步/纳管入口存在。
- **审计日志（P2）**：操作人列显示 **"1"**（原始用户 ID 而非用户名），目标服务/命令列大量为空。演示数据本身 OK，但展示口径需美化。

### 3.13 全局功能

1. **🔴 P1：⌘K 命令面板死链**：面板列出"监控面板 (G M)"，点击跳转 `/monitor`——该路由不存在，被 catch-all 静默重定向到工作台首页。用户按快捷键期望去"监控面板"，结果回到首页，体验断裂。（源码：NAV/命令面板与路由表不一致，监控面板页面已下掉但入口未删。）
2. 命令面板标签与导航不一致（"日志检索" vs 导航"日志与指标"），小瑕疵。
3. AI 浮动 Dock：入口、快捷提问（分析集群根因/集群巡检/服务延迟排查）、问答流均正常。
4. 集群选择器（全部集群/kubernetes-cluster）：存在但当前仅 1 个集群，切换意义有限。

---

## 四、数据源核验结果

### 4.1 ClickHouse（trace_spans / log_records / alert_events）

| 表 | 行数 | 说明 |
|---|---|---|
| trace_spans | 3,126 | **种子数据**，时间窗仅 `2026-08-11 02:00:02 ~ 02:08:08`（约 8 分钟，32h 前），无实时写入 |
| log_records | 3,189 | 同一 8 分钟窗口；内容为 deepflow 流记录 SQL 文本 |
| alert_events | 243 | 240 firing critical + 3 acknowledged critical |
| service_topology | 0 | **空表**（拓扑降级到 MySQL 关系） |
| metric_service_red | 0 | 空表 |

**结论**：链路/日志数据为一次性种子数据，**当前无实时 trace/log 采集**。这直接导致"近 1 小时/近 30 分钟"类查询（服务全景、服务详情）全部为空，而 24h 窗口查询（总览）有数据。

### 4.2 MySQL（服务目录 / 拓扑关系）

- topology_nodes 13、topology_relations 13（与拓扑"13 关系"一致）；
- **service_catalog / service_metadata 均为 0 行**——README 宣称"服务目录/拓扑关系"存于 MySQL，服务目录实际为空（前端 /services 的 tier/team/owner 等元数据列也全空）；
- alert_rules 6、audit_logs 8、approval_tasks 0、slo_targets 等表存在（slo 表是否有数据未逐一核对）。

### 4.3 VictoriaMetrics（容量 / 监控）

- 平台自监控指标（process_*、ai_ingest_*、aio_*）**实时**写入；
- **node-exporter 指标（node_cpu/node_memory 等）已停止写入约 63 小时** → 容量预测页展示的是过期"当前值"。

### 4.4 VictoriaLogs（日志）

- 实时写入 K8s Pod 生命周期事件（namespace/pod/phase，最新时间戳与当前时间同步），是四个数据源中**唯一在持续产生新数据**的。

### 数据源核验总结

> 平台四类数据源连通性正常，但**业务数据（trace/log）停留在 32h 前的种子数据、容量指标停留在 63h 前**，只有基础设施事件在实时流入。任何依赖"近期时间窗"的业务视图（服务全景/详情/容量）都会呈现空或过期状态，与总览的 24h 聚合数字形成明显矛盾。

---

## 五、AICHAT 真实 LLM 对话评估（重点）

> 环境确认：`LLM_MOCK=false`，模型 deepseek-v4-flash（DeepSeek 官方 API），"测试当前配置"返回成功。以下评估基于 3 轮真实对话（2 轮新对话 + 1 轮多轮追问）。

### 5.1 测试问题与输出摘要

| # | 用户问题 | AI 输出要点 |
|---|---|---|
| 1 | 为什么集群出现 Pod 频繁重启和 OOMKilled 告警？请分析根因并给出处置建议 | 判定"严重内存资源紧张"；正确引用告警计数（3655/79/344）与单节点 8C/12G 环境；区分容器级 OOM 与节点压力；给出 4 层处置建议（止血/资源优化/集群层面/监控预防）；指出数据缺口（无法定位具体 Pod） |
| 2 | 具体是哪个 Pod 触发了 OOMKilled？请给出可执行命令 | **诚实回答"当前数据无法定位"**；给出 6 条只读 kubectl 命令（get/describe/logs/top）用于人工排查；标注"均为只读、安全" |
| 3 | （预设）分析 prod 集群故障根因 | 完整 6 段报告：核心判断/根因/风险/数据缺口/处理建议/结论；提出故障链"节点内存不足→OOM→重启→Deployment 不可用"；明确排除无关原因（非 ES 分片、非 Nginx） |

### 5.2 内容准确性核验（对照数据源）

| AI 表述 | 核验结果 |
|---|---|
| Pod 频繁重启触发 3655/3666 次 | ✅ 与 alert_events/事件页一致（且随时间递增 3653→3673） |
| OOMKilled 79 次、Deployment 不可用 344 次 | ✅ 与事件页一致 |
| 单节点 8 CPU / 12GiB（12305980Ki） | ✅ 与 K8s 实际节点一致 |
| 14 个 Pod（含 2 个 Succeeded init 任务） | ✅ 与集群实际 Pod 列表一致 |
| 集群仅 1 个节点 orbstack | ✅ 与纳管集群信息一致 |
| 无"节点资源压力"告警 → 更可能是容器级 OOM | ✅ 逻辑正确（当前告警仅 3 类，无 node_pressure） |
| 无法从告警定位具体 OOM Pod | ✅ 诚实——告警确实不含 Pod 名（数据缺口） |

**结论：AI 输出的核心数字与系统数据源完全对齐，推理链条符合运维逻辑，未发现幻觉编造。**

### 5.3 交互合理性

- **好的方面**：流式输出 + 阶段进度提示；分析后可追问，多轮上下文保持正确（追问时引用了上一轮的"3656 次"）；处置建议卡 + 审批按钮符合"人工审批"安全模型；快捷问题入口（首页/Dock/对话页）一致。
- **可改进方面**：① 处置执行按钮实际不可用（见 3.7-1，P0）；② 分析文本重复渲染；③ 无命令时出现空执行卡；④ 无风险评分展示；⑤ 40-90s 等待无取消入口；⑥ NL2SQL 与 AICHAT 是两套能力，AICHAT 内未打通"把生成的 SQL 直接去执行/查询"。

---

## 六、代码逻辑问题清单（按优先级）

### P0 — 核心功能不可用

| # | 问题 | 位置 | 说明 |
|---|---|---|---|
| 1 | 新建告警规则 100% 失败 | 前端 `AlertRules.tsx` submit 发送 `rule_name/service_name`；后端 `alerts.go` `AlertRule` 结构体为 `name/service` | 字段契约不匹配，恒返回 "name is required" |
| 2 | AI 处置"确认执行"端到端 403 | `nginx.conf` 将 `/api/v1/ai/` 直连 orchestrator，绕过 query-api 的 `X-Internal-Token` 注入；orchestrator `_require_approver` 拒收 | 闭环执行环节完全断裂；前端又因 suggestion 卡片忽略 content 而无任何反馈 |

### P1 — 功能受损 / 明显缺陷

| # | 问题 | 位置 |
|---|---|---|
| 3 | 新增用户成功后模态框不关闭 | `AdminUsers.tsx` submit .then 中 setOpen(false) 未生效 |
| 4 | 规则"历史告警"的 `?rule=` 参数被忽略 | `AlertEvents.tsx` load() 未读取 searchParams |
| 5 | 命令面板"监控面板"死链（/monitor 无路由） | `CommandPalette.tsx` 与 App.tsx 路由不一致 |
| 6 | 告警数量口径不一致（首页 222 vs 事件页 3） | `DashboardStats` 统计原始 firing 事件；事件页按 rule 聚合 |
| 7 | 服务全景/详情全零（近 1h 窗口 vs 种子数据） | `ServiceObservability` 请求 minutes=60；数据为 32h 前种子 |
| 8 | 容量页"当前值"过期 + ETT 未展示 | 容量 API 返回 ETT 但 `Capacity.tsx` 未渲染；node 指标 63h 未更新 |
| 9 | RCA"重新分析"恒返回缓存结果 | `alerts.go`/前端 rca 逻辑直接读 checkpoint |
| 10 | NL2SQL 结果不准确（无时间过滤/排序错/LIMIT 错/无错误率） | ai-orchestrator nl2sql prompt |

### P2 — 体验 / 展示问题

| # | 问题 |
|---|---|
| 11 | 系统健康度 100 与严重告警 222 并存（健康度模型未纳入告警/可用性） |
| 12 | 日志检索被 deepflow 流 SQL 刷屏、无 ERROR 应用日志；超长 SQL 撑破表格 |
| 13 | 所有 trace 仅 1 服务 1 span，无跨服务链路 |
| 14 | AI 分析文本在聊天流重复渲染；无命令时空执行卡；风险评分未展示 |
| 15 | 审计日志操作人显示原始用户 ID "1" |
| 16 | 首页"分析根因"按钮忽略用户输入（硬编码预设） |
| 17 | 告警仍在后台持续高频触发（测试中 3653→3673），存在告警风暴隐患 |

### P3 — 小瑕疵

| # | 问题 |
|---|---|
| 18 | 命令面板"日志检索"与导航"日志与指标"标签不一致 |
| 19 | 报告预览摘要过薄（仅"目标/问题"一行） |
| 20 | 服务详情健康度"-- 未知"文案 |
| 21 | 容量页环比恒 0.00% 无解释 |

---

## 七、产品改进建议（按优先级）

### 7.1 立刻修复（阻断交付，1 周内）

1. **修复告警规则创建字段契约**：前端按后端 `name/service` 发送（或后端兼容 `rule_name/service_name`），并将"服务名称"改为必填（后端已强制）。顺手统一规则列表的 service 展示。
2. **打通 AI 处置执行闭环**：将 `/api/v1/ai/` 中需要内部鉴权的路径（suggestion/execute 等）改经 query-api 代理（或在 nginx 层注入 X-Internal-Token），并修复 suggestion 卡片对执行状态/结果/错误的渲染反馈。修复后全链路验证：确认执行 → 命令执行 → 审计落库 → 自动下一轮分析。
3. **删除或补全命令面板死链**：下线"监控面板"入口或补齐路由。
4. **统一告警口径**：首页"严重 N"改为与告警事件页一致的口径（按规则聚合或明确标注"触发次数"），并加 tooltip 说明。

### 7.2 短期优化（1 个月内）

5. **解决"近期时间窗为空"问题**：① 尽快接入真实/持续的数据采集（trace/log）；② 作为演示兜底，服务全景/详情的时间窗默认放宽（如 24h），并对"数据为空"给出明确空态文案而非静默全零。
6. **容量预测页补齐 ETT**：API 已返回 ETT，前端渲染"预计 X 小时后触达阈值"及风险提示，兑现 README 承诺；同时修复 node 指标采集中断，避免展示 63h 前的"当前值"。
7. **优化 NL2SQL**：约束 prompt 包含时间过滤、按需求排序/LIMIT、正确计算错误率，并在结果旁提供"复制/执行"入口。
8. **修复用户管理弹窗关闭问题**；审计日志操作人展示用户名。
9. **AI 对话去重与反馈**：卡片只展示方案要点+命令；确认执行增加 loading/成功/失败状态；无命令时不渲染执行按钮；展示风险评分。

### 7.3 中期产品化（1 个季度）

10. **健康度模型升级**：纳入告警严重度、可用性（SLO）、容量风险等维度，输出"健康分 + 扣分原因"，避免"满屏严重告警但健康度 100"。
11. **告警风暴治理**：当前告警在持续高频触发，建议实现聚合降噪/抑制（同规则冷却已存在但效果不足），并补充告警通知渠道配置。
12. **服务目录完善**：填充 MySQL service_catalog（团队/负责人/层级），服务列表/详情才能承载"归属与治理"能力。
13. **跨服务链路数据构造**：补充多服务调用的 demo trace 数据，让链路瀑布图与拓扑真正"可下钻"。
14. **报告价值提升**：把 AI 分析结论/风险/处置建议写入报告 summary，并支持定时巡检报告与导出 PDF/Word。
15. **AI 能力扩展**：把 NL2SQL 生成的 SQL 接入 AICHAT 直接执行并回填结果；为"确认执行"补充预演/回滚机制，强化"安全执行"卖点。

---

## 八、测试资产与证据

- 截图证据目录：`gui-test-screenshots/`（t01~t20，含登录、各页面、RCA 弹窗、AI 分析结果等）
- 本报告关键结论均有"GUI 操作 + 后端 API/数据源 + 源码"三方交叉验证（详见各小节）。

### 附录：测试过程说明

- 登录 admin/admin123，逐页走查 13 个页面 + 命令面板 + AI Dock。
- AICHAT 使用真实 DeepSeek LLM（LLM_MOCK=false），完成 3 轮自然语言对话。
- 数据源核验通过 kubectl 直查 ClickHouse/MySQL/VictoriaMetrics/VictoriaLogs。
- 测试产生的脏数据已清理：创建的测试用户（pm_test_user、pm_test2）均已删除；未执行任何破坏性命令。
