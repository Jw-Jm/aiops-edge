# AIOps 平台全面技术审核与生产整改最终报告

> **文档用途**：本文件是 `Jw-Jm/aiops-edge` 当前代码基线的最终技术审核结论，也是后续智能体执行整改的唯一任务约束。后续修复不得仅以“代码已修改”“测试局部通过”作为完成依据，必须逐项满足本文给出的代码边界、验证方法和关闭条件。
>
> **审核日期**：2026-09-03  
> **仓库**：`https://github.com/Jw-Jm/aiops-edge`  
> **审核基线**：`main@bd93acd8974b3b5132061698ab517867640ba96c`  
> **原始审核材料**：`AIOps_全面审核报告_2026-09-03.md`  
> **当前发布结论**：**NO-GO / NOT PUBLISHABLE**

---

## 1. 审核结论

当前平台不是“架构不可用”或“功能为演示实现”的项目。经复核，V2 主体架构中的以下设计仍然成立，并且是后续整改必须保留的基础：

- MySQL 作为 IAM、Session、Scope、Run、Chat、Action 等权威数据的唯一 SoT；
- Query API 负责用户认证、租户/集群 Scope、运行与动作状态；
- Run/Action 使用 outbox、lease、fencing、reconcile 处理重复投递、未知执行结果和进程故障；
- Kubernetes 写操作收敛到 `query-api -> credential-broker -> ai-action-executor`；
- Orchestrator/Investigation 链路原则上只读取事实，不应直接持有生产写权限；
- 服务间具备 mTLS、方向性服务令牌、签名上下文、nonce 防重放等安全机制；
- Event Collector 的 WAL、ack 水位、replay、背压设计是当前可靠性体系的重要资产；
- Production Helm 已具备 default-deny NetworkPolicy、Secret 占位符拒绝、legacy mutation 默认关闭等基础能力。

但是，原审核报告附录中若干“已修复”结论过于乐观。按当前 `main@bd93acd` 重新核查后，至少存在以下生产发布阻断：

1. `rca.py::_run_kubectl_safe` 仍允许白名单管道程序通过文件参数读取 orchestrator 容器内可读文件，原 P1-S1 **只完成了部分修复**；
2. 当前 `main` 对应 GitHub Actions `aiops-workflow-gates` **实际失败**，最新候选代码没有完整绿色 CI 证据；
3. `main` 当前未启用 Branch Protection / Ruleset / Required Status Check，红色提交仍可成为主分支 HEAD；
4. CI 使用 Python 3.14，而 production Dockerfile 基于 Python 3.12，且生产镜像依赖来自 `bin/*.tar.gz` 离线快照，CI lock 与真实 runtime dependency closure 不一致；
5. production 镜像部署模板强制使用 tag，Helm helper 明确拒绝 `repository@sha256:digest`，与不可变镜像发布要求不一致；
6. 最新 CI 的 `npm ci` 明确报告 `1 critical + 2 high + 6 moderate`，但当前没有完成生产依赖可达性分析，也没有 SCA 阻断门禁；
7. 当前 HEAD 的安全修复发生在旧 release evidence 之后，既有镜像、Helm、运行态和 RCA evidence 不能继承到 `bd93acd`；
8. Orchestrator legacy/V2 双运行时仍未物理收敛，当前仍主要依赖环境开关维持安全边界；
9. workflow E2E 和部分测试仍存在“测试自建模型”“检查源码字符串”“仅检查文件存在”等形式化门禁；
10. Production 容器安全上下文仍偏弱，`ai-orchestrator` 镜像默认 root，Helm 通用 securityContext 当前主要只有 `fsGroup`。

因此，本报告的最终结论是：

> **当前 `main@bd93acd` 不具备生产发布资格。**  
> 必须先关闭本文定义的所有 P1/Release Blocker，再针对新的最终 commit 重新构建、重新部署、重新测试、重新生成完整 release evidence。旧提交、旧镜像、旧运行态、旧测试结果均不得作为最终版本的发布证据。

---

## 2. 严重度定义与关闭原则

### 2.1 严重度

| 等级 | 定义 | 发布规则 |
|---|---|---|
| P0 | 已证实可造成未经授权的直接生产控制、远程代码执行、关键凭据直接泄漏或大范围数据破坏 | 任意 1 项未关闭即 NO-GO |
| P1 | 安全边界实质缺口、权威状态不可靠、发布门禁可绕过、候选版本无可重复构建/验证证据 | 任意 1 项未关闭即 NO-GO |
| P2 | 低概率安全风险、架构债务、测试质量不足、性能/维护性问题 | 不得遗忘；必须给出明确整改状态 |
| P3 | 工程优化、文档治理、非阻断改进 | 可进入后续治理计划 |

### 2.2 问题关闭必须同时满足

一个问题只有同时满足以下条件才能标记为 `CLOSED`：

1. **根因消失**，不能只是封堵报告中的单一 PoC；
2. 有至少一个针对原失败场景的**回归测试**；
3. 有至少一个针对相邻绕过路径的**负向测试**；
4. 修复后全量相关测试通过；
5. 不通过关闭安全检查、放宽断言、删除测试、改成 mock 的方式“变绿”；
6. 若涉及生产配置，必须检查 **Helm 最终渲染结果**，不能只看 values；
7. 若涉及发布链路，必须绑定**最终 commit SHA + image digest + rendered manifest digest**；
8. 结论必须保存可复核证据，不能只写“已验证”。

---

## 3. 后续智能体修复的强制约束

以下约束优先于任何局部修复便利性。任何智能体不得违反。

### 3.1 架构边界不得破坏

- MySQL 继续作为 IAM/Run/Chat/Action 的唯一权威来源；不得新建第二套本地权威状态。
- Orchestrator/Investigation Worker 不得新增生产 MySQL、ClickHouse、Kubernetes 写凭据。
- Kubernetes mutation 必须保持 `Query API -> Credential Broker -> Action Executor -> Kubernetes API`。
- 不得为了绕过问题重新开启：
  - `LEGACY_FLOW_RUNTIME_ENABLED=1`
  - `LEGACY_DIRECT_MUTATIONS_ENABLED=1`
  - 未审批的 `EXECUTION_AFTER_APPROVAL=1`
- 不得通过扩大 ClusterRole verbs 为 `create/update/patch/delete` 来解决只读调查问题。
- 不得把 Provider API Key 回传给 Orchestrator；LLM Provider Key 继续只存在于 egress proxy。
- 不得以关闭 mTLS、NetworkPolicy、签名、nonce、审批验签方式解决联调失败。

### 3.2 安全修复不得使用的方式

禁止以下做法：

- 在 `_run_kubectl_safe` 上继续堆叠危险字符正则作为最终安全边界；
- 用 `shell=True` 执行 LLM 可影响的命令；
- 允许模型自由输出任意 `awk`、`grep -f`、文件路径参数；
- 在 CI 中使用 `|| true`、`continue-on-error: true` 掩盖门禁失败；
- 把 npm/pip 漏洞检查设为仅日志输出；
- 修改测试期望值来适配明显错误行为而不说明原因；
- 删除真实生产测试，只保留 fixture/self-harness；
- 将生产 Secret 放入 Git、CI log、镜像层或 Helm 明文 values；
- 用 mutable tag 作为最终 production release identity；
- 用旧 release evidence 证明新 commit 可发布。

### 3.3 每个整改任务的交付要求

每个任务必须输出：

1. 修改文件清单；
2. 根因说明；
3. 代码修复；
4. 新增/修改测试；
5. 执行过的验证命令；
6. 验证结果；
7. 是否改变架构契约；
8. 是否产生新的兼容性风险；
9. 关联 commit SHA；
10. 关闭结论：`CLOSED / PARTIAL / BLOCKED`。

不得只输出“修复完成”。

---

## 4. 当前问题总表

| 编号 | 级别 | 问题 | 当前状态 | 是否阻断发布 |
|---|---|---|---|---|
| P1-S1 | P1 | kubectl 安全执行仍可通过管道工具文件参数读取容器文件 | OPEN | 是 |
| P1-CI1 | P1 | 当前 `bd93acd` GitHub Actions 实际失败 | OPEN | 是 |
| P1-GOV1 | P1 | main 无 Branch Protection/Ruleset/Required Check | OPEN | 是 |
| P1-SUP1 | P1 | CI Python 3.14 与生产 Python 3.12 不一致；runtime 依赖不受 CI lock 完整约束 | OPEN | 是 |
| P1-SUP2 | P1 | Helm self-owned image 强制 tag、拒绝 digest pin | OPEN | 是 |
| P1-SCA1 | P1 | npm 报告 critical/high advisory，未完成 triage 和门禁 | OPEN | 是 |
| P1-REL1 | P1 | 当前 HEAD 缺少重新绑定的 release evidence | OPEN | 是 |
| P1-A1 | P1 架构债 | Orchestrator legacy/V2 双运行时未物理收敛 | OPEN | 上线前应关闭或形成严格例外证明 |
| P1-R1 | P1/待证 | legacy ApprovalStore 仍允许 MySQL 写失败后内存降级 | PARTIAL | 必须证明生产链不使用，否则阻断 |
| P2-S6B | P2 | LLM egress proxy token 比较仍为普通字符串比较 | OPEN | 否 |
| P2-S9 | P2→并入供应链阻断 | 本地二进制/模型/tar 依赖无完整 hash manifest | OPEN | 已并入 P1-SUP1 |
| P2-S11 | P2 | `:memory:.ses` 类落盘根因尚未正式关闭 | 待确认 | 否 |
| P2-R5 | P2 | `LLM_MOCK` 代码默认 true | OPEN | 准生产风险 |
| P2-R6 | P2 | investigator 文档“默认 true”与实际默认 false 漂移 | OPEN | 否 |
| P2-A2 | P2 | 前端 GLOBAL_PATHS + 每请求 localStorage JSON.parse | OPEN | 否 |
| P2-F1 | P2 | workflow-e2e 测试自建 Harness，不测生产路径 | OPEN | 是，作为发布证据时阻断 |
| P2-F2 | P2 | 源码字符串测试/文件存在性测试仍存在 | OPEN | 否，但必须整改 |
| P2-SEC1 | P2 | 自研容器安全上下文不完整、orchestrator 默认 root | OPEN | 建议上线前关闭 |
| P2-HA1 | P2/生产就绪 | Orchestrator RWO 单副本、状态未外部化 | OPEN | 取决于正式 SLO，建议生产前关闭 |

---

# 5. P1-S1：重构 kubectl 调查执行路径

## 5.1 已确认根因

当前 `ai-orchestrator/rca.py::_run_kubectl_safe` 已经补充 `<` 到危险字符集合，但仍存在以下结构性问题：

1. 使用 `shell=True`；
2. 首段只检查字符串是否以 `kubectl ` 开头；
3. 后续管道只检查工具名是否属于：
   - `tail`
   - `head`
   - `sort`
   - `grep`
   - `tr`
   - `wc`
   - `awk`
   - `echo`
4. **不校验这些工具的参数**；
5. LLM 生成的 `proposed_check` 可以直接进入 `cluster_check -> _run_kubectl_safe`，不局限于固定模板；
6. ai-orchestrator Pod 环境中存在内部 token、签名 key、LLM proxy token，并挂载内部 TLS 文件。

因此，下列类型的输入不依赖 `<` 也可能读取本地文件：

```text
kubectl get pods | head /proc/self/environ
kubectl get pods | tail /proc/self/environ
kubectl get pods | sort /proc/self/environ
kubectl get pods | grep token /proc/self/environ
```

风险边界应准确描述为：

> **可读取 ai-orchestrator 容器内当前进程权限可读的文件/环境，而不是未经证据证明的“宿主机任意文件”。**

## 5.2 唯一推荐修复方案

彻底移除 RCA 调查路径中的通用 shell。

### 5.2.1 新增结构化检查模型

建议新增：

`ai-orchestrator/cluster_checks.py`

定义受控对象，例如：

```python
@dataclass(frozen=True)
class ClusterCheck:
    kind: Literal[
        "pod_events",
        "pod_restarts",
        "pod_oom",
        "pod_waiting",
        "node_status",
        "node_usage",
        "pod_usage",
        "deploy_replicas",
        "svc_endpoints",
        "describe_pod",
    ]
    namespace: str | None = None
    pod: str | None = None
```

LLM 只允许输出 `kind + namespace + pod` 这类结构化参数，不再输出 shell command。

### 5.2.2 参数验证

- namespace：按 Kubernetes DNS label / subdomain 规范验证；
- pod：按 K8s object name 允许字符验证；
- 禁止任意路径；
- 禁止 `/`、`..`、`~` 作为参数；
- 不允许调用方提供 argv flag；
- JSONPath、custom-columns 等表达式只能在代码模板内静态定义。

### 5.2.3 kubectl 使用 argv 执行

必须：

```python
subprocess.run(
    ["kubectl", "get", "pods", "-n", namespace, "-o", "wide"],
    shell=False,
    ...
)
```

禁止：

```python
subprocess.run(command_string, shell=True)
```

### 5.2.4 后处理改用 Python

原 `tail/head/grep/tr/sort/wc/awk` 的用途全部在 Python 中实现。

例如：

- tail：`lines[-20:]`
- head：`lines[:20]`
- grep：受控字符串匹配/正则；正则由程序定义，不接受文件参数
- sort：`sorted(lines)`
- wc：`len(lines)`
- tr：只做固定空格转换行
- awk：删除，不允许通用 awk

### 5.2.5 输出限制

- stdout 最大保留 2000~8000 字符，按当前产品需求统一；
- stderr 必须净化后返回；
- 超时固定上限；
- 禁止把内部环境变量、命令执行完整 exception dump 给 LLM；
- 审计记录必须包含 `check_kind / namespace / pod / exit_code / duration / truncated`，不得记录 Secret。

## 5.3 必须新增的安全测试

至少新增：

`ai-orchestrator/tests/test_cluster_checks_security.py`

必须覆盖：

```text
1. 任何 check 都不得产生 shell=True
2. namespace='../x' -> reject
3. pod='/proc/self/environ' -> reject
4. pod='../../etc/passwd' -> reject
5. 任意自由 shell 字符串接口 -> 不存在/拒绝
6. 不允许 kubectl exec
7. 不允许 kubectl cp
8. 不允许 kubectl port-forward
9. 不允许 kubectl proxy
10. 不允许 kubectl apply/create/delete/patch/replace/edit
11. 不允许调用方自定义 -o jsonpath
12. 不允许调用方传入 --kubeconfig
13. 不允许调用方传入 --token
14. 不允许调用方传入 --server
15. 固定模板仍能正常读取 events/pods/nodes/deployments/endpoints
```

## 5.4 关闭条件

必须全部成立：

```bash
git grep -n "shell=True" -- ai-orchestrator/rca.py ai-orchestrator/cluster_checks.py
```

RCA/cluster check 真实执行路径不得存在命中。

```bash
cd ai-orchestrator
python -m pytest -q
```

全量通过。

并增加针对 `/proc/self/environ`、`/etc/passwd`、`../`、`--kubeconfig` 等负向测试。

只有完成上述结构性改造，P1-S1 才允许标记 CLOSED。

---

# 6. P1-CI1：修复当前 HEAD 的真实 CI 失败

## 6.1 当前事实

最新 `bd93acd` 对应 `aiops-workflow-gates` 为 `failure`。

失败点：

```text
ai-apm-query-go/internal/api/capacity_test.go
TestEWMAConstant
EWMA constant = 2.9999999999999996, want 3
```

当前测试使用精确浮点比较：

```go
if v != 3
```

而 EWMA 为正常浮点计算：

```go
out[t] = alpha*series[t] + (1-alpha)*out[t-1]
```

这属于测试断言不合理，不代表可以绕过测试。

## 6.2 修复方式

将常数 EWMA 断言改为误差判断：

```go
if math.Abs(v-3) > 1e-12 {
    t.Fatalf(...)
}
```

原则：

- 不修改 EWMA 生产算法来迎合测试；
- 不使用四舍五入修改生产结果；
- 不删除该测试；
- 增加至少一个非整数输入测试，防止后续有人错误引入 rounding。

例如：

```text
series=[0.1,0.1,0.1,0.1]
期望每项与 0.1 的误差 < epsilon
```

## 6.3 CI 结构整改

当前所有门禁集中在一个 shell 脚本，G0 一失败，后续 G0.5/G1/G2/G3/G4/G5 均没有证据。

建议把 workflow 拆为独立 jobs：

```text
go-query-tests
workflow-contract-tests
orchestrator-tests
executor-tests
frontend-tests
helm-contract-tests
supply-chain-tests
production-image-build
release-gate
```

最后：

```yaml
release-gate:
  needs:
    - go-query-tests
    - workflow-contract-tests
    - orchestrator-tests
    - executor-tests
    - frontend-tests
    - helm-contract-tests
    - supply-chain-tests
    - production-image-build
```

这样单项失败不会阻止其他检查产生诊断结果，但最终 release-gate 必须全部成功。

## 6.4 关闭条件

最终 commit 对应 Actions 必须：

```text
status=completed
conclusion=success
```

且所有 required jobs 均有成功记录。

不得以 rerun 偶然成功作为唯一证据；如果存在 flaky，必须先修 flaky 根因。

---

# 7. P1-GOV1：主分支必须启用不可绕过门禁

## 7.1 当前根因

当前 GitHub 状态：

```text
main.protected = false
rulesets = []
required_status_checks = off
```

因此 workflow 即使失败，提交仍已经进入 main。

## 7.2 修复要求

这是**仓库设置整改**，不是代码修改。

推荐使用 Repository Ruleset：

- Target：`refs/heads/main`
- Require pull request before merging：开启
- Require status checks to pass：开启
- Required check：最终聚合 `release-gate`，或所有核心 jobs
- Block force pushes：开启
- Block deletions：开启
- Require conversation resolution：建议开启
- Require signed commits：如组织具备签名基础设施则开启
- Bypass：默认不允许；如必须保留 break-glass，只允许特定管理员组并要求审计

如果项目暂时仍为单人开发，也至少必须保证：

```text
push main -> 不允许直接更新
PR -> required checks success -> 才能 merge
```

## 7.3 验收

通过 GitHub API/gh CLI 复核：

```text
main protected = true
ruleset 非空
required status check 包含 release gate
force push disabled
```

然后故意创建一个会失败的 PR，确认 GitHub UI/API 返回不可 merge。

---

# 8. P1-SUP1：统一 CI 与生产 Python 依赖闭包

## 8.1 当前问题

当前存在明显环境漂移：

```text
CI: Python 3.14
Production Dockerfile: python:3.12-slim
```

并且：

```text
CI -> requirements-lock.txt
Production -> bin/sp.tar.gz + bin/pybin.tar.gz + hf.tar.gz + chroma.tar.gz
```

`requirements-lock.txt` 还明确说明 `crewai/sentence-transformers/torch` 等 runtime lazy dependency 不在 CI lock 内。

因此“CI 可重现”不等于“production runtime 可重现”。

## 8.2 最终目标

生产镜像依赖必须从**版本化、带 hash 的 lock**确定性构建。

推荐固定 production Python 版本为 Dockerfile 当前使用的 **Python 3.12.x**，CI 同步使用相同 minor 版本。

不要为了迁就 CI 把 production 直接升级到 3.14，除非先完成全部依赖兼容验证。

## 8.3 推荐目录结构

```text
ai-orchestrator/
  requirements.in
  requirements-dev.in
  requirements-lock.txt
  requirements-dev-lock.txt
  wheelhouse/
  wheelhouse.sha256
  models/
  models.sha256
  tools/
  tools.sha256
```

wheelhouse/models/tools 是否入 Git 由仓库大小策略决定；可存制品仓库，但 hash manifest 必须入 Git。

## 8.4 锁定要求

必须覆盖真实 production runtime：

- crewai
- sentence-transformers
- torch/onnxruntime 等实际模型运行依赖
- langgraph
- chromadb
- fastapi/uvicorn
- pymysql
- kubernetes client
- 所有传递依赖

要求：

```text
package==version
+ hash
```

使用 `pip-compile --generate-hashes`、uv 等均可，但产物必须能离线校验。

## 8.5 Dockerfile 改造

删除“从已运行成功容器导出 site-packages”的权威构建方式。

生产 Docker build 推荐：

```text
固定 python:3.12.x 基础镜像 digest
    -> COPY lock
    -> COPY wheelhouse
    -> sha256sum -c wheelhouse.sha256
    -> pip install --no-index --find-links=wheelhouse --require-hashes -r requirements-lock.txt
    -> COPY models/tools
    -> sha256sum -c models.sha256/tools.sha256
```

如完全离线，wheelhouse 在可信联网构建阶段提前生成，不得在生产集群现场解析开放版本依赖。

## 8.6 kubectl/k8sgpt/model 校验

必须记录：

```text
name
version
architecture
source
sha256
license
```

至少包括：

- kubectl
- k8sgpt
- bge-small-zh 模型
- Chroma ONNX 模型
- Python wheelhouse

## 8.7 CI 统一

CI 使用与 production 相同：

- Python 3.12
- requirements-lock
- hash 校验

再额外建立一个可选 Python 3.14 compatibility job，但不得让 3.14 job 替代 production 3.12 gate。

## 8.8 关闭条件

必须证明：

```text
fresh clone
+ 指定离线制品
+ 无历史本地 venv
+ 无历史运行容器
=> 可以得到相同 dependency manifest 的 production image
```

镜像内执行：

```bash
python --version
pip freeze --all
```

与 release manifest 中记录完全一致。

---

# 9. P1-SUP2：Production 镜像必须 digest pin

## 9.1 当前问题

Helm helper `aiops.imageWithGlobalTag` 当前逻辑明确：

- self-owned image 必须使用 `global.imageTag`；
- 如果 image 中包含 `@`，直接 fail；
- 因而 production 不能直接渲染 `repository@sha256:...`。

这与生产发布所需不可变镜像身份不一致。

## 9.2 改造方案

新增 production digest 配置，例如：

```yaml
global:
  environment: production
  imageTag: ""
  imageDigests:
    queryApi: "sha256:..."
    orchestrator: "sha256:..."
    executor: "sha256:..."
    ingest: "sha256:..."
    eventCollector: "sha256:..."
    credentialBroker: "sha256:..."
    llmEgressProxy: "sha256:..."
```

helper 规则：

```text
production:
  digest 必填
  tag 不得作为最终 identity
non-production:
  允许 tag
```

渲染结果必须为：

```text
registry.example/ai-orchestrator@sha256:<64hex>
```

不能只记录 Kubernetes `imageID` 事后证明；deployment manifest 本身就必须锁 digest。

## 9.3 测试

新增 Helm contract：

```text
1. production 缺 digest -> helm template fail
2. production 使用 tag-only -> fail
3. digest 格式错误 -> fail
4. 所有 self-owned workload 最终 image 均包含 @sha256:
5. local/dev 仍允许 global.imageTag
```

## 9.4 关闭条件

`helm template -f values-prod.yaml ...` 最终 manifest 中所有自研镜像都以 digest 引用。

---

# 10. P1-SCA1：建立真实供应链漏洞门禁

## 10.1 当前事实

最新 CI 在 `npm ci` 后报告：

```text
9 vulnerabilities
6 moderate
2 high
1 critical
```

当前不能仅凭这段输出断言生产一定可利用，因为尚未确认：

- 是 devDependency 还是 production dependency；
- 是否可达；
- 是否存在已修复版本；
- 是否属于前端最终 bundle 路径。

但在 triage 完成前不能发布。

## 10.2 前端整改流程

执行：

```bash
cd observability-frontend
npm audit --json > npm-audit.json
npm audit --omit=dev --json > npm-audit-prod.json
```

对每个 critical/high 记录：

```text
advisory/CVE
package
installed version
fixed version
direct/transitive
production/dev
dependency path
runtime reachable?
fix action
exception reason（若有）
exception expiry
```

### 门禁标准

默认：

```text
production critical = 0
production high = 0
```

若必须例外：

- 必须证明不可达；
- 必须有书面 risk acceptance；
- 必须设置失效日期；
- CI 读取 allowlist 文件，不能用 `|| true`。

## 10.3 Python/Go/Image 也必须扫描

建议增加：

- Python：`pip-audit` 或等价 SCA；
- Go：`govulncheck ./...`；
- image：Trivy/Grype 等；
- SBOM：Syft/CycloneDX/SPDX 任一稳定格式。

门禁输出必须绑定最终 image digest。

---

# 11. P1-REL1：旧 release evidence 全部失效，必须重建

## 11.1 当前事实

旧的 release evidence 绑定的是 `b387869` 等更早运行候选，并已经写明：

```text
registry_bound=false
publishable=false
RCA evidence 未 confirmed
```

之后发生多次安全与可靠性代码提交，直到当前：

```text
bd93acd
```

因此旧证据只能作为历史记录，不能证明当前 HEAD。

## 11.2 最终 release identity

必须建立如下不可变关联：

```text
git_commit
image_digest[]
SBOM_digest[]
rendered_manifest_digest
migration_checksum[]
policy_digest[]
dataset_digest[]
model_digest[]
frontend_lock_digest
python_lock_digest
```

## 11.3 发布证据链

最终候选提交后，按顺序重新执行：

### A. 代码与静态门禁

```text
Go test/vet/vuln
Python pytest/lint/audit
Frontend test/build/audit
Helm lint/render/contracts
```

### B. 镜像

```text
clean build
SBOM
CVE scan
registry push
immutable digest
signature
signature verify
```

### C. 候选环境

- 使用 production values；
- 使用 production Secret/证书；
- 所有自研镜像使用 digest；
- migrations 完成；
- readiness/liveness 全绿；
- 无 CrashLoop；
- 无 fixture/mock。

### D. 真实功能证据

至少验证：

- Metrics；
- Logs；
- Trace；
- Kubernetes events；
- DeepFlow 数据；
- 多租户/多集群 scope；
- AI Chat；
- RCA；
- Approval；
- Action dispatch；
- execution_unknown -> reconcile；
- Credential Broker TokenRequest；
- mTLS/签名/nonce；
- Graph 投影与查询。

### E. RCA

不得把“数据可查询”写成“RCA 已确认”。

最终必须有真实故障样本达到项目定义的 `confirmed` 证据闭环；如果仍为 `FAIL/BLOCKED_BY_ENV`，发布结论继续 NO-GO。

### F. 恢复与回滚

必须至少证明：

- MySQL 恢复；
- ClickHouse 恢复；
- WAL replay；
- outbox/reconcile；
- image rollback；
- migration rollback/forward-fix 策略；
- Secret/证书轮换不导致不可恢复停机。

## 11.4 关闭条件

只允许最终 release evidence 中明确：

```text
publishable=true
```

且所有引用的 SHA/digest 与当前部署一致时关闭。

---

# 12. P1-A1：物理删除 legacy 运行时

## 12.1 当前问题

Orchestrator 中仍存在多套历史/兼容实现：

```text
rca.py
rca_engine/
rca_engine.py
rca_engine_legacy.py
rca_production.py
rca_snapshot.py

investigator.py
investigation_app.py
investigation_runtime.py
investigation_dispatcher.py
investigation_state.py

LEGACY_FLOW_RUNTIME_ENABLED
INVESTIGATOR_ENABLED
LEGACY_DIRECT_MUTATIONS_ENABLED
INVESTIGATION_RUNTIME_ENABLED
```

生产安全目前很大程度依赖 values 和 CI 断言把 legacy 开关维持为 0。

长期风险是：历史代码仍可以被误接线、误开启或被后续维护者错误复活。

## 12.2 整改原则

**删除优于关闭。**

先做真实调用图，再删除。

### 12.2.1 建立使用矩阵

每个 legacy 文件必须标记：

```text
生产 import?
测试 import?
脚本 import?
Helm/entrypoint 引用?
公开 API 路由引用?
数据兼容用途?
```

没有生产用途的文件：物理删除。

仍承担数据迁移/离线读取用途的文件：移动到：

```text
tools/migration/
tools/offline/
```

不得继续位于 runtime import path。

### 12.2.2 目录重构

推荐：

```text
ai-orchestrator/
  investigation/
  rca/
  approval/
  execution/
  evidence/
  llm/
  security/
  adapters/
```

逐步降低 `main.py` 和 `orchestrator.py` 体积。

### 12.2.3 关闭 legacy 开关

当代码物理删除后，再删除对应 Helm values/env：

```text
LEGACY_FLOW_RUNTIME_ENABLED
LEGACY_DIRECT_MUTATIONS_ENABLED
```

如果 `INVESTIGATOR_ENABLED` 对 legacy investigator 已无用途，也删除。

## 12.3 验收

- `git grep` 不再存在 production legacy import；
- Helm 不再渲染无效 legacy 开关；
- 所有生产流程仍通过；
- 删除 legacy 后测试不能依赖兼容 shim 才通过。

---

# 13. P1-R1：ApprovalStore 必须从生产授权链彻底退出或 fail-closed

## 13.1 当前状态

`db_approval.py` 已从“静默吞错”整改为：

- 写失败记录日志；
- `degraded()` 可见；
- UPDATE 列名白名单。

但是数据库写失败后仍允许内存 fallback。

这对于普通缓存可接受，但对于“审批结果作为执行授权依据”不能作为生产 SoT。

当前架构文档已经声明：

```text
ai_actions/attempts/approvals owner = query-api + MySQL
```

因此需要做二选一，不允许保持模糊状态。

## 13.2 推荐方案：从生产路径删除 ApprovalStore

建立 production call-chain 测试证明：

```text
Browser approval
 -> Query API Action/Approval API
 -> MySQL transaction
 -> Action outbox
 -> dispatcher
 -> executor
```

生产不得经过 `db_approval.ApprovalStore`。

`ApprovalStore` 如果仅供 legacy/local demo：

- 移入 legacy/offline；
- production composition 不 import；
- production Helm 不配置；
- 增加 import guard 测试。

如果仍必须保留在 production：

- MySQL write 失败必须返回失败；
- 不得把内存结果用于授权执行；
- degraded 模式必须 disable approve/execute。

## 13.3 关闭条件

必须有真实生产模块测试证明授权决策来自 Query API/MySQL，不依赖内存 fallback。

---

# 14. P2-S6B：LLM egress proxy 使用常量时间令牌比较

当前 proxy fail-closed 已修复，但 token 校验仍类似：

```go
if proxyTokenFromRequest(r) != c.proxyToken
```

统一改为：

```go
subtle.ConstantTimeCompare([]byte(got), []byte(expected)) == 1
```

要求：

- 空 token 仍先 fail-closed；
- 长度不一致直接 false；
- 新增正确/错误/空 token 测试。

---

# 15. P2-R5：LLM_MOCK 默认值改为 false

## 15.1 当前问题

`main.py` 默认：

```python
LLM_MOCK=true
```

虽然 production 明确模式会 fail-fast，但任何忘记设置 `AIOPS_ENV` 的准生产环境可能得到模拟 AI 结果。

## 15.2 修复

代码默认：

```text
LLM_MOCK=false
```

本地开发由：

- `.env.local`
- compose
- local-validation script

显式开启 mock。

原则：**假能力必须 opt-in，真实能力不能依赖“记得关 mock”。**

增加测试：

```text
未设置任何 env -> mock=false
AIOPS_ENV=production + mock=true -> startup fail
local + 显式 mock=true -> 允许
```

---

# 16. P2-R6：investigator 文档/行为收敛

当前 docstring 写“默认 true”，实现默认 `"0"`。

如果 `investigator.py` 已属于 legacy：

**不要把默认值改成 true。**

正确做法：

1. 标记 deprecated/legacy；
2. 文档改成默认关闭；
3. 生产 composition 不加载；
4. 在 P1-A1 中最终物理删除。

---

# 17. P2-A2：消除前端 GLOBAL_PATHS 漂移

## 17.1 当前问题

`observability-frontend/src/api/client.ts`：

- 每个请求读取 `localStorage`；
- 每个请求 `JSON.parse(aiops-ui-v3)`；
- `GLOBAL_PATHS.some(startsWith)` 决定是否追加 `cluster_id`。

该逻辑不应该成为安全边界。

## 17.2 修复方案

### 服务端

所有需要 scope 的 API 由 Query API 根据 HttpOnly session + active scope 强制注入，不依赖前端提供 tenant/cluster 作为授权依据。

### 前端

建立内存态 `scopeStore`，请求拦截器直接读取当前内存状态，不解析 localStorage。

更推荐：

- API 方法显式声明 `scope: global | cluster`；
- 由 typed client 生成请求；
- 删除 URL prefix 黑名单。

例如：

```ts
getServices({ scope: 'cluster' })
getUsers({ scope: 'global' })
```

## 17.3 验收

- client interceptor 中无 `JSON.parse(localStorage...)`；
- 新增路由不会因忘记更新 `GLOBAL_PATHS` 导致静默行为变化；
- 服务端 scope test 覆盖跨 tenant/cluster 越权。

---

# 18. P2-F1：重写 workflow E2E，必须调用生产模块

## 18.1 当前问题

`tests/workflow-e2e/test_action_workflow.py` 自己定义 `Harness`，然后测试 Harness。

该测试不能发现真实：

- store 事务错误；
- dispatcher 回归；
- idempotency key 失效；
- reconcile 接线断裂；
- executor response mapping 错误。

## 18.2 重写要求

workflow contract test 必须使用真实生产代码。

允许使用：

- 临时 MySQL/Testcontainers；
- fake HTTP executor；
- fake K8s server；

但不允许重写生产状态机。

最低覆盖场景：

```text
1. Create Action
2. Approve
3. Outbox claim
4. Dispatch
5. Executor success
6. Lost response -> execution_unknown
7. Reconcile success
8. duplicate approval idempotent
9. duplicate dispatch no second mutation
10. stale version reject
11. idempotency key reused with changed decision reject
12. rejected action never dispatch
13. tenant/cluster scope mismatch reject
14. approval signature tamper reject
```

## 18.3 关闭条件

测试文件必须 import 生产 package/模块；删除自建 Harness 状态机。

---

# 19. P2-F2：删除形式化“伪门禁”测试

## 19.1 gateway tool registry

当前测试只是检查源码文本含：

```text
init_default_tool_registry()
```

改为运行时测试：

- 构造 gateway composition；
- 查询 registry；
- 断言 required tools 真正注册；
- 断言调用时走 canonical internal query adapter。

## 19.2 RBAC toggle

当前只检查 grant/revoke 脚本文件存在。

改为：

- Helm render；
- 解析 ClusterRole；
- 验证 read-only verbs；
- grant 后预期权限存在；
- revoke 后绑定消失；
- production manifest 永远无 mutation verbs。

---

# 20. P2-SEC1：自研容器最小权限加固

## 20.1 当前问题

`ai-orchestrator/Dockerfile` 未设置 `USER`，默认 root。

Helm helper 的通用 Pod securityContext 当前主要为：

```yaml
fsGroup: 65532
fsGroupChangePolicy: OnRootMismatch
```

缺少常见容器最小权限项。

## 20.2 整改目标

对所有自研服务评估并默认启用：

```yaml
securityContext:
  runAsNonRoot: true
  runAsUser: 65532
  runAsGroup: 65532
  seccompProfile:
    type: RuntimeDefault

containers:
  securityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop: ["ALL"]
```

需要写目录的组件通过明确 PVC/emptyDir 挂载提供：

- `/tmp`
- `/var/lib/aiops`
- WAL directory

不得因为应用写 rootfs 失败而把 `readOnlyRootFilesystem` 整体关闭，应修正写路径。

## 20.3 特别要求

Orchestrator 仍需执行 kubectl 时：

- kubectl 本身不需要 root；
- K8s RBAC 权限与 Linux UID 无关；
- 不得以“kubectl 需要 root”为理由保留 root。

---

# 21. P2-HA1：Orchestrator 状态外部化与多副本

当前 production Helm 明确：

```text
orchestratorReplicas = 1
PVC = ReadWriteOnce
ChromaDB/sqlite/checkpoint 与进程耦合
```

这意味着 Orchestrator 仍有单实例可用性风险。

如果正式 SLO 要求平台控制面无单点，需要执行：

1. 把 canonical Run/Action 状态继续全部留在 MySQL；
2. LangGraph checkpoint 改为可多实例共享的后端；
3. Chroma/RAG 如不是 SoT，可拆成独立服务或使用可多副本存储；
4. gateway/API 进程无状态化；
5. worker 使用 lease/fencing 抢占；
6. Deployment 至少 2 replicas；
7. 做 kill -9/Pod delete/节点驱逐恢复测试。

如果业务正式确认单副本可接受，必须把可用性目标、RTO 及失效行为写入 `runtime-slo.md`，不能保留“生产高可用”模糊表述。

> **2026-09-03 执行状态：保持 OPEN（用户裁决）。** 已按裁决补 `docs/runtime-slo.md`「可用性与失效行为」一节：只记录客观部署事实（orchestrator 1 副本、PVC RWO、checkpoint/session sqlite/Chroma 与进程本地耦合、canonical Run/Action 状态在 MySQL），明确**业务 SLO 未确认**；未填写 RTO/RPO，未以单副本现状声称"满足生产高可用"。关闭本项需业务 SLO 输入后二选一：(a) checkpoint 外部化 + worker lease/fencing + ≥2 副本及故障恢复验证；(b) 业务明确可用性目标与失效行为后收口。

---

# 22. 已修复问题的复核结论

以下原问题在当前基线上可以保留“已整改”或“主要部分已整改”判断，但仍需最终全量回归：

| 原编号 | 复核结论 |
|---|---|
| P1-S2 egress proxy 未配置 token fail-open | **主要修复成立**：空 token 时 503 fail-closed |
| P1-S3 apply.sh 弱 admin 默认值 | **弱密码生产守卫已修复**；生产 Secret argv/Helm value 路径仍应继续收敛到 existingSecret |
| P2-S6 Go 多处普通 token 比较 | **大部分已修**；egress proxy 遗漏单独列 P2-S6B |
| P2-S7 Approval signature 字段覆盖 | **已修复**：改为完整 contract 字段规范化签名，并扩充 hash |
| P2-S8 动态 SQL 列名 | **已修复**：update 字段白名单 |
| P2-S10 WAL 权限 | **已修复**：目录/文件权限收紧 |
| P2-R2 shell 策略误导注释和死循环 | **已修复** |
| P2-R3 固定窗口限流 | **已修复**：token bucket |
| P2-R4 WriteTimeout=0 | **已修复**：全局 deadline + SSE 单独续期 |
| P2-A3 phase4 gate 伪检查 | **主要修复成立** |
| P2-F3 历史报告根目录污染 | **已整改**：移入 archive |
| P2-F4 Makefile 硬编码 `.venv314` | **已整改** |

注意：任何“已修复”都不代表当前 release 已通过，因为 `bd93acd` 全量 CI 当前仍为失败状态，最终版本还需要重新验证。

---

# 23. CI/CD 最终推荐结构

最终不建议继续把整个 release gate 只包装成一个长 shell job。

推荐：

```text
PR / push
│
├─ go-query
├─ go-ingest
├─ go-collector
├─ go-executor
├─ go-broker
├─ go-egress-proxy
├─ python-orchestrator
├─ workflow-e2e-real
├─ frontend
├─ helm-contract
├─ security-static
├─ dependency-sca
└─ production-image
        │
        └─ release-gate (needs all)
```

### release-gate 必须检查

```text
所有 job success
production image digest 已生成
SBOM 已生成
critical/high policy 通过
Helm prod render 使用 digest
legacy mutation off
mTLS required
NetworkPolicy default deny
Secret 未使用 CHANGE_ME
branch required check 已启用
```

### 发布阶段再增加

```text
registry signature verify
candidate deploy
migration
smoke
real evidence
failure injection
backup/restore
rollback
release manifest
```

---

# 24. 最终整改执行顺序

后续智能体必须严格按以下顺序，不得先做大规模重构再回头修 P1。

## Phase 0：冻结基线

1. 记录开始 commit；
2. 确认工作区；
3. 建整改分支；
4. 保存当前失败 CI evidence；
5. 不修改历史报告作为“修复证据”。

## Phase 1：关闭直接发布阻断

按顺序：

1. **P1-S1**：去掉 RCA 通用 shell；
2. **P1-CI1**：修复 EWMA 浮点测试；
3. **P1-SCA1**：完成 critical/high triage；
4. **P1-SUP1**：统一 Python 3.12 + production lock；
5. **P1-SUP2**：production digest pin；
6. **P1-GOV1**：开启 branch/ruleset required check；
7. 全量 CI 必须 success。

## Phase 2：提高门禁真实性

1. 重写 workflow E2E；
2. 删除源码字符串/文件存在性伪测试；
3. constant-time proxy token；
4. LLM_MOCK 默认 false；
5. 前端 scope client 重构；
6. 容器最小权限。

## Phase 3：架构收敛

> **2026-09-03 执行状态：全部完成**（分支 `phase3/architecture-convergence` 已并入 main；详见 `docs/remediation/2026-09-03/phase3/PHASE3_DELIVERY.md`）。五项：P1-R1 ✅（ApprovalStore fail-closed 退出）；legacy 调用矩阵 ✅（`LEGACY_USAGE_MATRIX`）；物理删除 legacy ✅（含 RCA 引擎收敛为 canonical，关闭条件 grep 0 命中）；域拆包 ✅ 按用户裁决以收敛形态收口（production_surface 路由 allowlist + 域 router 装配 + ARCH-316/317/601-607 契约，禁止大规模无行为变化搬移）；HA/状态外部化 = P2-HA1 保持 OPEN（见 §21）。

1. ApprovalStore 从 production path 退出；✅（P1-R1）
2. legacy runtime 调用矩阵；✅（P1-A1）
3. 物理删除 legacy；✅（P1-A1 + P1-A1b）
4. Orchestrator 按域拆包；✅（收敛形态收口）
5. 需要时完成状态外部化和 HA。🔴 保持 OPEN（见 §21）

## Phase 4：最终生产验证

只针对最终 commit（执行状态 = 2026-09-04，main `d883759`；详见 `docs/remediation/2026-09-03/phase4/PHASE4_DELIVERY.md`）：

1. clean build；✅ `IMAGE_TAG=git-da9e85b` 12 组件镜像构建成功
2. image digest；✅ 本地 config digest 已收集；registry digest 待 release system 解析（候选未推送 registry）
3. SBOM/SCA；✅ CI `supply-chain-tests` 门禁持续通过（PR #3/#4/#5）
4. 签名；🔄 release 流程项（本候选未推送 registry）
5. Helm prod digest render；✅ 真实 digest 渲染：14 处 `@sha256`、0 mutable tag（10 Deployment + 3 StatefulSet）
6. candidate deploy；✅ 候选集群 rev 63 全 11 Deployments Ready（过程中修复 frontend nginx caps → PR #3）
7. migration；✅ Gate 4 PASS（真实 MySQL 8 A/B/C + MinIO S3 Object Store；修复 baseline 缺 `auth_sessions.updated_at` → 版本 0018 / PR #4）
8. real observability evidence；⬜ 待正式窗口
9. RCA confirmed；⬜ 待正式窗口
10. Action/Approval/Reconcile；⬜ 待正式窗口
11. failure injection；🔄 pod delete 驱逐自愈已验证（25s 重建 2/2 Ready）；kill -9/节点驱逐待正式窗口
12. backup/restore；⬜ 待正式窗口（建议迁移前后备份对比）
13. rollback；⬜ 待正式窗口
14. 生成 release manifest；🔄 `deploy/evidence/release-evidence.json`：commit `d883759`、dirty=false、commands 全 0（含 unit_tests=0 已在上轮记录）
15. `publishable=true` 后才允许 GO。⬜ 当前 `publishable=false`（正确初态），需破坏性深测 + real evidence 齐备后判定

---

# 25. 智能体每轮修复后的强制验证命令

具体命令可按模块调整，但下列门禁不得被跳过。

## Go

```bash
cd ai-apm-query-go && go test ./... -count=1
cd ai-apm-query-go && go vet ./...

cd ../ai-action-executor && go test ./... -count=1
cd ../ai-credential-broker && go test ./... -count=1
cd ../ai-llm-egress-proxy && go test ./... -count=1
cd ../ai-event-collector && go test ./... -count=1
cd ../ai-apm-ingest-go && go test ./... -count=1
```

并执行 `govulncheck`（接入后）。

## Python

```bash
cd ai-orchestrator
python -m pytest -q
ruff check .
```

接入 SCA 后：

```bash
pip-audit
```

## Workflow E2E

```bash
python -m pytest tests/workflow-e2e -q
```

必须确认测试已改为生产模块，不得仍是自建 Harness。

## Frontend

```bash
cd observability-frontend
npm ci
npm run test:run
npm run build
npm audit --omit=dev
```

## Helm

```bash
helm lint deploy/helm/aiops
```

使用完整 production 注入参数执行 `helm template`，并验证：

```text
所有 self-owned image = @sha256
legacy mutation = off
mTLS required
NetworkPolicy default deny
Secret 不含 CHANGE_ME
orchestrator ClusterRole 无 mutation verbs
```

## 项目既有 gate

```bash
deploy/scripts/verify-aiops-workflow-gates.sh
deploy/scripts/test-deployment-contracts.sh
```

存在其他 production architecture/secret/graph gate 时一并执行，不能因为本文件未列出就跳过。

---

# 26. 任何智能体禁止的“假完成”行为

发现以下任一情况，审核结论直接退回 `NOT FIXED`：

- 把测试删掉后 CI 变绿；
- 把安全测试标记 skip；
- 使用 `|| true` 绕过失败；
- 仅修复 `<`、`;` 等字符而保留任意 shell；
- 仅提交 requirements lock，但 production Dockerfile 不使用；
- 只记录 image tag，不生成 digest；
- npm critical/high 只写“当前不影响”但无依赖路径证据；
- 只做本机镜像，不验证 registry digest；
- 用 fixture/mock 作为生产 RCA evidence；
- 用旧 commit 的 Pod/Helm/测试证明新 commit；
- 开启 legacy runtime 解决功能回归；
- 给 orchestrator 新增写 RBAC 解决调查能力；
- 把审批失败降级到内存后仍允许动作执行；
- 用文档声明代替调用链测试；
- 只检查某函数名字是否出现在源码中作为 contract test；
- 只检查脚本文件存在作为权限测试。

---

# 27. 最终生产 GO 判定标准

只有同时满足以下全部条件，才允许将结论由 `NO-GO` 修改为 `GO`：

### 代码与安全

- P0 = 0；
- P1 = 0；
- P1-S1 结构化 kubectl executor 已落地；
- 无 LLM 可影响的 `shell=True` 调查路径；
- production Approval 权威状态仅来自 Query API/MySQL；
- legacy direct mutation 不存在或已物理删除。

### CI/GitHub

- 最终 commit 全部 required checks success；
- main protected；
- ruleset/required checks 生效；
- 失败 PR 无法 merge。

### 供应链

- production Python runtime 与 CI production job 同版本；
- runtime dependency 全量锁定并有 hash；
- kubectl/k8sgpt/model/offline artifact 有 checksum；
- production self-owned images 使用 digest；
- SBOM 完整；
- production critical/high 漏洞为 0，或存在正式、可审计、未过期例外。

### 部署与运行

- production Helm render 通过；
- 所有 Secret 由安全渠道注入；
- mTLS/签名/nonce/NetworkPolicy 正常；
- migrations 成功；
- 所有核心 Pod Ready；
- 没有依赖 LLM mock；
- 没有 fixture 作为生产证据。

### 功能与可靠性

- Run/Action/Approval/Reconcile E2E 使用真实生产代码通过；
- duplicate/lost-response/stale-version 测试通过；
- 真实 observability 数据链通过；
- RCA 达到项目定义的 confirmed；
- backup/restore/rollback/failure injection 有证据。

### Release Evidence

最终 manifest 至少包含：

```text
git SHA
image digest
SBOM digest
manifest digest
migration checksum
policy/dataset/model digest
CI run IDs
security scan result
candidate deployment identity
validation evidence digest
rollback evidence
```

并最终明确：

```text
publishable=true
```

在此之前，无论局部测试通过多少，**统一保持 NO-GO**。

---

# 28. 最终审核意见

当前项目的主要问题已经从“缺少 AIOps 功能”转向“生产化证据与安全边界最后收口”。V2 架构本身有较清晰的 SoT、权限边界、可靠投递和执行隔离设计，值得保留；当前最危险的做法是为了尽快变绿而继续在旧 shell、legacy runtime、双依赖体系和 tag 发布模式上打补丁。

整改应遵循以下唯一主线：

```text
先关闭真实安全边界
    -> 再让 CI 可信
    -> 再让 production build 可重复
    -> 再让镜像和部署不可变
    -> 再清理 legacy
    -> 最后重新生成当前 commit 的真实发布证据
```

**最终状态：`main@bd93acd` = NO-GO。**

本报告中的 P1 项全部关闭并针对新的最终 commit 重新完成发布证据前，不得发布生产版本。
