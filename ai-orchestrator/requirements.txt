crewai>=1.0
langgraph>=1.0,<2.0
# langgraph-checkpoint-sqlite 3.x 的 SqliteSaver.put 才与 checkpoint 4.x(JsonPlusSerializer dumps_typed) 兼容；
# 2.x 的 put 用旧 API self.jsonplus_serde.dumps() 会报 "'JsonPlusSerializer' object has no attribute 'dumps'"
langgraph-checkpoint-sqlite>=3.0,<4.0
fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.0
chromadb>=0.4.24
sentence-transformers>=3.0
# P1-SUP1: torch 必须用 CPU 构建（PyPI linux 默认 wheel 携带 CUDA 运行时，
# 体积数百 MB 且生产不需要）。+cpu 本地版本仅存在于 pytorch cpu index。
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0+cpu
httpx>=0.28
pydantic>=2.0
prometheus-client>=0.20
pymysql>=1.1
DBUtils>=3.0
apscheduler>=3.10,<4.0
