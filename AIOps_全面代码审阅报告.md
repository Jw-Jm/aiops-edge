# AIOps 平台全面代码审阅报告（当前代码复核版）

审阅日期：2026-08-24  
复审修订日期：2026-08-24  
审阅方式：仓库静态审阅、关键测试代码检查、针对性测试尝试与部署清单检查；未在真实 Kubernetes、真实身份源、真实模型和真实观测数据环境执行端到端压测或故障演练。

## 1. 结论摘要

从源代码看，平台已实现可观测性查询、跨集群基础管理、受控 AI 对话诊断、Run 控制面、审批与执行适配器等较完整的能力框架。其多租户与可信调用上下文设计方向正确，采集链、数据职责划分和诊断证据模型也有明显投入；这些属于代码实现结论，不代表相关能力已经通过生产环境可用性验证。

但它尚不宜作为“可对生产集群自主执行运维动作”的平台直接开放。当前最重要的风险不是单个算法，而是产品入口、主工作流、部署开关和真实运行验证之间仍有缝隙：

1. AI 对话前端在未选集群时发送 cluster_id=all，而服务端对对话强制要求单一、可授权的规范集群 ID，默认路径会被拒绝。
2. 对话入口走精简 chat 图；规划、风险、审批、执行、校验等完整运维图虽已实现，但不是普通自然语言对话的默认主链。
3. 页面仍会展示由图节点推断出的“工具活动”；这不等价于模型实际逐次调用了工具，容易让使用者高估 Agent 的自治能力。
4. 自动执行虽已默认关闭，并且 Kubernetes 写权限也有显式开关，但一旦启用，编排器仍使用集群级写授权；审批、目标快照、动作哈希与线上执行适配器必须做完整联调和演练。
5. Helm 默认没有启用全局出站默认拒绝；因此生产部署必须把网络出站白名单、密钥注入与写权限开关一并作为上线门槛。

总体判断：**适合继续作为“以人工决策为主、AI 辅助诊断和生成建议”的内测/受控生产能力；不建议在当前证据下开放无人值守的真实处置执行。**

## 2. 审阅范围与原则

本报告以当前工作区代码为准，不把先前架构合同、Gate 文档或历史整改结论视作事实来源。审阅覆盖：

- 前端、query-api、ai-orchestrator、采集和事件链；
- MySQL、ClickHouse、VictoriaMetrics、VictoriaLogs 相关的数据职责；
- AI 对话、调查 Run、RCA、工具注册、审批、执行与 SSE；
- Helm、RBAC、NetworkPolicy、凭据注入；
- 关键单元测试及其对真实生产闭环的覆盖边界。

按本次复审统一口径，代码扫描基线约为 695 个自有源码/配置文件、116,252 行代码和 255 个测试文件。统计包含 Go、Python、TypeScript、JavaScript、YAML、JSON、TOML、SQL 和 Shell 等源码/配置扩展名，排除 vendor、node_modules、dist、build 和 .git；测试文件按 tests/test 目录、Go 的 _test.go、Python 的 test_*.py，以及前端 test/spec 文件识别。该数字仅用于说明审阅范围，不等同于测试覆盖率或生产验证结果。

## 3. 当前实际架构

### 3.1 浏览器和控制面

浏览器不应直接调用编排器。当前设计由 query-api 负责 JWT、租户、集群归属和能力校验，并为内部调用签发可信上下文：

    前端
      → query-api（身份、租户、集群解析、能力授权）
      → ai-orchestrator（验签后的内部入口）
      → 查询、诊断、RCA、建议或受控 Run 工作流

这条边界是系统最有价值的设计之一。query-api 会重新解析集群引用并校验归属，而不是直接信任浏览器提交的 tenant、cluster 或角色字段。对话能力使用 ai.chat；调查 Run 使用 ai.investigate，二者在语义上被刻意区分。

### 3.2 两条并存的 AI 路径

当前并非只有一条自然语言运维主线：

- 对话路径：AiChat → query-api 的 /api/v1/ai/chat → 编排器内部 chat 入口 → chat_graph。它面向快速、只读的诊断和问答。
- 调查路径：浏览器创建 Run → query-api 持久化 Run 与 outbox → 可信 RunInvocation → 编排器。其代码目标是支持计划、风险、审批、执行、验证与事件回放。

第二条路径拥有更严格的控制模型，但普通聊天没有自然地进入它。因此产品上不能把“聊天诊断”描述为已经完成的端到端自治运维。

### 3.3 数据与事实来源

当前代码和部署结构体现的职责划分大体合理：

| 数据类别 | 主要职责 |
|---|---|
| 指标 | VictoriaMetrics |
| 日志 | VictoriaLogs |
| Trace/Span、拓扑、变更与部分观测关联数据 | ClickHouse |
| 用户、租户、集群、Run、事件、Outbox 和控制面状态 | MySQL |
| 知识检索/向量能力 | Chroma 与对象存储的可选集成 |

应继续坚持“原始观测事实、证据、模型推论、处置动作、审批记录”分层保存。LLM 输出不应成为事实来源，也不应覆盖原始证据。

## 4. 自然语言智能运维工作流评估

### 4.1 当前对话工作流

普通 AI Chat 的实际主流程可概括为：

    用户问题
      → query-api 鉴权与单集群规范化
      → 编排器 chat_graph
      → 固定采集服务/基础设施/告警/RED/Trace/日志等上下文
      → 清洗与轻量判断
      → RCA、RAG、CrewAI 汇总（按需）
      → 流式返回诊断结论和建议

它不是“模型先决定调用哪个工具、观察结果、再循环决策”的通用 Agent loop。仓库中确有 function_calling、dual_agent、Tool Registry、RealToolExecutor 和 Agent Runtime Integration 等模块，也有相应测试；但对话主图没有默认以这些模块做逐工具闭环。

因此，当前方案对“问当前异常、给排查方向、生成安全建议”是合理的；对“模型自主调查、评估风险、调用工具并执行修复”的表述则不够准确。

### 4.2 合理之处

- 对话和调查 Run 分开，避免聊天自动创建高权限任务。
- query-api 进行租户与集群再解析，再签发内部可信上下文，降低前端伪造范围的风险。
- 有结构化 RCA、证据缺口、反证检查、计划、审批、执行适配器与校验等模块，技术路线完整。
- 运行记录与事件流已不再只是内存模型：公共 Run 创建会写入持久化存储和 outbox；SSE 从持久化事件回放并按租户校验。
- 默认关闭审批后的真实执行，默认不授予编排器 Kubernetes 写权限。

### 4.3 主要不合理或不完整之处

1. **默认对话范围自相矛盾。** 前端默认使用 all，服务端明确拒绝空值和 all。用户没有显式选择集群时，聊天会以权限拒绝结束，而不是引导其选择有权访问的单一集群。
2. **“工具调用”可观测性不精确。** 流式代码会根据图节点和关键词生成工具开始/完成事件，并注明为节点级推断。界面若把它展示为实际工具调用，会损害审计可信度。
3. **主流程不统一。** 完整图、双 Agent 图和 function calling 能力仍与默认 chat 图并行。多套路径的权限、证据、状态和 UI 语义难以持续保持一致。
4. **Trace 写入闭环未完成。** 查询层和 RCA 会读取 ClickHouse 的 trace_spans，ingest 也保留 OTLP/DeepFlow Span 解析逻辑；但当前生产入口已删除旧 ClickHouse writer，并将 Pipeline 和 DeepFlowSyncer 的 SpanSink 设为 nil，因此解析后的 Span 不会由现行 ingest 主链持久化到 trace_spans。需要补齐新的 Trace SoT 写入链，再用真实数据验证时间、租户与集群隔离。
5. **源码直启与 Helm 的模型模式默认值不同。** 编排器 Python 进程在未传环境变量时会把 LLM_MOCK 默认设为 true；当前 Helm values 已将 aiOrchestrator.llmMock 设为 false，并由 Deployment 显式注入。生产检查重点应是核验实际渲染值、运行时环境变量、模型密钥、失败降级与响应标记，而不是把 Helm 默认配置判定为 Mock。

## 5. 已复核的高优先级问题

本报告使用以下分级：

- P0：当前默认配置下可直接造成越权、数据破坏、系统级不可用，或明确阻断本次发布核心验收的已证实问题；
- P1：核心功能缺陷、生产加固缺口，或启用高风险能力前必须完成的上线门禁；
- P2：可维护性、可观测性、文档一致性或非核心体验问题。

在本次静态审阅和有限验证范围内，没有把“默认关闭能力启用后才出现的风险”判为当前活跃 P0。若 AI Chat 被发布合同定义为核心验收功能，下述 P1-01 可在该发布范围内升级为 P0。

### P1-01：AI Chat 默认范围导致功能不可用

前端 AiChat 在未选集群时会提交 cluster_id=all；query-api 的 ProxyChat 对空集群或 all 直接返回 CLUSTER_ACCESS_DENIED。这是当前最确定的产品闭环缺陷。

建议：前端在发送前要求用户选择单一集群，或服务端返回可恢复的“请选择集群”业务错误；不要把多集群 all 偷偷映射为默认集群，也不要为了兼容而降低服务端的单集群授权要求。

### P1-02：自动处置只能在端到端证明后开启

当前 Helm 默认 grantK8sWrite=false，审批后执行默认也关闭。这是正确的 fail-closed 默认值。与此同时，若显式打开写权限，编排器 ClusterRole 仍可对 workload 执行 patch/scale、创建 pods/eviction、patch nodes。虽然注释说明其服务于受控动作，但 ClusterRole 的作用域仍然很大。

建议：生产上线前将动作缩减到命名空间 Role 和白名单目标；将节点动作拆为更严格的专用执行器；强制绑定审批人、动作哈希、目标快照、过期时间、幂等键、前后状态和验证结果。未完成这些线上演练前，保持两个开关关闭。

### P1-03：网络隔离尚未成为默认部署基线

NetworkPolicy 已提供默认拒绝出站和逐组件白名单模板，但 values.yaml 的 egressDefaultDeny 默认值为 false。编排器直连数据库凭据的注入默认关闭，这一点较好；然而网络层仍允许部署配置偏离最小访问原则。

建议：在预生产环境以 event-collector、ingest、query-api、orchestrator 的顺序灰度验证白名单，再将 egressDefaultDeny 设为生产默认。升级或异常回滚流程应同时验证 DNS、身份验证、观测后端和模型出口。

### P1-04：真实 Agent 能力未成为默认主链

完整运维图有计划、风险、审批、执行与验证节点；双 Agent 和函数调用循环也已存在。但主入口默认使用精简 chat_graph。需要选择一个唯一、受控、可审计的调查主链，并明确：

- 聊天只读，不创建 Run、不执行处置；
- “开始调查”必须显式创建单集群 Run；
- Run 内才允许 Planner、工具执行、RCA 和审批；
- 实际工具调用必须由执行器事件产生，而不是 UI/图节点推测；
- 写操作只能由经审批的结构化 OpsAction 进入专用执行适配器。

### P1-05：前端遗留入口与新网关必须做兼容性验收

query-api 对除 Run API 和 Chat 外的大多数 legacy AI 路由保持 fail-closed；同时前端仍保留 suggestion/execute 等调用封装。路由注册虽已覆盖 suggestion 前缀，但 ProxyAI 的实现说明其仅允许 Run 路径，其余旧路径返回 permission_denied。

这不是应通过放宽代理来修复的问题。应清理或迁移前端旧入口，令所有用户可触达功能经过同一套 JWT、租户、集群、能力与审计边界；并添加浏览器级回归测试确认没有“看得见但做不了”的按钮。

### P1-06：HA 与恢复需以运行证据确认

部署默认副本数为 1，编排器持久卷配置为 ReadWriteOnce。Run 和 SSE 的持久化设计已经优于历史内存模型，但仍需演练服务重启、重复派发、断线重连、Outbox 重试、并发创建和故障转移。

建议在上线门禁中加入：Run 创建幂等、跨租户拒绝、事件顺序、SSE Last-Event-ID 回放、编排器重启恢复和数据库短暂不可用恢复。

## 6. 已确认的积极整改

相较于历史审阅中常见的“standalone / test-only”担忧，以下能力在当前代码中已看到主入口接线，不能再简单判定为未实现：

- ManualBoundary 和 AuthorizationMatrix 已接入内部 RunInvocation 创建入口；
- Run 公共创建由 query-api 持久化并写 Outbox；
- Run SSE 由 query-api 从持久化事件回放和 live-tail，而非简单转发编排器内存流；
- 浏览器 AI Chat 已由独立 ProxyChat 处理，而不是由通用 ProxyAI 放行；
- 编排器 Kubernetes 写权限、数据库直连凭据和审批后执行均有默认关闭的部署/运行开关。

以上是代码接线结论，不等价于生产环境已正确配置。仍需以渲染后的 Helm、运行时环境变量、数据库 migration、身份源和真实集群演练证明。

## 7. 验证状态、测试限制与验收建议

### 7.1 本次实际验证状态

本次复审执行了少量针对性测试，结果如下：

- ai-apm-ingest-go/internal/pipeline 测试通过；
- ai-apm-query-go/internal/api 测试在当前受限环境中因 httptest 无法监听本地端口而中止，不能据此判定代码通过或失败；
- ai-orchestrator 的 Run/Chat 安全边界测试未能完成收集：本机为 Python 3.9、LangGraph 0.2.76，而项目 requirements 要求 LangGraph 1.x，容器基线为 Python 3.12。该结果说明本机依赖环境不符合项目要求，不等同于测试用例证明产品缺陷。

因此，本报告对未实际通过的能力只采用“源码已实现”“已看到入口接线”等表述，不声明全量测试通过或生产可用。正式发布必须在与容器一致、允许本地测试服务监听的可重复环境中重跑全量测试并保存结果。

### 7.2 发布验收建议

现有测试代码覆盖了不少安全边界、RCA 推理和状态行为，但测试代码存在不等于测试已经通过，“单元测试通过”也不能替代真实纵向闭环。建议将以下场景设为发布门禁：

1. 用户在未选集群时得到明确的选择提示；选择无权集群、all、伪造 tenant 或伪造 canonical ID 必须失败。
2. 一个 OOMKilled 或高错误率案例，从原始指标、日志、Trace、Kubernetes 事件到证据、RCA、建议和报告的关联完整可追溯。
3. 两个租户同名服务、同名命名空间、不同集群的查询和 Run 事件绝不串数据。
4. 创建 Run 后重启 query-api 与 orchestrator，验证 Outbox 不重复执行、SSE 可按 Last-Event-ID 连续回放。
5. 对一个最低风险、可回滚的动作执行“提案 → 审批 → 预检 → 执行 → 验证 → 审计”，并验证拒绝过期审批、目标漂移和动作哈希变化。
6. 在启用出站默认拒绝后，验证 DNS、观测查询、身份鉴权、模型服务和必要数据库访问均只通过白名单可达。
7. 将实际部署中 LLM_MOCK=false 的渲染结果和容器运行时环境纳入检查；模型不可用时页面和审计记录必须明确标记为降级结果，而非与真实模型结论混淆。

## 8. 建议的整改顺序

### 第一阶段：保证用户实际可用且不误导

- 修复 Chat 的单集群选择流程；
- 删除或迁移所有被 fail-closed 的旧前端操作入口；
- 把“工具活动”改为严格区分“计划/节点”与“已实际执行的工具调用”；
- 在 UI 中明确区分只读聊天、调查 Run、人工审批和真实执行状态。

### 第二阶段：收敛为唯一的调查主链

- 让显式创建的 Run 成为唯一能进入 Planner、证据、RCA、审批、执行和验证的入口；
- 将 function calling / Tool Registry / 实际执行器纳入该 Run 的事件与审计模型；
- 保持对话路径只读、单集群、无隐式副作用。

### 第三阶段：为受控执行建立生产门槛

- 生产默认开启网络出站拒绝并维护白名单；
- 保持 Kubernetes 写权限和执行开关默认关闭，按环境、租户、动作类型逐步放开；
- 将集群级写权限进一步收窄到最小命名空间和最小动作；
- 完成上一节的真实端到端演练，并将证据纳入发布记录。

## 9. 最终评价

平台的观测查询、多租户边界、可信上下文、持久化 Run/SSE 和结构化 RCA 已形成较清晰的代码级建设基础，但仍需运行验证证明其可靠性。当前更适合定位为“AI 辅助的可观测性与运维调查平台”：帮助人收集证据、形成假设、生成建议和保留审计记录。

若目标是生产级自然语言自动运维，下一步不应继续堆叠新的 Agent 模块，而应优先完成入口一致性、唯一主链、真实工具审计、最小权限执行和端到端演练。只有这些控制面能力得到运行证据支持后，才应逐步开放真实自动处置。

## 附：本次复核的关键代码位置

- 前端 Chat 默认集群：observability-frontend/src/pages/ai/AiChat.tsx
- 浏览器 AI 边界：ai-apm-query-go/internal/api/settings.go
- Run 持久化创建：ai-apm-query-go/internal/api/runs_public.go
- SSE 回放：ai-apm-query-go/internal/api/sse_proxy.go
- Run 人工边界与授权：ai-orchestrator/main.py、manual_boundary.py、authorization_matrix.py
- 对话与完整工作流选择：ai-orchestrator/orchestrator.py
- Kubernetes 权限与部署开关：deploy/helm/aiops/templates/ai-orchestrator/rbac.yaml、deploy/helm/aiops/values.yaml
- 网络隔离：deploy/helm/aiops/templates/networkpolicy.yaml
