# 部署验证

> 适用范围：本机 OrbStack/Kubernetes 验证环境，可销毁现有部署并从零重新安装。  
> 当前审查基线：`Jw-Jm/aiops-edge main@b30e6da492eb78d4337fe75fc7199a4d6f0ae271`。  
> 本文不承担历史数据保留、旧版本升级兼容、跨版本数据迁移和生产灰度升级设计。

## 1. 目标与结论

本次验证的目标不是证明“代码能编译”或“单元测试通过”，而是证明：**从一个没有旧 AIOps 数据、没有旧 PVC、没有旧 Secret 的干净本机 Kubernetes 环境出发，可以使用当前源码构建当前镜像，完整部署平台，并通过前端和真实 Kubernetes 对象验证 AIOps 主链真实可用。**

本机允许销毁重建，因此本方案采用 **Fresh Install / Fresh Database** 路线：

1. 删除旧 `observability`、`deepflow` 部署以及对应本机持久化数据。
2. 从当前 Git 提交构建全部自研镜像。
3. 用全新 Secret 初始化数据库账号和服务间身份。
4. 从空 MySQL 建立当前最新 schema。
5. 启动 Query API、Investigation Worker、LLM Egress Proxy、采集链、前端等运行时。
6. 先验证只读调查链，再验证审批链。
7. 最后只在专用 `aiops-canary` namespace 中开启 Action Executor 真实写操作，验证 mutation、TOCTOU、reconcile、verification、持久化恢复和前端展示。

### 1.1 明确不做的事情

本次不验证：

- 旧数据库数据迁移到新版本。
- 旧版本 schema 的在线升级兼容。
- 保留现有 PVC 后滚动升级。
- 生产环境多批次灰度升级。
- 历史 Run、Evidence、Action、审计数据保留。

### 1.2 `schema-migrator` 仍必须保留

“不做历史迁移”不等于“不需要 `schema-migrator`”。

当前 Query API 启动时执行的是只读 `RequireCurrent()` 检查，而数据库表结构由 `schema-migrator` 负责建立。对空数据库而言，`schema-migrator` 仍是 **从 0 建立到当前 schema 的正式初始化器**。

因此本机验证应取消“旧库升级兼容”要求，但不能删除 `schema-migrator`。正确目标是：

> 空 MySQL → 创建 `aiops_app` / `aiops_migrator` → 运行当前提交构建出的 schema-migrator → 一次性建立当前完整 schema → Query API `RequireCurrent()` 通过。

---

## 2. 当前代码状态判断

当前 AIOps 核心运行时架构已经基本具备完整验证条件：Run、Investigation、ToolRun/Evidence、Action、Approval、Action Outbox、Executor、Reconcile、Verification、SSE 等主链已经形成。

当前阻碍本机“从零一键部署验证”的主要问题不在核心状态机，而在 **部署封装和发布一致性**。

### 2.1 部署验证前必须修复的硬阻断项

| 编号 | 问题 | 当前影响 | 本机验证要求 |
|---|---|---|---|
| B1 | `aiops-secrets` 未渲染 `MYSQL_APP_PASSWORD`、`MYSQL_MIGRATOR_PASSWORD` | Query API、mysql-users-init、schema-migrator 在干净安装时无法正常启动 | 必须补齐 values、Secret 模板和部署脚本注入 |
| B2 | `ai-action-executor`、`ai-llm-egress-proxy` 无正式 Dockerfile；`build-images.sh` 未构建 executor/proxy/schema-migrator | 从干净 Git checkout 无法构建完整验证拓扑 | 必须补 Dockerfile 并纳入统一 build |
| B3 | Helm 中仍存在历史固定 tag 和 `latest`，schema-migrator 也是历史固定 tag | 当前源码、实际构建镜像、实际部署镜像可能不是同一版本 | 所有自研组件必须使用同一 `RELEASE_TAG` 或 digest |
| B4 | `mysql-users-init` 和 `mysql-init` 使用 `post-install` hook，而现有 `apply.sh` 一次性启用运行时并使用 `helm --wait` | Fresh Install 时 Query API 可能因 schema 未完成而不能 Ready，而 post-install hook 又要等普通资源进入 Helm 的 wait 阶段后才执行，形成启动顺序冲突 | **不直接使用现有一阶段 `apply.sh`**；本机验证改为 `bootstrap install → users-init/migrator 完成 → runtime upgrade` 两阶段 Helm 流程 |
| B5 | `aiActionExecutor.enabled=false` 仍会渲染 Executor Deployment/Service | “关闭组件”语义不真实，部署结果与 values 不一致 | 必须用 `if .Values.aiActionExecutor.enabled` 包住全部 Executor 资源 |
| B6 | Executor 写权限 namespace 写死为 `action-test` | 无法按验证环境精确限定写范围 | 必须改为 `targetNamespaces`，本机只允许 `aiops-canary` |
| B7 | `apply.sh` 未注入 canonical Investigation、LLM Proxy、Executor 所需的全部服务身份/签名密钥 | 组件即使 Pod 启动，也可能因服务间验签/代理凭据不完整而 fail-closed | 必须补齐本机验证 Secret 生成与注入 |
| B8 | `values.yaml` 对 Ed25519 私钥格式的注释与 Query API 实际解析契约不完全一致 | 例如 Query API 的 `DecodePrivateKey` 实际要求 **64-byte Ed25519 private key**，并按 `base64.RawURLEncoding` 解码；若按“32-byte raw private key”注释生成会启动失败 | 统一内部身份密钥生成脚本，以实际代码解析格式为准，并同步修正 values 注释 |

以上 B1～B8 未完成前，不应开始“功能可用性验收”，否则得到的结果只是部署脚本缺陷，而不是 AIOps 功能结论。

### 2.2 验证过程中检查，但不阻断开始的事项

- `mysql-migrations` ConfigMap 目前只包含旧 `0001_init.sql`，而 schema-migrator 实际使用自身嵌入的 versioned SQL。该 ConfigMap/挂载对当前 migrator 无实际作用，建议清理，避免误导。
- `ai-orchestrator` 仍保留旧有状态数据 PVC，但 canonical Investigation Worker 已经是独立无状态 worker。验证时应明确：主调查任务由 Worker 执行，旧 Orchestrator 不是 HA 调查执行器。
- `DeepFlow` 安装脚本当前允许失败后继续。对“平台基础部署”可以容忍，但如果本轮要验证网络/APM 数据场景，则 DeepFlow 必须 Ready 后才能判定相应能力通过。
- 备份、Ingress、正式存储类等不属于本机 AIOps 功能可用性硬门槛。

---

## 3. 本机验证目标拓扑

本轮验证统一采用如下 canonical 拓扑：

```text
Frontend
   │
   ▼
Query API ─────────────── MySQL
   │                       │
   │                       ├─ Run / ToolRun / Evidence
   │                       ├─ Action / Approval
   │                       ├─ Outbox / Attempt / Verification
   │                       └─ Audit / Lease / State
   │
   ├────────► Investigation Worker (2 replicas)
   │               │
   │               └────────► LLM Egress Proxy ─────────► Real LLM Provider
   │
   ├────────► VictoriaMetrics / VictoriaLogs / ClickHouse
   │
   └────────► Action Dispatcher
                       │
                       ▼
                Action Executor
                       │
                       ▼
              Kubernetes API
                       │
                       ▼
                aiops-canary
```

### 3.1 固定运行开关

本机最终验证 profile 必须明确设置：

```yaml
aiOrchestrator:
  llmMock: "false"
  investigationRuntimeEnabled: "0"
  legacyFlowRuntimeEnabled: "0"
  investigatorEnabled: "0"
  legacyDirectMutationsEnabled: "0"

investigationWorker:
  enabled: true
  replicas: 2

llmEgressProxy:
  enabled: true
```

说明：

- canonical Investigation 由 `ai-investigation-worker` 执行。
- `ai-orchestrator` 不再承担 canonical Investigation runtime。
- 旧 Flow、旧 Investigator、旧直接 mutation 全部关闭。
- LLM 必须走 egress proxy。
- `LLM_MOCK=false`，必须使用至少一个真实 provider key，才能证明 AI 能力真实可用，而不是 mock。

### 3.1.1 本机验证 profile 不直接使用 `values-prod.yaml`

`values-prod.yaml` 中包含正式环境 `storageClass: standard` 等基础设施假设，本机 OrbStack 不应为了“看起来像生产”直接套用。应新增 `values-local-validation.yaml`，只覆盖本轮需要验证的 canonical AIOps 行为，例如：

```yaml
aiOrchestrator:
  llmMock: "false"
  investigationRuntimeEnabled: "0"
  legacyFlowRuntimeEnabled: "0"
  investigatorEnabled: "0"
  legacyDirectMutationsEnabled: "0"

investigationWorker:
  enabled: true
  replicas: 2
  image:
    repository: ai-orchestrator
    tag: "{{ RELEASE_TAG }}"   # 实际实现应统一引用 global.imageTag，而不是保留独立 tag

llmEgressProxy:
  enabled: true

aiActionExecutor:
  enabled: true
  executionMode: disabled
  realMutation: false
```

最终 Chart 模板应让这些自研组件直接引用统一 `global.imageTag`，上面 `RELEASE_TAG` 仅表达目标，不应在正式 values 中写死。

### 3.2 Executor 分两阶段

第一阶段仅验证完整审批链，不写 Kubernetes：

```yaml
aiActionExecutor:
  enabled: true
  executionMode: disabled
  realMutation: false
```

第二阶段只对 canary namespace 开启真实 mutation：

```yaml
aiActionExecutor:
  enabled: true
  executionMode: approved
  realMutation: true
  targetNamespaces:
    - aiops-canary
```

真实写阶段必须满足：

- 仅 `deployments`。
- 仅 `get`、`patch`。
- 无 `delete`。
- 无 ClusterRole 写权限。
- 无其他 namespace 写权限。

---

## 4. 部署前代码封装整改

本节不是重新设计 AIOps，而是把现有代码封装成可以被可靠验证的部署产物。

### 4.1 Secret 闭环

`values.yaml` 增加：

```yaml
secrets:
  mysqlAppPassword: ""
  mysqlMigratorPassword: ""
```

`aiops-secrets` 增加：

```yaml
MYSQL_APP_PASSWORD: ...
MYSQL_MIGRATOR_PASSWORD: ...
```

并要求：

- `MYSQL_ROOT_PASSWORD`、`MYSQL_APP_PASSWORD`、`MYSQL_MIGRATOR_PASSWORD` 三者必须不同。
- 空值和 `CHANGE_ME` 必须在 Helm render 阶段直接失败。
- Query API 只拿 `MYSQL_APP_PASSWORD`。
- schema-migrator 只拿 `MYSQL_MIGRATOR_PASSWORD`。
- 只有 users-init Job 拿 root 密码。

本机验证还必须生成并注入以下服务身份：

```text
ORCHESTRATOR_TO_QUERY_TOKEN
ORCHESTRATOR_TO_QUERY_SIGNING_KEY
ORCHESTRATOR_TO_QUERY_VERIFY_KEYS

QUERY_TO_ORCHESTRATOR_TOKEN
QUERY_TO_ORCHESTRATOR_SIGNING_KEY
QUERY_TO_ORCHESTRATOR_VERIFY_KEYS

EXECUTOR_TOKEN
AI_ACTION_EXECUTOR_SIGNING_KEY
AI_ACTION_EXECUTOR_VERIFY_KEYS

LLM_PROXY_TOKEN
LLM_PROVIDER_KEYS
```

`LLM_PROVIDER_KEYS` 当前格式为：

```text
deepseek:sk-xxxx
```

或：

```text
openai:sk-xxxx
```

也可以同时配置：

```text
deepseek:sk-xxxx,openai:sk-yyyy
```

Ed25519 私钥/公钥必须由项目统一生成脚本产生，三条方向分别生成独立 key pair，禁止复用：

1. Orchestrator/Worker → Query API。
2. Query API → Orchestrator/Worker。
3. Query API → Action Executor。

特别要求：**不要直接按当前 `values.yaml` 中“32-byte raw private key”的注释手工生成 Query API 私钥。** 当前 Go 代码 `auth.DecodePrivateKey()` 明确要求 `ed25519.PrivateKeySize=64` 字节，并使用 `base64.RawURLEncoding` 解码。因此在开始 Fresh Install 前，应先把“密钥生成脚本 + values 注释 + Go/Python 两侧解析格式”统一，并通过现有 cross-language fixture/unit test 验证。验证环境只使用该脚本生成的输出，不接受人工拼接 key。

### 4.2 全量镜像构建

`build-images.sh all` 必须覆盖至少：

```text
observability-frontend
query-api
ingest-pipeline
ai-orchestrator
event-collector
ai-action-executor
ai-llm-egress-proxy
schema-migrator
ipmi-exporter
```

其中：

- `ai-action-executor` 增加正式 Dockerfile。
- `ai-llm-egress-proxy` 增加正式 Dockerfile。
- `schema-migrator` 使用 `ai-apm-query-go/Dockerfile.schema-migrator` 构建。
- Investigation Worker 与 Orchestrator 必须使用 **同一个 `ai-orchestrator` 镜像**。

### 4.3 单一版本来源

部署前：

```bash
export RELEASE_SHA="$(git rev-parse HEAD)"
export RELEASE_TAG="git-${RELEASE_SHA:0:12}"
```

所有自研镜像统一：

```text
<image>:${RELEASE_TAG}
```

禁止：

- `latest`
- `v1.2.0-p20-24b157a0` 等历史固定 tag
- `schema-migrator:rebuild-20260820`
- Executor/Proxy 独立历史 tag

Helm render 后必须能证明全部自研镜像引用的 tag 与 `RELEASE_TAG` 完全一致。

### 4.4 Fresh Install 的数据库启动顺序

本机不需要做旧库升级，因此不必重构 migration 引擎。最小、最稳妥的方案是保留现有 users-init/schema-migrator Job，但把 Helm 部署拆成 **bootstrap + runtime 两阶段**。

目标顺序：

```text
Phase A: bootstrap Helm install
         仅启用 Secret + MySQL/ClickHouse/VM/VLogs 等基础中间件
         Query API / Worker / Orchestrator / Ingest / Event Collector /
         Frontend / LLM Proxy / Executor 全部关闭
       ↓
Phase B: MySQL Ready
       ↓
Phase C: post-install mysql-users-init 成功
       ↓
Phase D: post-install schema-migrator 成功
       ↓
Phase E: runtime Helm upgrade
         启用 Query API / Worker / Proxy / 采集 / Frontend / Executor(disabled)
```

这样处理可以保留当前 migration 代码，同时消除一阶段 Fresh Install 的启动顺序冲突：

- 第一次 `helm install --wait` 时没有 Query API 等依赖 schema 的运行时 Pod 参与 Ready 判定。
- MySQL Ready 后，现有 post-install hook 按 weight 10/20 顺序执行 users-init 和 migrator。
- 第二次 `helm upgrade --wait` 时 schema 已是当前版本，Query API 的 `RequireCurrent()` 可以直接通过。
- 本机每次都从空库开始，因此不需要验证 post-upgrade migration 兼容性。

新增本机 bootstrap profile：`deploy/helm/aiops/values-local-bootstrap.yaml`，至少关闭：

```yaml
frontend:
  enabled: false
queryApi:
  enabled: false
ingest:
  enabled: false
aiOrchestrator:
  enabled: false
investigationWorker:
  enabled: false
eventCollector:
  enabled: false
llmEgressProxy:
  enabled: false
aiActionExecutor:
  enabled: false
```

基础中间件保持开启。

Bootstrap：

```bash
helm upgrade --install aiops deploy/helm/aiops   -n observability --create-namespace   -f deploy/helm/aiops/values-local-bootstrap.yaml   -f /tmp/aiops-local-secrets.yaml   --set deepflow.enabled=false   --set global.imageTag="${RELEASE_TAG}"   --wait   --timeout 15m
```

Bootstrap 完成后确认两个 Job：

```bash
kubectl -n observability wait   --for=condition=complete job/mysql-users-init   --timeout=180s

kubectl -n observability wait   --for=condition=complete job/mysql-init   --timeout=300s
```

随后执行 runtime upgrade：

```bash
helm upgrade aiops deploy/helm/aiops   -n observability   -f deploy/helm/aiops/values-local-validation.yaml   -f /tmp/aiops-local-secrets.yaml   --set deepflow.enabled=false   --set global.imageTag="${RELEASE_TAG}"   --wait   --timeout 15m
```

`values-local-validation.yaml` 基于本机 `values.yaml`，只覆盖 canonical AIOps 行为，不直接套用 `values-prod.yaml` 中的 `storageClass: standard` 等生产基础设施参数。

只有 `mysql-users-init` 和 `mysql-init` 都完成后，才允许启用 Query API。

### 4.5 现有 `apply.sh` 的使用结论

在 B1～B8 修复前，**不要把当前 `deploy/scripts/apply.sh` 作为 Fresh Install 验收入口**。原因不是脚本完全不可用，而是它目前：

- 只构建部分自研镜像；
- 只注入部分 Secret；
- 一次性启用运行时并 `--wait`；
- 不能保证 canonical Worker/LLM Proxy/Executor 全链所需凭据；
- 不能证明部署镜像与当前源码一一对应。

整改后的目标不是再造一套复杂发布系统，而是让本机入口明确执行：

```text
generate secrets
→ build all images with RELEASE_TAG
→ bootstrap helm install
→ wait users-init
→ wait schema-migrator
→ runtime helm upgrade
→ deploy DeepFlow
→ run validation
```

---

## 5. 干净环境准备

### 5.1 记录当前源码

```bash
git status --short
git rev-parse HEAD
git branch --show-current
```

要求：

- 当前分支明确。
- `HEAD` 明确。
- 本轮验证报告记录完整 SHA。
- 若有未提交代码，则本轮结果不能标记为“GitHub main 对应版本验证通过”。

### 5.2 删除旧部署

本机允许销毁，因此直接执行干净重建：

```bash
helm uninstall aiops -n observability 2>/dev/null || true
helm uninstall deepflow -n deepflow 2>/dev/null || true

kubectl delete namespace observability --ignore-not-found --wait=true
kubectl delete namespace deepflow --ignore-not-found --wait=true
kubectl delete namespace aiops-canary --ignore-not-found --wait=true
```

随后检查：

```bash
kubectl get ns observability deepflow aiops-canary
kubectl get pvc -A
```

要求：

- 不存在旧 `observability` PVC。
- 不存在旧 MySQL/ClickHouse/VM/VLogs/Orchestrator/WAL 数据。
- 如果本机 StorageClass 使用 `Retain`，必须删除本轮旧部署对应的残留 PV 后再继续。
- 不复用旧 `aiops-secrets`。

### 5.3 建立 canary namespace

```bash
kubectl create namespace aiops-canary
```

创建唯一真实写测试对象：

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aiops-mutation-canary
  namespace: aiops-canary
  labels:
    app: aiops-mutation-canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aiops-mutation-canary
  template:
    metadata:
      labels:
        app: aiops-mutation-canary
    spec:
      containers:
        - name: nginx
          image: nginx:alpine
```

要求：

```bash
kubectl -n aiops-canary rollout status deployment/aiops-mutation-canary
```

通过后记录初始：

```bash
kubectl -n aiops-canary get deployment aiops-mutation-canary \
  -o jsonpath='{.metadata.resourceVersion}{"\n"}'
```

---

## 6. 构建验证

### 6.1 先跑现有质量门

```bash
make test-workflow-all
./deploy/scripts/verify-aiops-workflow-gates.sh
```

要求全部通过。

### 6.2 构建全部自研镜像

```bash
export RELEASE_SHA="$(git rev-parse HEAD)"
export RELEASE_TAG="git-${RELEASE_SHA:0:12}"

IMAGE_TAG="${RELEASE_TAG}" ./deploy/scripts/build-images.sh all
```

### 6.3 校验镜像存在

至少检查：

```bash
for image in \
  observability-frontend \
  query-api \
  ingest-pipeline \
  ai-orchestrator \
  event-collector \
  ai-action-executor \
  ai-llm-egress-proxy \
  schema-migrator \
  ipmi-exporter
do
  docker image inspect "${image}:${RELEASE_TAG}" >/dev/null \
    || { echo "missing image: ${image}:${RELEASE_TAG}"; exit 1; }
done
```

### 6.4 Helm render 一致性检查

部署前先 render：

```bash
helm lint deploy/helm/aiops

helm template aiops deploy/helm/aiops \
  -n observability \
  -f deploy/helm/aiops/values-local-validation.yaml \
  --set global.imageTag="${RELEASE_TAG}" \
  > /tmp/aiops-rendered.yaml
```

检查：

```bash
grep -n 'image:' /tmp/aiops-rendered.yaml
```

必须满足：

- 自研镜像全部为 `${RELEASE_TAG}`。
- 无 `:latest`。
- 无历史固定 tag。
- `ai-investigation-worker` 与 `ai-orchestrator` 的 image 完全相同。
- schema-migrator 与 Query API 来自同一 Git SHA 对应构建。
- Executor `enabled=false` 时 render 中不存在 Executor Deployment/Service/SA/Role/RoleBinding。
- Executor `realMutation=false` 时不存在写 RBAC。
- Executor `realMutation=true` 时只产生 `aiops-canary` 的 `deployments get/patch` 权限。

---

## 7. Fresh Database 初始化验证

### 7.1 MySQL Ready

```bash
kubectl -n observability wait \
  --for=condition=ready pod \
  -l app=mysql \
  --timeout=300s
```

### 7.2 用户初始化

执行 `mysql-users-init` 后：

```bash
kubectl -n observability wait \
  --for=condition=complete job/mysql-users-init \
  --timeout=180s
```

失败时：

```bash
kubectl -n observability logs job/mysql-users-init
```

验收：

- `aiops_app` 创建成功。
- `aiops_migrator` 创建成功。
- 两者密码不同。
- `aiops_app` 没有 DDL 权限。
- `aiops_migrator` 具备本项目 migration 所需 DDL 权限。

### 7.2.1 账号权限核对

使用 root 仅做验收查询：

```sql
SHOW GRANTS FOR 'aiops_app'@'%';
SHOW GRANTS FOR 'aiops_migrator'@'%';
```

判定：

- `aiops_app` 只能对 `aiops.*` 做运行时所需 DML，不得拥有 `CREATE`、`ALTER`、`DROP`。
- `aiops_migrator` 拥有当前 migration 所需 DDL。
- Query API Deployment 的 `MYSQL_USER` 必须是 `aiops_app`。

### 7.3 schema 初始化

```bash
kubectl -n observability wait \
  --for=condition=complete job/mysql-init \
  --timeout=300s
```

检查日志：

```bash
kubectl -n observability logs job/mysql-init
```

必须出现：

```text
schema-migrator: migrations applied successfully
```

验收：

- 空库可以一次建立到当前 schema。
- `aiops_schema_migrations` 中存在当前代码包含的全部版本记录；当前基线应覆盖 `0001_control_plane_baseline`、`0002_ai_runtime`、`0003_ai_runtime_v2`、`0003_platform_audit`、`0003b_ai_runtime_recovery`、`0004_runtime_convergence`、`0005_alert_worker_state`、`0006_lease_cancel_toolrun`、`0007_ai_action_execution`、`0008_action_attempt_verification`、`0009_action_workflow_closure`。
- checksum 正常。
- 没有跳过失败 migration。
- 无需验证“旧版本已有数据如何升级”。

### 7.4 Query API fail-closed 验证

在 schema 初始化完成前，不应把 Query API 判定为 Ready。

schema 完成后启动 Query API：

```bash
kubectl -n observability rollout status deployment/query-api
```

检查：

```bash
kubectl -n observability get pods
kubectl -n observability logs deployment/query-api --tail=200
```

必须满足：

- 不再出现 schema not current。
- 不存在 `MYSQL_APP_PASSWORD` Secret key 缺失。
- `/readyz` 为 200。
- Query API 使用 `aiops_app`，而不是 root/migrator。

---

## 8. 平台基础健康验证

执行：

```bash
kubectl -n observability get deploy,statefulset,daemonset,job,pod,svc
kubectl -n deepflow get pods
```

### 8.1 必须 Ready 的核心组件

至少：

```text
query-api
observability-frontend
ai-investigation-worker
ai-llm-egress-proxy
mysql
victoria-metrics
victoria-logs
clickhouse
ingest-pipeline
event-collector
```

如果本轮包含 DeepFlow/APM 网络能力验收，则 DeepFlow 相关 Pod 也必须 Ready。

### 8.2 禁止用“Pod Running”代替功能验证

以下情况均不能判为通过：

- Pod Running，但 `/readyz` 失败。
- Worker Running，但无法领取 Run lease。
- Proxy Running，但无真实 provider key。
- Query API Running，但 SSE 无事件。
- Executor Running，但 `executionMode=disabled` 时仍能直接写 K8s。
- 前端可打开，但所有数据来自静态 mock。

---

## 9. 采集与可观测数据验证

AIOps 要判定“真实可用”，至少要有真实输入数据。

### 9.1 Kubernetes 事件

在 `aiops-canary` 产生一次真实事件，例如：

```bash
kubectl -n aiops-canary annotate deployment aiops-mutation-canary \
  aiops-test-event="$(date +%s)" --overwrite
```

要求 Event Collector 能采集到相应 Kubernetes 事件或对应资源变化数据，Query API 能查询到。

### 9.2 主机指标

确认 Categraf/对应采集链向 VictoriaMetrics 持续写入。

验收：

- CPU、内存、磁盘、网络至少有一组真实主机时间序列。
- Query API 查询接口返回真实时间戳和值。
- 前端图表显示值与 Query API 返回一致。

### 9.3 日志

创建一个带唯一标记的真实日志源：

```bash
LOG_MARKER="AIOPS_LOG_VALIDATION_$(date +%s)"
kubectl -n aiops-canary run aiops-log-test   --image=busybox:1.36   --restart=Never   -- sh -c "echo ${LOG_MARKER}; sleep 300"
```

先确认容器真实输出：

```bash
kubectl -n aiops-canary logs pod/aiops-log-test
```

随后通过平台验证：

```text
采集 → VictoriaLogs → Query API → 前端
```

必须检查：

- Query API 能按 `${LOG_MARKER}` 精确查到日志。
- namespace/resource/cluster/tenant 归属正确。
- 时间戳与实际生成时间一致。
- 前端能按标记和时间范围过滤。
- 不使用静态 mock 或预置数据库记录代替。

若 Kubernetes 日志采集链当前没有覆盖该 Pod，则本项判失败，并形成明确缺陷；不能改用 `kubectl logs` 冒充平台日志能力。

验证完成后：

```bash
kubectl -n aiops-canary delete pod aiops-log-test --ignore-not-found
```

### 9.4 DeepFlow

当前 Chart 将 DeepFlow 作为平台内置子系统，且 ingest 存在 DeepFlow 数据同步配置。因此，**要得出“完全满足设计要求”结论，本轮必须覆盖网络/APM**：

```text
DeepFlow Agent → DeepFlow Server/ClickHouse → ingest → 平台查询 → Investigation Evidence
```

必须至少构造一次真实请求流量，并确认 flow/span 能被调查工具读取。

DeepFlow 未 Ready 或没有真实 flow/span 时，本轮最多只能得出“AIOps 核心控制链通过”，不能得出“平台完全满足设计要求”。

---

## 10. 真实 LLM 链验证

### 10.1 Proxy readiness

```bash
kubectl -n observability get pod -l app=ai-llm-egress-proxy
kubectl -n observability logs deployment/ai-llm-egress-proxy --tail=100
```

必须满足：

- `LLM_PROVIDER_KEYS` 至少包含一个有效 provider。
- `LLM_PROXY_TOKEN` 非空。
- `/readyz` 返回 200。

### 10.2 出站边界

验证：

- Worker/Orchestrator 只配置 `LLM_PROXY_URL` 和 proxy token。
- Provider API key 只存在于 egress proxy Pod。
- Worker/Orchestrator 环境中没有真实 provider key。

### 10.3 实际模型调用

通过正常 AIOps Investigation 发起一次需要 LLM 的任务。

判定标准：

- Proxy 有真实请求审计。
- Provider 返回成功。
- Investigation 中能看到模型生成参与结果。
- `LLM_MOCK=false`。
- 若真实 provider 不可达或 key 无效，本项直接失败，不得用 mock 结果替代。

---

## 11. Canonical 只读调查链验证

这是判断 AIOps 是否“真正可用”的第一条核心主链。

### 11.1 从前端发起

必须从前端页面发起，而不是只用数据库或内部 API。

推荐测试问题：

```text
分析 aiops-canary 中 aiops-mutation-canary 当前状态，
检查最近 Kubernetes 事件、相关指标和日志，
给出根因判断和证据。
```

### 11.2 后端持久化链

必须确认同一 `run_id` 下形成：

```text
Run
  ↓
Lease
  ↓
ToolRun
  ↓
Evidence
  ↓
Hypothesis / RCA
  ↓
Run terminal state
```

要求：

- Run 不是仅存在内存。
- Worker 领取 lease。
- ToolRun 有明确开始、完成或失败状态。
- Evidence 可追溯到真实查询结果。
- RCA 明确引用 Evidence。
- Run 最终进入合法 terminal state。

### 11.3 前端展示

前端必须能看到：

- 运行状态。
- 调查步骤。
- 工具调用。
- Evidence。
- RCA。
- 时间线。
- 最终结论。

展示数据必须与 Query API 返回和数据库状态一致。

---

## 12. SSE 与断线恢复验证

### 12.1 正常流

运行 Investigation 时观察前端实时更新：

```text
queued
→ running
→ tool started
→ evidence produced
→ RCA produced
→ completed
```

### 12.2 浏览器刷新

Investigation 运行中刷新页面。

要求：

- Run 不丢失。
- 页面可恢复当前状态。
- 已产生事件不重复污染 UI。
- 之后的新事件继续到达。

### 12.3 SSE 重连

人为断开前端网络后恢复。

要求：

- 使用 sequence/after_sequence 恢复。
- 不遗漏关键事件。
- 不把同一事件重复展示为多个步骤。

---

## 13. Action 审批链验证（先不执行）

保持：

```yaml
aiActionExecutor:
  enabled: true
  executionMode: disabled
  realMutation: false
```

从一次 Investigation 产生修复建议。

前端 Action 页面必须明确显示：

- 目标 cluster。
- namespace。
- resource kind/name。
- 目标 UID。
- `resourceVersion`。
- action 类型。
- 标准化参数。
- action hash/version。
- 风险说明。
- 审批状态。

### 13.1 审批持久化

审批后检查：

```text
Action
→ Approval
→ Action Outbox
```

要求：

- 审批记录可审计。
- 同一 Action 重复点击审批不会形成两个独立执行意图。
- Executor disabled 时不得出现 Kubernetes 变更。
- 前端必须明确显示“已审批但执行能力关闭/未执行”，不能伪装成成功。

---

## 14. Executor 安全边界验证

在打开真实 mutation 前先验证 RBAC。

### 14.1 权限检查

```bash
kubectl auth can-i get deployments \
  -n aiops-canary \
  --as=system:serviceaccount:observability:ai-action-executor

kubectl auth can-i patch deployments \
  -n aiops-canary \
  --as=system:serviceaccount:observability:ai-action-executor

kubectl auth can-i delete deployments \
  -n aiops-canary \
  --as=system:serviceaccount:observability:ai-action-executor
```

预期：

```text
get    yes
patch  yes
delete no
```

再检查其他 namespace：

```bash
kubectl auth can-i patch deployments \
  -n observability \
  --as=system:serviceaccount:observability:ai-action-executor
```

预期：

```text
no
```

### 14.2 Orchestrator 不得拥有 mutation 权限

```bash
kubectl auth can-i patch deployments \
  -n aiops-canary \
  --as=system:serviceaccount:observability:ai-orchestrator
```

预期：

```text
no
```

Query API 也不应直接具有目标 workload patch 权限。

---

## 15. 真实 Kubernetes mutation 验证

确认所有只读、审批、安全边界均通过后，才切换：

```yaml
aiActionExecutor:
  enabled: true
  executionMode: approved
  realMutation: true
  targetNamespaces:
    - aiops-canary
```

### 15.1 第一项真实 mutation：annotation patch

第一项不采用 scale/restart，优先做最低影响 annotation patch：

```text
metadata.annotations["aiops.openai.com/validation"] = <run-id>
```

要求：

1. 必须来自已审批 Action。
2. Query API 生成 signed execution context。
3. Executor 成功验签。
4. Executor 校验 UID/resourceVersion。
5. Kubernetes 只发生一次 patch。
6. Action Attempt 持久化。
7. Reconcile 能确认实际对象状态。
8. Verification 判定成功。
9. 前端显示实际执行结果而不是预期结果。

验证对象：

```bash
kubectl -n aiops-canary get deployment aiops-mutation-canary -o yaml
```

---

## 16. TOCTOU 验证

目标：证明“审批时看到的对象”和“执行时真实对象”不一致时，系统不会盲目执行。

### 16.1 故障注入要求

TOCTOU 必须稳定制造“Action 已按 `resourceVersion=A` 完成审批，但 Executor 尚未收到请求”的窗口。不能依赖人工快速点击。

本机 E2E 需要一个 local-validation 专用 dispatch gate，语义为：

```text
Action approved
→ Outbox 已生成
→ 暂停向 Executor 发送
→ 外部修改 Kubernetes 对象
→ 释放 dispatch gate
→ Executor 收到旧 resourceVersion 的合法已审批请求
```

该 gate 仅用于本机验证，默认关闭，不进入生产业务语义。

### 16.2 步骤

1. 创建 Action，确认审批页面记录目标 UID 和 `resourceVersion=A`。
2. 打开 local-validation dispatch gate。
3. 从前端审批 Action，确认 Approval 和 Outbox 已持久化，但 Executor 尚未执行。
4. 手工修改目标 Deployment：

```bash
kubectl -n aiops-canary annotate deployment aiops-mutation-canary   external-change="$(date +%s)" --overwrite
```

5. 确认 Kubernetes 对象已变为 `resourceVersion=B`，且 `B != A`。
6. 释放 dispatch gate，让原 Action 正常发送到 Executor。

### 16.3 预期

- Executor 验签通过，但因 UID/resourceVersion 前置条件不匹配拒绝 mutation。
- 返回明确 drift/TOCTOU 冲突。
- 原审批 Action 不发生真实 patch。
- Attempt 记录冲突。
- Reconcile 读取 `resourceVersion=B` 的实际对象。
- Verification 不得错误标记成功。
- 前端明确提示“目标资源在审批后已变化，需要基于新版本重新生成/重新审批 Action”。

---

## 17. response lost / reconcile 验证

目标：证明“Executor 已经完成 Kubernetes mutation，但 Query API 没收到成功响应”时，系统不会通过简单重试造成第二次 mutation。

### 17.1 故障注入必须可重复

本项不能把“人工随机删除 Executor Pod”作为正式验收方法，因为无法证明 Pod 是在 **Kubernetes patch 已成功、HTTP response 尚未被 Query API 接收** 的准确窗口内被终止。

本机 E2E 必须提供一个确定性的 response-loss 注入点，满足以下语义：

```text
Executor 接收合法已审批请求
→ 完成 Kubernetes patch
→ 记录/可观察到 patch 已应用
→ 在返回 HTTP success 之前主动断开连接
```

实现方式限定为 **测试 harness 或 local-validation 专用 fault injection**，不得改变 production 默认行为。触发条件必须绑定测试 Action ID/显式测试开关，默认关闭。

### 17.2 验证步骤

1. 生成并审批一个新的 annotation Action。
2. 为该 Action 打开一次性 `drop-response-after-apply` 故障注入。
3. Query API 正常 dispatch。
4. Executor 实际完成一次 Kubernetes patch，但连接在 success response 前断开。
5. Query API 不得把该请求直接当作“确定未执行”再次 mutation。
6. 系统进入不确定/待 reconcile 状态。
7. Reconcile 读取实际 Kubernetes 对象。
8. Verification 根据实际状态收敛 Action。

### 17.3 预期

最终链路应表现为：

```text
Attempt → unknown/reconciling
        → Reconcile(actual object)
        → applied/not-applied determination
        → Verification
        → terminal state
```

硬要求：

- Kubernetes annotation 只变化一次。
- 同一 Action 不产生第二次真实 patch。
- HTTP response 丢失不能直接触发盲重试 mutation。
- Reconcile 的判断来自 Kubernetes 实际对象，而不是客户端猜测。
- 前端显示“执行结果已通过 reconcile 确认”，不能把网络异常直接显示为执行失败或执行成功。

## 18. Query API / Worker 重启恢复验证

### 18.1 Query API 重启

在 Run 正在进行时：

```bash
kubectl -n observability rollout restart deployment/query-api
```

预期：

- Run 持久化不丢。
- Query API 恢复后可继续读取状态。
- SSE 前端重连后可恢复。

### 18.2 Worker 重启

删除正在处理任务的 Worker Pod：

```bash
kubectl -n observability delete pod <worker-pod>
```

预期：

- lease/fencing 生效。
- 旧 Worker 不得在失去 lease 后继续合法提交结果。
- 新 Worker 可恢复可恢复任务。
- 不产生重复 ToolRun/Evidence。
- Run 最终可以正常终止。

---

## 19. 幂等性验证

至少覆盖：

1. 重复审批同一 Action。
2. 重复触发 dispatch。
3. Executor 请求重试。
4. Worker 重启后重新领取任务。
5. SSE 重连。
6. 浏览器重复提交。

判定：

- 不产生重复 Kubernetes mutation。
- 不产生两个语义相同的已执行 Action。
- Evidence/ToolRun 不因重试无限复制。
- UI 状态最终与数据库权威状态一致。

---

## 20. 前端完整功能验证

本轮最终验收必须以“前端页面可以完成实际闭环”为准。

至少覆盖：

| 页面/能力 | 验证内容 |
|---|---|
| 登录 | 真实账号登录、错误密码、Token 过期 |
| 集群/资源 | 能看到真实 `aiops-canary` 资源 |
| 指标 | 数据值、时间范围、单位、空数据 |
| 日志 | 查询、筛选、时间范围、资源关联 |
| 事件 | Kubernetes 真实事件 |
| Investigation | 创建、运行、步骤、Evidence、RCA |
| Run 历史 | 刷新后仍存在、状态一致 |
| Action | 建议、审批、拒绝、执行状态 |
| Approval | UID/resourceVersion/hash/参数展示完整 |
| Verification | 执行后验证结果 |
| SSE | 实时更新、刷新恢复、断线恢复 |
| 错误展示 | LLM 失败、工具失败、TOCTOU、Executor unavailable 均不能显示为成功 |

关键原则：

> 前端显示必须能通过 API、数据库状态和 Kubernetes 实际对象三方交叉验证。

---

## 21. “AIOps 真实可用”最终判定标准

只有以下条件全部通过，才能结论为“本机 AIOps 真实可用”。

### 21.1 部署真实性

- 从空 namespace、空 PVC、空 Secret 开始。
- 当前源码能构建全部自研镜像。
- 部署镜像与当前 Git SHA 一致。
- 无生产/验证主链自研组件使用 `latest` 或历史固定 tag。
- 空 MySQL 能初始化到当前 schema。
- 所有核心组件 Ready。

### 21.2 数据真实性

- 至少有真实 Kubernetes 事件。
- 至少有真实指标。
- 至少有真实日志。
- 若声明网络/APM能力通过，则必须有真实 DeepFlow flow/span。
- 前端数据可与后端原始结果核对。

### 21.3 AI 真实性

- `LLM_MOCK=false`。
- 通过 LLM Egress Proxy 调用真实 provider。
- Provider key 不进入 Worker/Orchestrator。
- Investigation 使用真实工具数据形成 Evidence。
- RCA 可追溯到 Evidence，而不是纯模型自由生成。

### 21.4 状态机真实性

- Run 持久化。
- Lease/fencing 生效。
- ToolRun/Evidence 持久化。
- Action/Approval/Outbox/Attempt/Reconcile/Verification 持久化。
- 非法状态转换不被接受。
- 重启后状态能恢复。

### 21.5 mutation 安全真实性

- Orchestrator 无直接写权限。
- Query API 无目标 workload 直接 patch 权限。
- Executor 是唯一 mutation owner。
- Executor 只写 `aiops-canary`。
- 只允许 Deployment get/patch。
- 必须审批。
- 必须 signed context。
- UID/resourceVersion 校验生效。
- TOCTOU 生效。
- response lost 时通过 reconcile 收敛，不重复 mutation。
- Verification 与实际 Kubernetes 状态一致。

### 21.6 前端真实性

- 调查可从前端发起。
- Evidence/RCA 可查看。
- Action 可审批/拒绝。
- mutation 状态可查看。
- SSE 可恢复。
- 所有失败状态都能正确显示。
- UI 不以静态 mock 代替真实后台结果。

---

### 21.7 E2E 故障注入真实性

以下故障必须可重复触发，不能依赖人工碰运气：

- TOCTOU：通过“Executor 暂停/不可达 → 审批 → 外部修改对象 → 恢复 Executor”稳定制造 `resourceVersion` 漂移。
- response lost：通过 local-validation 专用 fault injection 在“patch 已应用、response 未返回”窗口稳定断连。
- Worker 丢失 lease：通过删除持有 lease 的 Worker Pod 触发。
- Query API 重启：通过 Deployment restart 触发。

---

## 22. 最终验收矩阵

| 验证项 | 必须通过 | 失败是否阻断“AIOps真实可用” |
|---|---:|---:|
| 现有单元/契约测试 | 是 | 是 |
| 全部自研镜像干净构建 | 是 | 是 |
| 镜像与 Git SHA 一致 | 是 | 是 |
| 空库初始化 | 是 | 是 |
| Query API Ready | 是 | 是 |
| Investigation Worker 2 副本 | 是 | 是 |
| 真实 LLM Proxy 调用 | 是 | 是 |
| 真实指标 | 是 | 是 |
| 真实日志 | 是 | 是 |
| 真实 Kubernetes 事件 | 是 | 是 |
| DeepFlow flow/span | 是（完整设计验收） | 是；缺失时只能判核心链通过 |
| 前端只读 Investigation 闭环 | 是 | 是 |
| ToolRun/Evidence/RCA 持久化 | 是 | 是 |
| SSE 实时与恢复 | 是 | 是 |
| Action 审批链 | 是 | 是 |
| Executor RBAC 最小权限 | 是 | 是 |
| annotation patch | 是 | 是 |
| TOCTOU | 是 | 是 |
| response lost/reconcile | 是 | 是 |
| Query API 重启恢复 | 是 | 是 |
| Worker lease/fencing 恢复 | 是 | 是 |
| 重复审批/dispatch 幂等 | 是 | 是 |
| 前端与 K8s 实际状态一致 | 是 | 是 |

---

## 23. 推荐执行顺序

严格按以下顺序执行，不并行跳步：

```text
0. 修复 B1～B8
1. test-workflow-all + workflow gates
2. 删除旧 namespace/PVC/Secret
3. 创建 aiops-canary + canary Deployment
4. 用当前 Git SHA 构建全部自研镜像
5. Helm lint/template，检查 Secret、image、enabled、RBAC
6. 使用 bootstrap profile 执行第一阶段 Helm install
7. 等待 mysql-users-init
8. 等待 schema-migrator
9. 使用 local-validation profile 执行第二阶段 Helm upgrade，启动 Query API/Worker/Proxy/采集/前端
10. 核心 Pod readiness
11. 真实指标/日志/事件/DeepFlow 数据验证
12. 真实 LLM Proxy 验证
13. 前端只读 Investigation
14. Run/ToolRun/Evidence/RCA/SSE 验证
15. Action 生成 + 审批，Executor disabled
16. RBAC 检查
17. Executor approved + realMutation，仅 aiops-canary
18. annotation patch
19. TOCTOU
20. response lost + reconcile
21. Query API/Worker 重启恢复
22. 幂等性
23. 前端最终核对
24. 输出验收报告
```

---

## 24. 最终结论口径

本轮验证结束后只允许使用以下三种结论之一：

### 结论 A：AIOps 本机真实可用

仅当第 21、22 节所有强制项通过。

### 结论 B：平台可部署，AIOps 主链未完全验证

例如：

- 平台组件 Ready。
- 只读调查通过。
- 但真实 LLM、mutation、TOCTOU、reconcile、恢复中任一关键项未通过。

此时不得称为“AIOps 完全可用”。

### 结论 C：部署链未闭环

例如：

- 空库无法初始化。
- Secret 缺失。
- 镜像版本不一致。
- Worker/Proxy/Query API 无法 Ready。
- Helm fresh install 无法完成。

此时应先修部署封装，不进入功能验收。

---

## 25. 当前基线下的直接判断

基于当前 `main@b30e6da492eb78d4337fe75fc7199a4d6f0ae271`：

1. **不需要设计历史数据迁移方案。**
2. **仍需要 schema-migrator，但只把它当作空库 schema 初始化器。**
3. AIOps 核心持久化工作流不建议继续大改。
4. 当前最优先事项是修复 B1～B8，把“当前代码”变成“可以从空环境稳定部署的同一套镜像”。
5. B1～B8 完成后，直接做一次完整 Fresh Install，比继续设计旧环境升级流程更有价值。
6. 本机真实 mutation 已经单独验证过 Executor→Kubernetes 的基本能力；本轮重点是把它放回完整 canonical 链中验证。
7. 最终是否“完全满足设计要求”，必须以 **前端→Query API→Worker→Evidence/RCA→Action/Approval→Executor→Kubernetes→Reconcile/Verification→SSE→前端** 的整链实测结果为准。
