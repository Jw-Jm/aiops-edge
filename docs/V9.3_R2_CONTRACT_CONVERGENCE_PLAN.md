# R2 合同收敛 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 收敛 P7/P8/P9 平行 dataclass 模型（tool_result.py / evidence_hub.py / planner.py / hypothesis.py / investigation_state.py）到权威合同（contracts.py + contract-fixtures.json），消除审计 B0-02 合同退化，按分模型原子切换。

**Architecture:** 两阶段。**第一阶段（当前 In-memory 环境可执行）**：Python 侧平行模型收敛到 `contracts.py` 权威 pydantic binding + 短期 ACL 适配 + 分模型原子切换，TDD 验证，业务组件组合/引用权威实体。**第二阶段（需工具链/真实环境）**：从 versioned Contract Bundle（schema + invariants + conformance vectors + manifest）生成三端 binding，补齐 Go strict 模型（当前 RawMessage）、TS binding，deterministic regeneration Gate。

**Tech Stack:** Python 3.9 / pydantic（ContractModel）/ pytest / 现有 tool_result.py / evidence_hub.py / planner.py / hypothesis.py / contracts.py

## Global Constraints

- 不破坏 V1 wire contract：`success:bool + status` 联合语义、已签名 Context（TrustedRequestContext/RunContext）、OpsAction/Verification 冻结。
- V2 只加不改（新增字段带默认值 / 新增枚举值不改变既有序列化值）。
- 冻结红线 F1-F5：LLM 非权威、三+一身份、Evidence 边界、Planner 唯一、不触发真实执行。
- Agent ≠ Execution 隔离（grep execute|credential|kubeconfig|adapter → 0 match）。
- 删除 Gate 只删重复合同类/导出，**不删承载业务逻辑的模块**（Store/Generator/Planner）。
- ACL 每模型原子切换后立即删除，禁止长期双主。
- 内部状态组合/引用权威实体，禁止复制权威字段到端内 dataclass。
- Execution Production Execution = NOT YET APPROVED。

---

# 第一阶段：Python 侧平行模型收敛（当前环境可 TDD）

## Task 0: 建立 Python 合同 binding 目录与 Contract Bundle 骨架

**Files:**
- Create: `ai-orchestrator/contracts/__init__.py`
- Create: `ai-orchestrator/contracts/models.py`（权威 pydantic binding，从 contracts.py 复用或独立）
- Create: `docs/contracts/bundle/v2/invariants.md`
- Create: `docs/contracts/bundle/v2/conformance-vectors.json`
- Create: `docs/contracts/bundle/v2/manifest.json`
- Test: `ai-orchestrator/tests/test_r2_bundle_manifest.py`

**Interfaces:**
- Consumes: `contracts.py` 的 `ContractModel`、`EvidenceType`、`ClaimType`、`ToolStatus`、`PlanStepStatus`、`HypothesisStatus`。
- Produces: `contracts.models.ToolResultContract / EvidenceContract / HypothesisContract / PlanStepContract / PlannerStateContract`（权威 binding）+ `manifest.json`（版本/checksum/工具参数）。

- [ ] **Step 1: 写失败测试**（bundle 骨架存在 + manifest 可验证）

```python
# tests/test_r2_bundle_manifest.py
def test_bundle_manifest_exists_and_verifies():
    import json, os
    from pathlib import Path
    root = Path(__file__).resolve().parents[2]  # aiops/
    manifest_path = root / "docs/contracts/bundle/v2/manifest.json"
    assert manifest_path.exists()
    m = json.loads(manifest_path.read_text())
    assert "bundle_version" in m
    assert "tools" in m  # 每端工具版本
    assert "checksum" in m
    # 三端工具链固定
    assert m["tools"]["python"]["name"] == "datamodel-codegen"
    assert m["tools"]["go"]["name"] == "go-generate"
    assert m["tools"]["ts"]["name"] == "quicktype"
```

- [ ] **Step 2: 运行确认失败**（manifest 不存在）
- [ ] **Step 3: 创建 `contracts/__init__.py` + `contracts/models.py` 空骨架 + manifest/invariants/vectors**
- [ ] **Step 4: 运行确认通过**
- [ ] **Step 5: Commit**

---

## Task 1: ToolResult 收敛

**Files:**
- Create: `ai-orchestrator/contracts/tool_result.py`（权威 ToolResult binding）
- Create: `ai-orchestrator/acl/tool_result_ac.py`（临时 ACL：权威↔平行互转）
- Modify: `ai-orchestrator/tool_result.py`（ToolResult 类改 import 权威 binding，保留 normalize 业务函数）
- Test: `ai-orchestrator/tests/test_r2_tool_result.py`

**Interfaces:**
- Consumes: `contracts.py.ToolStatus`、`contracts.py.ToolResult`（V1 wire 保留 success+status）。
- Produces: `tool_result.py.normalize_tool_result` 返回权威 `ToolResultContract`；`acl.tool_result_ac.to_contract(parallel)/from_contract(contract)`。

**裁决（v0.2 矩阵）：** `success` 保留(V1 wire)+派生；`status` 7 态保留；`tenant_id/cluster_id` 保留；`source_system` 兼容映射（非法拒绝）。

- [ ] **Step 1: 写失败测试**（权威 ToolResult 字段 + ACL 无损互转 + 非法 source 拒绝）

```python
# tests/test_r2_tool_result.py
def test_tool_result_contract_keeps_v1_success_status():
    from contracts.tool_result import ToolResultContract
    tr = ToolResultContract(
        tool_name="query_logs", cluster_id="c1", success=True, status="success",
        summary="ok", tenant_id="t1", source_system="VLogs",
    )
    assert tr.success is True
    assert tr.status == "success"

def test_acl_roundtrip_preserves_fields():
    from acl.tool_result_ac import to_contract, from_contract
    # 平行 → 权威 → 平行 无损
    ...

def test_acl_rejects_illegal_llm_source():
    import pytest
    from acl.tool_result_ac import to_contract
    with pytest.raises(ValueError):
        to_contract(source_system="LLM")
```

- [ ] **Step 2: 运行确认失败**
- [ ] **Step 3: 实现 `contracts/tool_result.py` + `acl/tool_result_ac.py`，tool_result.py 迁移到权威 binding**
- [ ] **Step 4: 运行既有 test_p73 + 新 test_r2 确认全绿**
- [ ] **Step 5: 删除 `acl/tool_result_ac.py`（Task 完成即删 ACL），Commit**

---

## Task 2: Evidence 收敛（fingerprint invariant + LifecycleState 拆分）

**Files:**
- Create: `ai-orchestrator/contracts/evidence.py`（权威 Evidence + EvidenceLifecycleState）
- Create: `ai-orchestrator/acl/evidence_ac.py`
- Modify: `ai-orchestrator/evidence_hub.py`（Evidence 类改 import 权威 binding；Store 组合权威 Evidence + 单独 LifecycleState）
- Modify: `ai-orchestrator/evidence_hub.py`（fingerprint 算法固化进 invariant，含 tenant/cluster/run）
- Test: `ai-orchestrator/tests/test_r2_evidence.py`

**Interfaces:**
- Consumes: `contracts.py.EvidenceType/ClaimType`；现有 `_fingerprint(source, query_id, resource_id, time_range, digest, tenant_id, cluster_id, run_id)`。
- Produces: `contracts.evidence.EvidenceContract`（不可变）+ `EvidenceLifecycleState`；`evidence_hub.save_from_tool_result` 返回权威 EvidenceContract。

**裁决（v0.2）：** run/tenant/cluster UUID 保留；provenance_fingerprint 算法固化（含隔离维度）；Evidence 不可变 + LifecycleState 按 evidence_id 拆分。

- [ ] **Step 1: 写失败测试**（Evidence 不可变 + fingerprint 含隔离维度 + LifecycleState 独立管理）

```python
# tests/test_r2_evidence.py
def test_evidence_contract_immutable():
    from contracts.evidence import EvidenceContract
    ev = EvidenceContract(evidence_id="e1", run_id="r1", tenant_id="t1", cluster_id="c1",
                          evidence_type="metric_anomaly", claim_type="fact", source="VM",
                          source_reliability=0.95, fact="spike",
                          provenance_fingerprint="fp-t1-c1-r1")
    with pytest.raises(Exception):
        ev.fact = "changed"  # 不可变

def test_lifecycle_state_separate():
    from contracts.evidence import EvidenceLifecycleState
    ls = EvidenceLifecycleState(evidence_id="e1", status="created")
    ls2 = ls.transition("validated")
    assert ls2.status == "validated"
    # 状态不进入权威 Evidence 字段

def test_fingerprint_includes_tenant_cluster_run():
    from evidence_hub import _fingerprint
    fp1 = _fingerprint("VM","q1","svc","t","d","tenant-a","cluster-1","run-1")
    fp2 = _fingerprint("VM","q1","svc","t","d","tenant-b","cluster-1","run-1")
    assert fp1 != fp2  # 隔离维度生效
```

- [ ] **Step 2: 运行确认失败**
- [ ] **Step 3: 实现 `contracts/evidence.py` + `acl/evidence_ac.py`；evidence_hub 迁移到权威 Evidence + 单独 LifecycleState；fingerprint 固化**
- [ ] **Step 4: 运行既有 test_p74 + test_p0_evidence_isolation + 新 test_r2 确认全绿**
- [ ] **Step 5: 删除 `acl/evidence_ac.py`，Commit**

---

## Task 3: PlanStep/PlannerState 收敛（预算固化）

**Files:**
- Create: `ai-orchestrator/contracts/plan.py`（权威 PlanStep + PlannerState + Budget）
- Create: `ai-orchestrator/acl/plan_ac.py`
- Modify: `ai-orchestrator/planner.py`（PlanStep 改 import 权威 binding；Planner 持有权威 PlanStep[] + 内部计数状态）
- Test: `ai-orchestrator/tests/test_r2_plan.py`

**Interfaces:**
- Consumes: `contracts.py.PlanStep`（id/run_id/agent/action/parameters/required/cluster_id/status）、`PlanStepStatus`。
- Produces: `contracts.plan.PlanStepContract / PlannerStateContract / BudgetContract`（max_steps/max_tools/max_latency 固化）。

**裁决（v0.2）：** planner.step_id→id（重命名）；planner.tool_id→action（兼容映射）；params→parameters；result_ref→PlannerState 内部（C 类不入 wire）；预算固化。

- [ ] **Step 1: 写失败测试**（PlanStep 权威字段 + 预算固化 + tool_id→action 映射）

```python
# tests/test_r2_plan.py
def test_plan_step_uses_authoritative_fields():
    from contracts.plan import PlanStepContract
    step = PlanStepContract(id="s1", run_id="r1", agent="log_agent", action="query_logs",
                            parameters={"service":"checkout"}, required=True,
                            cluster_id="c1", status="pending")
    assert step.id == "s1"
    assert step.action == "query_logs"

def test_planner_state_budget_frozen():
    from contracts.plan import BudgetContract
    b = BudgetContract(max_steps=10, max_tools=20, max_latency=60000)
    assert b.max_steps == 10

def test_tool_id_maps_to_action():
    from acl.plan_ac import tool_id_to_action
    assert tool_id_to_action("query_logs.v1") == "query_logs"
```

- [ ] **Step 2: 运行确认失败**
- [ ] **Step 3: 实现 `contracts/plan.py` + `acl/plan_ac.py`；planner.py 迁移到权威 PlanStep + Budget**
- [ ] **Step 4: 运行既有 test_p76 + 新 test_r2 确认全绿**
- [ ] **Step 5: 删除 `acl/plan_ac.py`，Commit**

---

## Task 4: Hypothesis/RcaResult 收敛（强隔离 + 类型定义）

**Files:**
- Create: `ai-orchestrator/contracts/hypothesis.py`（权威 Hypothesis + HypothesisEvaluation/ScoreBreakdown/Contradiction/MissingEvidence/RootCauseRef）
- Create: `ai-orchestrator/acl/hypothesis_ac.py`
- Modify: `ai-orchestrator/hypothesis.py`（Hypothesis 改 import 权威 binding）
- Modify: `ai-orchestrator/rca_engine.py`（RcaResult 使用权威 RootCauseRef/HypothesisEvaluation）
- Test: `ai-orchestrator/tests/test_r2_hypothesis.py`

**Interfaces:**
- Consumes: `contracts.py.Hypothesis`（id/run_id/title/description/supporting/contradicting/missing/confidence/status）+ 强隔离 tenant/cluster/resource（V2 新增）。
- Produces: `contracts.hypothesis.HypothesisContract`（+tenant/cluster/resource）+ `ScoreBreakdown/Contradiction/MissingEvidence/RootCauseRef`。

**裁决（v0.2）：** title↔claim 别名；description↔mechanism；supporting↔required_support；增加 tenant/cluster/resource 强隔离；confidence=Semantic Invariant 计算。

- [ ] **Step 1: 写失败测试**（Hypothesis 强隔离 + ScoreBreakdown/RootCauseRef 类型 + title/claim 映射）

```python
# tests/test_r2_hypothesis.py
def test_hypothesis_requires_isolation_fields():
    from contracts.hypothesis import HypothesisContract
    with pytest.raises(Exception):
        HypothesisContract(id="h1", run_id="r1", title="t", description="d",
                           confidence=0.5, status="candidate")  # 缺 tenant/cluster
    h = HypothesisContract(id="h1", run_id="r1", title="t", description="d",
                           tenant_id="t1", cluster_id="c1", resource_id="c1/svc",
                           confidence=0.5, status="candidate")
    assert h.cluster_id == "c1"

def test_rca_result_types_defined():
    from contracts.hypothesis import ScoreBreakdown, RootCauseRef, Contradiction
    sb = ScoreBreakdown(base_score=0.8, components={}, penalties=[], final_score=0.8)
    ct = Contradiction(type="time_conflict", severity="critical", evidence_ref="e1", resolved=False)
    rc = RootCauseRef(root_cause="h1", confidence_state="confirmed", ranked=[])
    assert rc.confidence_state == "confirmed"

def test_title_claim_mapping():
    from acl.hypothesis_ac import claim_to_title, title_to_claim
    assert claim_to_title("变更发布导致错误率上升").startswith("变更发布")
```

- [ ] **Step 2: 运行确认失败**
- [ ] **Step 3: 实现 `contracts/hypothesis.py` + `acl/hypothesis_ac.py`；hypothesis.py/rca_engine.py 迁移**
- [ ] **Step 4: 运行既有 test_p92/test_p9_rca_engine + 新 test_r2 确认全绿**
- [ ] **Step 5: 删除 `acl/hypothesis_ac.py`，Commit**

---

## Task 5: 删除重复类 + 全仓禁重复合同 + 收敛完成 Gate

**Files:**
- Modify: 删除已迁移模型的重复 dataclass 声明（tool_result.py 的 ToolResult 类、evidence_hub.py 的 Evidence 类、planner.py 的 PlanStep 类、hypothesis.py 的 Hypothesis 类——**保留业务函数**：normalize_tool_result / save_from_tool_result / propose_plan / generate）
- Create: `ai-orchestrator/scripts/check_no_duplicate_contracts.py`（CI 扫描禁止重复同名合同定义）
- Test: `ai-orchestrator/tests/test_r2_cleanup_gate.py`

**Interfaces:**
- Consumes: 各收敛后模块的权威 binding。
- Produces: `check_no_duplicate_contracts.py` 返回 0（无重复）或非 0（有重复）。

- [ ] **Step 1: 写失败测试**（重复合同检测器 + 业务函数保留）

```python
# tests/test_r2_cleanup_gate.py
def test_no_duplicate_tool_result_class():
    import tool_result, contracts.tool_result
    # tool_result 不再声明自己的 ToolResult 类（import 权威）
    assert not hasattr(tool_result, "ToolResult") or \
           tool_result.ToolResult is contracts.tool_result.ToolResultContract

def test_business_function_retained():
    import tool_result, evidence_hub, planner, hypothesis
    assert hasattr(tool_result, "normalize_tool_result")  # 业务函数保留
    assert hasattr(planner, "Planner")
    assert hasattr(evidence_hub, "EvidenceHub")
    assert hasattr(hypothesis, "HypothesisGenerator")

def test_duplicate_scanner_detects_dup():
    from scripts.check_no_duplicate_contracts import has_duplicate
    assert has_duplicate("class ToolResult:") is True  # 示例模式
```

- [ ] **Step 2: 运行确认失败**
- [ ] **Step 3: 删除重复类声明 + 实现 `scripts/check_no_duplicate_contracts.py`**
- [ ] **Step 4: 运行全部 P7-P9 + P0/R1/R2 测试确认全绿（无回归）**
- [ ] **Step 5: 全仓 grep 确认无重复合同类，Commit**

---

# 第二阶段：三端 Contract Bundle 生成与 Go/TS 严格模型（需工具链/真实环境）

> 本阶段需：datamodel-codegen（Python）、go generate（Go）、quicktype（TS）工具链就绪 + Go/TS 真实仓库 + 真实环境。当前 In-memory 会话无法执行，作为 R2 跨语言验证的后续 Gate。

## Task 6: 从 Bundle 生成三端 binding + Go 严格模型

- [ ] **Step 1: 固定工具链**（datamodel-codegen/quicktype/go-generate 确切版本/参数/输出目录 → manifest.json）
- [ ] **Step 2: 生成 Python binding**（`contracts/` 从 bundle schema 生成）+ 三端 round-trip
- [ ] **Step 3: 补齐 Go 严格模型**（`internal/contract/` 从 json.RawMessage → 类型化模型 + Semantic Invariants 校验）
- [ ] **Step 4: 生成 TS binding**（observability-frontend）
- [ ] **Step 5: 三端对同一 conformance-vectors.json 给出一致结果（含 invalid vector）**
- [ ] **Step 6: Deterministic regeneration Gate**（重新生成后 git diff 零 diff）

## Task 7: R2 最终验收 Gate

- [ ] V1 wire contract 冻结保持（success/status、Context 签名不变）
- [ ] V1→V2 升级 + V2→V1 拒绝/降级规则通过
- [ ] 全仓禁止重复同名合同（CI 扫描 0 违规）
- [ ] ACL 全部删除，业务组件组合/引用权威实体
- [ ] contracts.py 成为纯 Python binding
- [ ] 三端全量测试通过，跨租户/集群/LLM source 负测全绿

---

## Self-Review

**Spec coverage:** 设计 v0.2 的字段裁决矩阵（Task 1-4）、Contract Bundle（Task 0）、V1/V2 兼容（Task 1 保留 success）、工具链固定（Task 6）、内部状态组合（Task 2/3/5）、调用方影响（各 Task 迁移消费方）、删除 Gate（Task 5/7）全部覆盖。

**Placeholder scan:** 无 TBD/TODO；各 Task 含失败测试 + 实现步骤。

**Type consistency:** `ToolResultContract/EvidenceContract/PlanStepContract/HypothesisContract/ScoreBreakdown/RootCauseRef` 命名跨 Task 一致。
