# V9.3 Phase7 — P7.7 Implementation Evidence

```text
CONTRACT        = V9.3 DEEPSEEK EXECUTION R4 (Phase 7+ Manual-Triggered)
DESIGN          = V9.3_PHASE7_P77_STRUCTURED_STATE_DESIGN.md v0.1（APPROVED）
MODE            = In-memory MVP + TDD
SCOPE           = P7.7 ONLY
STATUS          = IMPLEMENTED（待用户评审）
GIT_ACTION      = NONE
DATE            = 2026-08-20
```

## 1. 修改文件清单

| 文件 | 类型 | 职责 |
|------|------|------|
| `ai-orchestrator/investigation_state.py` | 新增 | InvestigationState/StepState/Hypothesis + StateStore(create/get/restore/update_step/consume_budget/transition) |
| `ai-orchestrator/tests/test_p77_investigation_state.py` | 新增 | P7.7 TDD 测试（T1-T4），9 用例 |

未修改任何既有文件。

## 2. Code Map

```text
run_id + intent_id + plan_id
   ↓ create / restore(snapshot)
InvestigationState（结构化，非 prompt 历史）
   ├─ phase: IntentCreated→EvidenceCollected→PlanGenerated→AwaitingApproval
   ├─ steps[StepState]: pending/running/completed/skipped/failed
   ├─ tool_result_refs / evidence_refs / missing_evidence
   ├─ budget: consumed_steps/tools + remaining（consume_budget 超限拒绝）
   ├─ findings / hypotheses（禁止执行指令）
   └─ to_dict()/from_dict() → 结构化恢复
```

## 3. TDD RED → GREEN

```text
RED   : ModuleNotFoundError: investigation_state（功能缺失）
GREEN : 实现 investigation_state.py → 9 passed
回归   : P7.7(9) + P7.6(14) + P7.5(18) + P7.4(13) + P7.3(26) = 80 passed，无 lint
```

## 4. Test 结果（T1-T4）

- **T1 状态维护**：create/add_step/update_step（pending→completed+result_ref）；missing_evidence 记录 PASS
- **T2 Budget 追踪**：consume_budget 更新 consumed/remaining；超 max_steps → ValueError PASS
- **T3 状态恢复**：to_dict→restore 从结构化快照恢复（非 prompt 历史）；无 incidents/detections 字段 PASS
- **T4 边界**：findings 含执行指令（kubectl delete）→ 拒绝；状态更新无外部 side effect PASS

## 5. Boundary Verification

- ❌ 无真实持久化 / 无 DB schema / 无 Agent execution
- P7.7：禁止仅靠 prompt 历史恢复状态；禁止新增 Incident/Detection 状态机
- findings/hypotheses 只记录调查结果，不含执行指令
- 状态更新不影响外部资源

## 6. Known Tradeoff

- `budget` 用 dict（consumed/remaining）；真实预算模型与 P7.6 Budget 对齐在后续整合。
- `restore` 接收 `to_dict()` 快照；真实 DB 持久化（MySQL）属后续阶段。
- `_EXECUTION_KEYWORDS` 关键词过滤执行指令（findings/hypotheses）；更严格语义校验属 P7.8/P7.10。
