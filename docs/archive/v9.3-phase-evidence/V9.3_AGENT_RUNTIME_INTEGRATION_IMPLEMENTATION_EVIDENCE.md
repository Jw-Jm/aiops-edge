# V9.3 Agent Runtime Integration — Implementation Evidence

```text
CONTRACT        = V9.3 DEEPSEEK EXECUTION R4（Agent Runtime Integration）
CONTRACT_BASE   = V9.3_AGENT_RUNTIME_INTEGRATION_DESIGN.md（ARI.1-ARI.5，PASS）
MODE            = In-memory MVP + TDD
SCOPE           = ARI.1-ARI.5 + N3（AgentInsight 协议冻结）
STATUS          = IMPLEMENTED（待用户评审）
GIT_ACTION      = NONE
DATE            = 2026-08-21
```

## 1. 修改文件清单

新增 3 个实现 + 4 个测试；增强 1 个 Phase 8 文件（agents.py 加 to_insight）。未修改已提交既有文件。

| 组件 | 实现文件 | 测试文件 | 测试 |
|------|---------|---------|------|
| ARI.4 AgentInsight 协议 | `agent_insight.py`（新增） | `test_ari4_agent_insight.py` | 5 |
| ARI.1/2 RealToolExecutor | `agent_runtime_integration.py`（新增） | `test_ari12_tool_executor.py` | 6 |
| ARI.3 Resource Graph provider | `resource_graph_provider.py`（新增） | `test_ari3_resource_graph_provider.py` | 4 |
| ARI.5 隔离自动化测试 | `agents.py`（增强 to_insight） | `test_ari5_isolation.py` | 4 |

## 2. Code Map

```text
Planner DAG → PlanStep → Agent.run（agents.py，返回 AgentOutput）
   ↓ AgentRuntimeFramework.execute_step
   ↓ ARI.1 RealToolExecutor（InternalQueryClient → query-api /internal/v1/query/*）
   ↓ ARI.2 TrustedRequestContext V2（tenant/cluster/capability + request_id/nonce 签名）
   ↓ normalize ToolResult → Evidence Hub
   ↓ ARI.4 to_insight() → AgentInsight（N3 协议，evidence_confidence）
   ↓ ARI.3 ResourceGraphProvider（经 query-api 采集 typed graph，隔离）
   ↓ ARI.5 隔离断言（Agent 无 execute/credential/kubeconfig）
```

## 3. TDD RED → GREEN

```text
ARI.4: ModuleNotFoundError: agent_insight → 实现 + agents.py to_insight → 5 passed（更新 P8.2 测试适配协议）
ARI.1/2: ModuleNotFoundError: agent_runtime_integration → 实现 → 6 passed
ARI.3: ModuleNotFoundError: resource_graph_provider → 实现 → 4 passed
ARI.5: 隔离断言 → 4 passed
全量回归: ARI(19) + P8(28) + P7(163) + EX(49) + PE(25) + 历史(54) = 338 passed，无 lint，无回归
```

## 4. Test 结果（ARI.1-ARI.5 + N3）

- **ARI.4**（5）：AgentInsight 协议字段；agent_type 校验；confidence 0-1（evidence_confidence 非根因概率）；missing_slots 结构；to_insight 统一输出
- **ARI.1/2**（6）：URL 恒为 /internal/v1/query/*；TrustedRequestContext 携带 tenant/cluster/capability + request_id/nonce；capability 精确（从 Tool）；未注册拒绝；operation 映射
- **ARI.3**（4）：经 query-api topology 端点；构建 typed graph；跨 cluster 不泄漏；无直连 informer/CMDB
- **ARI.5**（4）：Agent 无 execute/self_execute；无 credential/kubeconfig；源码 grep 无 self.execute；AgentInsight 无执行指令

## 5. 评审非阻塞建议纳入

- TrustedRequestContext V2 携带 request_id/nonce（防重放/capability 篡改）→ 已验证（ARI.1/2 T2）
- AgentInsight confidence 语义明确（evidence_confidence，非根因概率）→ agent_insight.py
- Execution Boundary 回归（grep self.execute）→ ARI.5 T3

## 6. 隔离边界（Agent ≠ Execution）

```text
Agent: Evidence collection + Insight generation（ARI.1-ARI.4）
   ≠
Execution: Approved Action Execution（Trusted Execution Foundation，隔离）
Agent 无 execute/self_execute/credential/kubeconfig
Agent 不触碰 ExecutionContract/Adapter/Credential Broker
```

## 7. 红线验证

```text
F1 LLM 非权威：capability 从 Tool 读，Agent 不自选
F2 三+一身份：TrustedRequestContext 携带 tenant/cluster/capability
F3 Evidence 边界：LLM inference 不进入 Evidence；Insight ≠ Evidence
F4 Planner 控制流程：Agent 不决定调查顺序
F5 不触发真实执行：本阶段无真实 query-api 接入/无执行副作用
```

## 8. Boundary Verification

- ❌ 无真实 query-api 接入（RealToolExecutor 用注入 http mock）
- ❌ 无真实 K8s/OpenStack/informer/CMDB
- ❌ 无真实 credential / 无生产变更动作
- ❌ 无 Deployment / Schema Migration / Agent execution
- ✅ 仅 In-memory MVP + TDD（不连真实系统）

## 9. GIT_ACTION

```text
GIT_ACTION = NONE（未 git commit；新增 3 实现 + 4 测试，增强 agents.py）
```

## 10. Known Tradeoff

- RealToolExecutor 的 InternalQueryClient 用注入 http（真实 query-api 接入属后续）。
- AgentInsight confidence 用简单规则（有证据 0.8 / 缺证据 0.3）；真实置信度评分属后续。
- ResourceGraphProvider 从 query-api 返回的静态 topology 构建；真实拓扑采集属后续。
- P8.2 测试适配了 AgentInsight 协议（analyze→insights）；语义（no_data/unavailable 区分）保留。
