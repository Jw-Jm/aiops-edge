# V9.3 Phase 8（七类 Agent + Resource Graph）— Implementation Evidence

```text
CONTRACT        = V9.3 DEEPSEEK EXECUTION R4（正式 Phase 8：P8.1-P8.10）
BASELINE        = V9.2_BASELINE_AFTER_GATE6 + Phase 7（P7.1-P7.10 COMPLETE）
MODE            = In-memory MVP + TDD
SCOPE           = Phase 8 七类 Agent + Resource Graph V1
STATUS          = IMPLEMENTED（待用户评审）
GIT_ACTION      = NONE
DATE            = 2026-08-21
```

## 1. 修改文件清单

新增 3 个实现 + 4 个测试文件。未修改已提交既有文件（复用 P7.1 Tool Registry / P7.3 ToolResult / P7.4 Evidence Hub / P7.2 InternalQueryClient 契约）。

| P8.x | 实现文件 | 测试文件 | 测试 |
|------|---------|---------|------|
| P8.1 Agent Runtime Framework | `agent_runtime.py` | `test_p81_agent_runtime.py` | 6 |
| P8.2-P8.8 七类 Agent | `agents.py` | `test_p82_agents.py` | 7 |
| P8.9 Resource Graph V1 | `resource_graph.py` | `test_p89_resource_graph.py` | 4 |
| P8.10 Agent Contract Tests | （横切） | `test_p810_agent_contract_tests.py` | 7 |

## 2. Code Map

```text
Planner DAG（P7.6）→ 单个 PlanStep
   ↓
AgentRuntimeFramework.execute_step（P8.1）
   ├─ validate scope（Tool 注册/active）+ budget
   ├─ select registered Tool → query-api（无 direct DB/K8s）
   ├─ normalize ToolResult（P7.3）
   ├─ Evidence Hub（P7.4，success/partial 落库）
   └─ MissingEvidence（no_data/unavailable → slot，不伪装）
   ↓
7 类 Agent（P8.2-8.8）统一契约
   ├─ Observability(metrics) / Log(logs) / Trace(traces)
   ├─ Kubernetes(k8s) / Change(changes) / Knowledge(knowledge) / Infrastructure(topology)
   └─ analyze：领域 insight（不归因）
   ↓
Resource Graph V1（P8.9）：typed node/edge + tenant 过滤 + 禁跨 cluster
```

## 3. TDD RED → GREEN

```text
P8.1: ModuleNotFoundError: agent_runtime → 实现 → 6 passed
P8.2-8.8: ModuleNotFoundError: agents → 实现 → 7 passed
P8.9: ModuleNotFoundError: resource_graph → 实现 → 4 passed
P8.10: 实现后契约断言 → 7 passed
回归: Phase8(24) + Phase7(163) = 187 passed，无回归，无 lint
```

## 4. Test 结果（P8.1-P8.10）

- **P8.1**（6）：统一执行 PlanStep → ToolResult+Evidence；未注册 Tool 拒绝；budget 超限拒绝；no_data/unavailable → MissingEvidence；无第二状态机/无 root cause
- **P8.2-8.8**（7）：每类 Agent 选对 Tool/capability；success→Evidence；no_data/unavailable 严格区分；Observability first abnormal timestamp；Change timeline 不默认根因
- **P8.9**（4）：typed node/edge；跨 cluster 不泄漏；禁跨 cluster edge；错误 tenant → TraversalDenied
- **P8.10**（7，Gate 8）：统一 runtime contract；无 direct DB/K8s client；unavailable≠no_data/healthy；permission_denied 非 missing；timeout 区分；budget exhaustion；无 autonomy/governance 子系统

## 5. Gate 8 断言验证

| 断言 | 状态 |
|------|------|
| 所有 7 类 Agent 用同一 runtime contract | PASS |
| 无 direct DB/K8s client | PASS |
| Log Agent 仅 Run 内按 DAG 自动参与（Agent 无自触发） | PASS |
| source unavailable != no_data/healthy | PASS |
| Graph traversal 不泄漏 cross-cluster nodes | PASS |
| 无 edge autonomy/governance 子系统 | PASS |

## 6. 红线验证

```text
Agent 禁止 final root cause（AgentOutput 无 root_cause）
Agent 禁止 direct execution（无 execute/self_execute）
Agent 禁止 direct DB / direct K8s client（无 kubectl/db_client）
Agent 只经 Tool Registry → query-api（P7.2 唯一事实路径）
```

## 7. Boundary Verification

- ❌ 无真实 query-api 调用（tool_executor 为内存模拟）
- ❌ 无 direct K8s/OpenStack/DB/SSH
- ❌ 无 Deployment / Schema Migration / Agent execution
- ✅ 仅 In-memory MVP + TDD

## 8. GIT_ACTION

```text
GIT_ACTION = NONE（未 git commit；新增 3 实现 + 4 测试）
```

## 9. Known Tradeoff

- tool_executor 为内存模拟（真实 query-api 调用属后续集成 Phase）。
- 各 Agent analyze 只做基础 insight 提取；完整领域逻辑（RED 基线对比、trace dependency 深度）属后续。
- Resource Graph 为内存 typed graph；真实资源注册/拓扑采集属后续。
- `agents.py` 为本次新增（git diff 显示因 untracked），未覆盖已提交文件。
