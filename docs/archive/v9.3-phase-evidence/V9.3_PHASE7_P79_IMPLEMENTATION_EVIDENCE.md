# V9.3 Phase7 — P7.9 Implementation Evidence

```text
CONTRACT        = V9.3 DEEPSEEK EXECUTION R4 (Phase 7+ Manual-Triggered)
DESIGN          = V9.3_PHASE7_P79_DATA_SOURCE_MAPPING_DESIGN.md v0.1（APPROVED）
MODE            = In-memory MVP + TDD
SCOPE           = P7.9 ONLY
STATUS          = IMPLEMENTED（待用户评审）
GIT_ACTION      = NONE
DATE            = 2026-08-20
```

## 1. 修改文件清单

| 文件 | 类型 | 职责 |
|------|------|------|
| `ai-orchestrator/data_source_mapping.py` | 新增 | DataSourceMapping(map_tool/map_capability/resolve_cluster/assert_valid_config/classify_model/assert_not_blocked) + ClusterFailClosed + ArchitectureDeviation |
| `ai-orchestrator/tests/test_p79_data_source_mapping.py` | 新增 | P7.9 TDD 测试（T1-T4），12 用例 |

未修改任何既有文件。

## 2. Code Map

```text
Tool（P7.1）→ map_tool() → VM / VLogs / query-api
capability  → map_capability() → VM / VLogs / query-api
Cluster     → resolve_cluster()（registered_canonical）→ 已注册返回 / 未知 fail-closed
config      → assert_valid_config(credential_ref, cluster_registered) → 缺配置 fail-closed
模型充足性  → classify_model(needs_new_sot, missing_mandatory_fields) → reuse|minimal_extension|blocked
           → assert_not_blocked(needs_new_sot) → 需新 SoT → BLOCKED + Architecture Deviation
```

复用 V9.2 既有映射（Cluster Registry / credential_ref / canonical cluster），不新建 source 身份/授权体系。

## 3. TDD RED → GREEN

```text
RED   : ModuleNotFoundError: data_source_mapping（功能缺失）
GREEN : 实现 data_source_mapping.py → 12 passed
全量回归: P7.9(12)+P7.8(10)+P7.7(9)+P7.6(14)+P7.5(18)+P7.4(13)+P7.3(26)+P7.2(20)+P7.1(26)
         = 148 passed，无 lint
```

## 4. Test 结果（T1-T4）

- **T1 映射正确**：query_metrics.v1→VM、query_logs.v1→VLogs；observability.metrics.read→VM、logs.read→VLogs；已注册外部 cluster resolve 成功 PASS
- **T2 语义一致**：所有 source 来自同一枚举体系（VM/VLogs/query-api），不新增 source identity；外部平台不可达沿用现有 unavailable 语义 PASS
- **T3 fail-closed**：未知 cluster → ClusterFailClosed；无有效 credential → ClusterFailClosed；错误 canonical → 拒绝 PASS
- **T4 模型充足性**：现有模型足够→reuse；缺 1 强制字段→minimal_extension；需新 SoT→BLOCKED + ArchitectureDeviation PASS

## 5. Boundary Verification

- ❌ 无真实平台连接 / 无新增 SoT / 无 DB migration / Deployment
- P7.9：复用 V9.2 既有映射（Cluster Registry/tenant_clusters/credential_ref/canonical cluster）；无新 source 身份/授权体系
- 不可达 → unavailable；未注册 → fail-closed；需新 SoT → 明确 BLOCKED

## 6. Known Tradeoff

- `registered_clusters` 用内存集合（真实 Cluster Registry 查询属后续接入）。
- `TOOL_TO_SOURCE` / `CAPABILITY_TO_SOURCE` 为静态映射；真实 platform-data-source config 动态解析属后续。
- `map_tool` 未知 tool fail-closed 到 query-api（不新建 source identity）；P7.10 可加白名单强校验。
