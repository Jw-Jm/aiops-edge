# V9.3 Phase7 — P7.10 Implementation Evidence

```text
CONTRACT        = V9.3 DEEPSEEK EXECUTION R4 (Phase 7+ Manual-Triggered)
DESIGN          = V9.3_PHASE7_P710_SECURITY_DESIGN.md v0.1（APPROVED）
MODE            = In-memory MVP + TDD
SCOPE           = P7.10 ONLY（Security / Negative Tests）
STATUS          = IMPLEMENTED（待用户评审）
GIT_ACTION      = NONE
DATE            = 2026-08-20
```

## 1. 修改文件清单

| 文件 | 类型 | 职责 |
|------|------|------|
| `ai-orchestrator/security_gate.py` | 新增 | SecurityGate（10 项 deny_* fail-closed 断言 + Registry Snapshot Hash）+ SecurityViolation |
| `ai-orchestrator/tests/test_p710_security_gate.py` | 新增 | P7.10 TDD 测试（T1-T5），15 用例 |

未修改任何既有文件（横切聚合 P7.1-P7.9 组件，不改其实现）。

## 2. Code Map

```text
SecurityGate（聚合 P7.1-P7.9）
├─ T1 越权拒绝:
│   deny_unregistered_tool / deny_risk_downgrade / deny_readonly_change / deny_wrong_capability
├─ T2 Cross-Cluster / Scope:
│   deny_cross_cluster / deny_unknown_cluster / deny_wrong_mapping
├─ T3 Budget / 语义:
│   deny_budget_exceeded / deny_ambiguous_target
├─ T4 后端语义（复用 P7.3 normalize，验证 403→permission_denied、503→unavailable 不降级）
└─ T5 Registry Snapshot Hash:
    snapshot_registry(version, tools[], hash) + assert_registry_integrity（防运行期 Tool 偷换）
```

## 3. TDD RED → GREEN

```text
RED   : ModuleNotFoundError: security_gate（功能缺失）
GREEN : 实现 security_gate.py → 12 passed
强化   : 补 Registry Snapshot Hash（P7.10 非阻塞事项 Deferred → 纳入）
         补 3 用例（snapshot/untampered/tampered）→ 修 ToolDefinition 缺 description → 15 passed
全量回归: P7.1-P7.10 = 163 passed，无 lint
```

## 4. Test 结果（T1-T5 + 10 项必列 negative tests）

| Negative Test（设计 §2） | 断言 | 状态 |
|---|---|---|
| unregistered Tool | `deny_unregistered_tool("evil_tool.v1")` → SECURITY_VIOLATION | PASS |
| LLM 修改 risk | `deny_risk_downgrade(k8sgpt_diagnose.v1, R0)` → RISK_DOWNGRADE_DENIED | PASS |
| LLM 修改 read_only | `deny_readonly_change(query_logs.v1)` → READONLY_IMMUTABLE | PASS |
| wrong capability | `deny_wrong_capability(query_logs.v1, metrics)` → CAPABILITY_MISMATCH | PASS |
| cross-cluster Tool | `deny_cross_cluster(OTHER, CLUSTER)` → CROSS_CLUSTER_DENIED | PASS |
| unknown canonical cluster | `deny_unknown_cluster("unknown")` → CLUSTER_FAIL_CLOSED | PASS |
| mapping 指向错误 canonical | `deny_wrong_mapping(OTHER)` → CLUSTER_FAIL_CLOSED | PASS |
| budget exceeded | `deny_budget_exceeded(plan)` → BUDGET_EXCEEDED | PASS |
| ambiguous target | `deny_ambiguous_target(service, None)` → RESOURCE_AMBIGUOUS | PASS |
| backend unavailable | 503 normalize → `unavailable`（不降级） | PASS |
| permission denied | 403 normalize → `permission_denied`（不降级 no_data） | PASS |

### Gate 7 追加断言（T5，§4236）
- **Registry Snapshot Hash**：`snapshot_registry()` → version/tools/hash；未篡改通过；运行期偷换（注册恶意 evil.v1）→ REGISTRY_TAMPERED PASS
- **status exact**：P7.3 7 态精确 PASS
- **budget exact**：P7.6 budget_exceeded 精确 PASS

## 5. Boundary Verification

- ❌ 无 Go 代码 / DB migration / Helm change / Deployment / Agent execution
- ❌ 无生产安全系统接入 / 无认证体系修改
- F1-F5 红线：Agent 不能扩展权限（risk/read_only immutable）、不能越 cluster、不能绕过 approval
- Registry Snapshot Hash 已纳入（防运行期 Tool 偷换 + Evidence/Plan 可引用 Registry 版本 + Security Gate 审计依据）

## 6. Known Tradeoff

- `deny_readonly_change` 为安全红线强断言（即使 P7.1 registry.update 允许改 read_only，Security Gate 也拒绝）；P7.1 可后续补 immutable 校验。
- Registry Snapshot 用内存快照 hash；真实持久化 + Evidence/Plan 引用 registry 版本属后续整合。
- `deny_wrong_mapping` 读取 `_registered_clusters`（私有）；公开 resolve 接口已 fail-closed，此断言为显式审计。
