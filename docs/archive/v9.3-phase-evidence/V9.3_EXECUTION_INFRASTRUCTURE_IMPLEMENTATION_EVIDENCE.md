# V9.3 Execution Infrastructure — Implementation Evidence

```text
CONTRACT        = V9.3 DEEPSEEK EXECUTION R4 (Phase 8+ Manual-Triggered)
CONTRACT_BASE   = V9.3_PHASE8_GATE_REVIEW.md（5 Schema v1，不可变）+ V9.3_EXECUTION_PHASE_DESIGN.md（EX.1-EX.6）
MODE            = In-memory MVP + TDD
SCOPE           = Execution Infrastructure（EX.1-EX.6 + R4.1/R4.2/R4.3）
STATUS          = IMPLEMENTED（待用户评审）
GIT_ACTION      = NONE
DATE            = 2026-08-20
```

## 1. 修改文件清单

新增 4 个实现文件 + 6 个测试文件；增强 2 个 Phase8 实现文件（保留既有测试）。**未修改任何已提交既有文件**。

| 组件 | 实现文件 | 测试文件 | 测试 |
|------|---------|---------|------|
| EX.1 Approval Signature | `approval_signature.py`（新增） | `test_ex1_approval_signature.py` | 7 |
| EX.2 Credential Broker | `credential_broker.py`（新增） | `test_ex2_credential_broker.py` | 6 |
| EX.3 Adapter 增强 | `execution_adapter.py`（增强，R4.1/R4.2/signature/broker） | `test_ex3_adapter_infra.py` | 5 |
| EX.4 Rollback Framework | `rollback_framework.py`（新增） | `test_ex4_rollback_framework.py` | 6 |
| EX.5 Preview 防漂移 | `execution_preview.py`（增强，R2） | `test_ex5_preview_drift.py` | 4 |
| EX.6 Policy SoT | `sot_provider.py`（新增） | `test_ex6_policy_sot.py` | 5 |

增强文件向后兼容（P8.3/P8.5 测试仍全绿）。

## 2. Code Map

```text
ExecutionContract(active)
   ↓ EX.1 ApprovalSignature（Ed25519，signer=approved_by）验签
   ↓ EX.6 Policy Context（MySQL SoT + query-api 只读）→ ALLOW
   ↓ EX.5 Preview 资源版本绑定（防漂移）→ approved
   ↓ EX.2 Credential Broker（short-lived + 最小权限 + R4.3 audit）
   ↓ EX.3 Adapter.execute（R4.1 idempotency + R4.2 permission snapshot + before/after state）
   ↓ success → executed | 失败 → EX.4 Rollback（rollback_contract_id + Human 批准）
```

## 3. TDD RED → GREEN

```text
EX.1: ModuleNotFoundError → 实现 → 7 passed
EX.2: ModuleNotFoundError → 实现 → 6 passed
EX.3: TypeError(broker 参数缺失) → 增强 → 5 passed（修 verifier 调用位置参数）
EX.4: ModuleNotFoundError → 实现 → 6 passed
EX.5: ImportError(PreviewDrift) → 增强 → 4 passed
EX.6: ModuleNotFoundError → 实现 → 5 passed
全量回归: EX(49) + P8(47) + P7(163) = 259 passed，无 lint，无回归
```

## 4. Test 结果（EX.1-EX.6 + R4.x）

- **EX.1**（7）：有效签名通过 / signer≠approved_by 拒绝 / 篡改拒绝 / 必有签名 / key_id 轮换
- **EX.2**（6）：short-lived / 随 contract expire / revoke 失效 / Broker 唯一接触点 / 最小权限 scope / **R4.3 audit**（who/when/contract/adapter/scope/expire）
- **EX.3**（5）：**R4.1 idempotency**（同 key 返回缓存）/ **R4.2 permission snapshot** / signature 集成 / broker 集成
- **EX.4**（6）：无 Human 批准拒绝 / 无 before_state 拒绝 / policy 检查 / **rollback_contract_id**（新动作）/ Agent 不能自动 rollback
- **EX.5**（4）：版本漂移拒绝 / cluster 漂移拒绝 / namespace 漂移拒绝 / 版本一致允许（防执行对象漂移）
- **EX.6**（5）：auth 从 MySQL SoT / cluster 经 query-api / 无旁路 / LLM/Agent 仍拒绝

## 5. 红线验证（F1-F5 + Execution Infrastructure）

```text
F1 LLM 非权威：signature 由 Human 签发（signer=approved_by）；Policy/credential 禁 LLM
F2 三+一身份分离：approved_by ≠ requested_by ≠ executed_by
F3 Evidence 边界：credential 经 Broker，Secret 不落 Evidence
F4 Planner 边界：Planner 不直连执行；真实执行经 Contract+Adapter
F5 End State：本阶段不触发真实执行（无真实系统连接）
新增：无 Approval Signature 不执行；凭据经 Broker；Rollback 须 Human 批准；资源版本防漂移
```

## 6. Boundary Verification

- ❌ 无真实 K8s/OpenStack/SSH/SQL 执行（Adapter 内存模拟）
- ❌ 无真实生产 credential（Broker 内存 mock）
- ❌ 无真实变更动作
- ❌ 无 DB migration / Deployment / Agent execution / Auto Run
- ✅ 仅 In-memory MVP + TDD（不连真实系统）
- 真实执行 = NOT YET APPROVED

## 7. GIT_ACTION

```text
GIT_ACTION = NONE（未 git commit；新增 4 实现 + 6 测试，增强 2 Phase8 文件）
```

## 8. 保持冻结（未触碰）

```text
❌ 真实 K8s/OpenStack execution（Execution Production Enablement Gate 未批准）
❌ 生产安全系统接入 / 认证体系修改 / Schema Migration / Deployment
❌ 5 Schema v1（不可变契约）未改
```

## 9. Known Tradeoff

- EX.2/EX.6 provider 用内存 mock（真实 MySQL/query-api 接入属后续）。
- EX.1 signature 用纯 Ed25519（真实用户密钥体系/密钥轮换存储属后续）。
- EX.3 idempotency 用进程内缓存（真实分布式幂等键存储属后续）。
- `test_execution_gate.py`/`test_expr.py` 为历史遗留文件（非本次交付，不影响）。
