# V9.3 Phase7 — P7.6 Implementation Evidence

```text
CONTRACT        = V9.3 DEEPSEEK EXECUTION R4 (Phase 7+ Manual-Triggered)
DESIGN          = V9.3_PHASE7_P76_PLANNER_DESIGN.md v0.1（APPROVED）
MODE            = In-memory MVP + TDD
SCOPE           = P7.6 ONLY
STATUS          = IMPLEMENTED（待用户评审）
GIT_ACTION      = NONE
DATE            = 2026-08-20
```

## 1. 修改文件清单

| 文件 | 类型 | 职责 |
|------|------|------|
| `ai-orchestrator/planner.py` | 新增 | PlanProposal/PlanStep/Budget/PlanResult + Planner(propose_plan/ready_steps/complete_step/missing/finalize) + DAG 校验 |
| `ai-orchestrator/tests/test_p76_planner.py` | 新增 | P7.6 TDD 测试（T1-T5），14 用例 |

未修改任何既有文件。

## 2. Code Map

```text
Intent（P7.5）
   ↓ propose_plan(intent, steps)
Planner
   ├─ _validate_dag: 无环 + 依赖引用存在
   ├─ Tool gate: tool_id 必须已注册且 active（P7.1）
   ├─ 依赖缺失 → 该 step failed（不误执行）
   ├─ Budget: consumed_steps/tools 超限 → budget_exceeded
   ├─ requires_human_approval=True（强制）
   └─ ready_steps: 依赖满足的 pending step（可并行）
        ↓
PlanProposal（只产提案，无最终根因）
   └─ finalize → awaiting_approval（Phase7 停止点，不直连 Executor）
```

## 3. TDD RED → GREEN

```text
RED   : ModuleNotFoundError: planner（功能缺失）
GREEN : 实现 planner.py
修 1   : __init__ 参数 max_latency → max_latency_ms（对齐测试 fixture）
→ 14 passed
回归   : P7.6(14) + P7.5(18) + P7.4(13) + P7.3(26) = 71 passed，无 lint
```

## 4. Test 结果（T1-T5）

- **T1 Plan Proposal**：Intent → PlanProposal（steps+depends_on）；只产提案无 root_cause；requires_human_approval=True 强制 PASS
- **T2 Budget**：max_steps/max_tools 超限 → budget_exceeded；parallel 分支共享预算（consumed_steps 累计）PASS
- **T3 Dependency & Parallel**：依赖未满足不 ready；依赖满足后 ready；缺失依赖 → failed；环 → ValueError PASS
- **T4 MissingEvidence**：add_missing_evidence 生成 slot（tool_id/capability/reason）；不强行归因根因 PASS
- **T5 Approval Boundary**：Planner 无 executor 属性/execute 方法（不直连 Executor）；finalize → awaiting_approval PASS

## 5. Boundary Verification

- ❌ 无真实 LLM Planner / 无 Executor / 无 Agent execution
- F4 红线：只产出 Plan Proposal，不产出 Execution Plan；Planner → Executor 直连禁止
- 预算硬约束；MissingEvidence 不无限探索

## 6. Known Tradeoff

- DAG 生成由调用者提供 steps 定义（MVP 不接真实 LLM Planner）；真实 step 生成策略属后续。
- `complete_step` 用 `result_ref` 记录 ToolResult（tool_id/str）；完整 result 挂接属 P7.7 Structured State。
- capability 匹配不在此层强制（执行期由 P7.2 InternalQueryClient gate）；P7.10 Security 可补强。
