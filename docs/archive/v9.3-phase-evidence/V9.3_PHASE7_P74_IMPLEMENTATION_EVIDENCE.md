# V9.3 Phase7 — P7.4 Implementation Evidence

```text
CONTRACT        = V9.3 DEEPSEEK EXECUTION R4 (Phase 7+ Manual-Triggered)
DESIGN          = V9.3_PHASE7_P74_EVIDENCE_HUB_DESIGN.md v0.2（APPROVED）
MODE            = In-memory MVP + TDD
SCOPE           = P7.4 ONLY
STATUS          = IMPLEMENTED（待用户评审）
GIT_ACTION      = NONE
DATE            = 2026-08-20
```

## 1. 修改文件清单

| 文件 | 类型 | 职责 |
|------|------|------|
| `ai-orchestrator/evidence_hub.py` | 新增 | Evidence schema(frozen) + EvidenceHub(内存 Store) + 去重/生命周期/save_from_tool_result |
| `ai-orchestrator/tests/test_p74_evidence_hub.py` | 新增 | P7.4 TDD 测试（T1-T6），13 用例 |

未修改任何既有文件。

## 2. Code Map

```text
ToolResult（P7.3）
   ↓ save_from_tool_result(tr, run_id, evidence_type, claim_type, ...)
EvidenceHub
   ├─ _fingerprint(source, query_id, resource_id, time_range, digest)
   ├─ 去重: 相同 fingerprint → 复用同一 evidence_id（不重复计分）
   ├─ 校验: source 禁 AI/LLM/Agent; claim_type=inference 引 supporting; unknown 记 reason
   ├─ Evidence(frozen=True) metadata=mappingproxy（不可变）
   └─ transition(status): created→validated→expired→archived（仅 status 可迁移）
        ↓
current_facts() = 仅 validated（freshness isolation）
reference_status(id) = current(validated) | stale(其它)
```

## 3. TDD RED → GREEN

```text
RED   : 全部 ModuleNotFoundError: evidence_hub（功能缺失）
GREEN : 实现 evidence_hub.py
修 1   : tr.fact → tr.summary（ToolResult 无 fact 字段）
修 2   : metadata 不可变 → __post_init__ 转 mappingproxy（dict 修改需抛 TypeError）
修 3   : 测试 _make_tool_result 误传 evidence_type（移除）
→ 13 passed
回归   : P7.4(13) + P7.3(26) + P7.2(20) + P7.1(26) = 85 passed，无 lint
```

## 4. Test 结果（T1-T6）

- **T1 归一化与落库**：ToolResult→Evidence 映射（source=query-api/claim_type=fact/status=created/fact=summary）PASS
- **T2 去重**：相同 fingerprint 复用 evidence_id（len(all)==1）；不同事实新建 PASS
- **T3 生命周期**：created→validated→expired→archived；expired 后 current_facts==[] PASS
- **T4 不可变+LLM 红线**：metadata 修改抛异常（mappingproxy）；LLM source 拒绝；inference 无 supporting 拒绝 PASS
- **T5 关联与追溯**：run_id/evidence_id 关联；unknown 记录 unknown_reason=insufficient_data PASS
- **T6 Freshness Isolation**：archived 不作 current fact（审计保留）；引用 expired → stale PASS

## 5. Boundary Verification

- ❌ 无 MySQL / MinIO / 对象存储（纯内存 Store）
- ❌ 无 DB migration / Deployment / Agent execution
- F3 红线：LLM inference 绝不作为 Evidence（source 禁 AI/LLM/Agent + claim_type=inference 引 supporting）
- Evidence 只记录查询事实，不写 VM/VLogs/MySQL SoT

## 6. Known Tradeoff

- `raw_ref` 恒为 None（内存 MVP）；大型 payload→MinIO 属后续阶段。
- `save()` 与 `save_from_tool_result()` 双入口；`save()` 接受调用者构造的 Evidence（含 fingerprint），`save_from_tool_result()` 从 ToolResult 归一化（生产路径）。
- `source_reliability` 用固定表（VM=0.95 等）；真实可靠性评分属 P7.10。
