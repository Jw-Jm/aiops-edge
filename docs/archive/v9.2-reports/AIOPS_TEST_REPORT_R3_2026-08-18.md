# AIOps 平台 — 第三轮全量功能复测报告（R3）

- **测试日期**: 2026-08-18
- **测试方式**: 真实浏览器逐页/逐 Tab/逐弹窗/逐表单深度交互 + 真实 LLM（deepseek-v4-flash）多场景对话 + 后端 API 核验
- **范围**: 全部 22 个路由页面 + 全部抽屉/弹窗/表单/Tab + AiDock + 通知抽屉 + 工作流全链路 + 真实 LLM 5 场景
- **与前两轮关系**: 本轮为**深度交互复测**（第一轮偏页面级快照，本轮点开所有抽屉/弹窗/表单），并对照前两轮问题清单验证"仍在/已消失/新增"

---

## 一、总体结论

平台核心链路（观测→诊断→审批→执行→报告）**端到端可用且质量良好**：工作流 DAG 全链路执行成功、AI 完整诊断输出高质量、K8s 预检+审批门控生效、报告生成/预览/加入知识库闭环正常。但本轮深度交互暴露了**第一轮未发现的 3 个新问题**，并**实锤确认**了 2 个已知问题（A1 容量 0.00%、A3 k8sgpt 未调用），同时发现 1 个第一轮问题（A4 严重度筛选）**当前版本已修复**。

---

## 二、本轮新发现的问题（第一轮未覆盖）

### N1. [新] 服务列表详情抽屉数据缺失（服务详情端点数据不足）
- **现象**: 服务全景页切到"服务列表"视图，点击服务名（如 ai-orchestrator）打开详情抽屉，显示：健康度"-- 未知"、调用关系"下游 0 / 上游 0"、调用链"暂无数据"——与列表页该服务 6,612 次调用、拓扑图 12 节点 19 关系的实际数据严重矛盾。
- **根因（API 核验）**: 列表视图走 `openServiceDetail` → `GET /services/{name}`，该端点**只返回趋势数据**（`{count, data:[{t,calls,errors,avg_ms}]}`），无 apdex/health_score/上下游关系/trace；而拓扑节点走 `openDetail` → `GET /topology/node/{name}` 才返回完整数据（apdex=1, health_score=100, spans 等）。前端两个入口共用同一抽屉渲染，但服务详情端点数据不足。
- **影响**: 列表视图的详情抽屉对运维几乎无用（健康度未知、无关系、无调用链），且与拓扑视图详情体验不一致。
- **修复方向**: 统一详情数据源——`/services/{name}` 补齐 apdex/health_score/relations/traces，或前端列表视图改调 `/topology/node/{name}`。

### N2. [新] 工作流运行成功但"运行记录"为空
- **现象**: 工作流编辑器运行"完整诊断流程"，DAG 执行到"人工审批"节点弹出审批弹窗（风险分 0），批准后状态变为"已成功"；但点击"运行记录"抽屉显示"暂无运行记录"。
- **根因**: 运行状态与运行记录存储/查询不一致（运行状态实时更新成功，但运行记录列表未查到该次运行）。
- **影响**: 用户无法追溯已成功运行的节点级明细（各节点输出/错误），排障与审计能力缺失。
- **修复方向**: 运行记录与运行状态同源持久化；运行成功后立即写入运行记录表。

### N3. [新] Playbook 内容渲染未剥离 YAML frontmatter
- **现象**: 知识库→运维 Playbook→点击 high-cpu，内容区第一个标题显示原始 frontmatter：`title: CPU 使用率过高告警 tags: [alert, cpu, capacity] alert_keys: [...]`，随后才是正常渲染的正文标题。
- **根因**: Playbook 内容用 ReactMarkdown 渲染但未先解析/剥离 YAML frontmatter。
- **影响**: 视觉噪音，标题区显示原始元数据，不专业。
- **修复方向**: 渲染前解析并剥离 frontmatter（或后端返回时剥离）。

### N4. [新] SLO 目标值后端校验但前端无错误提示
- **现象**: 新建 SLO 填目标值 150（可用性应 ≤100），提交后后端返回 400，但**前端无任何用户可见错误提示**（仅控制台 400 错误），表单停留在 150 无反馈。
- **根因**: 后端有范围校验，前端无校验且未处理 400 错误展示。
- **影响**: 用户提交非法值后无反馈，误以为提交成功或系统故障。
- **修复方向**: 前端加目标值范围校验（availability 0-100、latency >0）+ 后端 400 错误 message 展示。

### N5. [新] 变更登记表单必填项缺失时静默失败
- **现象**: 登记变更弹窗填了服务/操作人/内容但未选"集群"，提交后表格仍 0 条，无任何错误提示（仅控制台错误）。
- **根因**: 表单有"请选择集群"必填项但前端未做必填校验提示，提交失败静默。
- **影响**: 用户操作无反馈，数据未保存。
- **修复方向**: 表单必填校验 + 提交失败错误提示。

### N6. [新] "加入知识库"操作无明确结果反馈
- **现象**: AI 对话最终报告点"加入知识库"，无成功/失败/去重提示，知识库案例数未变化（仍 89 条）。
- **根因**: 后端可能返回"已存在相似案例"（inserted=false）但前端未展示该结果。
- **影响**: 用户无法确认操作是否成功。
- **修复方向**: 前端展示后端返回的 inserted/dup 结果（成功/已存在相似案例）。

---

## 三、前两轮问题复测结果（仍在 / 已修复 / 未复现）

| 问题 | 状态 | 本轮证据 |
|---|---|---|
| **A1 容量页 0.00% 误导** | **仍在** | 容量页 CPU/内存当前值仍显示 0.00%（与总览 58.0% 内存矛盾） |
| **A3 k8sgpt 未调用** | **仍在（实锤）** | 明确要求"请调用k8sgpt工具诊断集群问题"，AI 回复"**本次未获取到 k8sgpt 扫描输出**"，未调用工具 |
| **A3 知识库 RAG** | **部分可用（细化）** | "查询知识库中关于内存泄漏的案例"→ **成功返回 3 个案例**；但 R1 的"请参考知识库"措辞未触发——RAG 依赖措辞，路由脆弱 |
| **A4 严重度筛选** | **已修复/未复现** | 严重筛选正确显示"暂无告警"（警告告警被正确过滤） |
| **B2 会话重载丢处置卡片** | **仍在（实锤）** | order-svc 会话重载后只剩纯文本，处置建议卡片（确认执行/驳回/风险星级）全部丢失 |
| **C4 Grafana iframe 无 sandbox** | **仍在** | iframe sandbox 属性为空 |
| **B10 工作流审批闭环** | **正常（正向）** | 运行→审批弹窗（风险分）→批准→"已成功"全链路成功 |
| **K8s 破坏性操作审批门控** | **正常（正向）** | 预检后提示"必须关联已批准审批单"，执行按钮禁用 |
| **报告生成/预览/加入知识库** | **正常（正向）** | 最终报告生成成功（风险 0.40），预览抽屉正常 |
| **平台健康/审计日志/纳管集群 Tab** | **正常（正向）** | 组件全 ok，审计日志记录 nl2sql 操作 |
| **通知抽屉/根因定位跳转** | **正常（正向）** | 通知抽屉显示告警，根因定位带完整上下文跳 AI 对话并自动分析 |

---

## 四、真实 LLM 对话专项评估（本轮 5 场景）

| 场景 | 结果 | 评价 |
|---|---|---|
| 告警根因定位（总览→根因定位） | 自动带告警上下文跳转并分析，输出完整诊断 | ✅ 深链闭环优秀 |
| AiDock 集群巡检 | 高质量诊断（6 条 critical、OOMKilled 82 次、处置命令、风险★×2/5） | ✅ 优秀 |
| 明确要求 k8sgpt | 回复"未获取到 k8sgpt 扫描输出"，未调用工具 | ❌ A3 实锤 |
| 查询知识库内存泄漏案例 | 成功返回 3 个案例（Java OOM/内核 OOM/容量高位）含根因与处置 | ✅ 措辞触发时 RAG 可用 |
| 输出最终版本报告 | 生成完整最终巡检报告（风险 0.40）+ 加入知识库按钮 | ✅ 闭环正常 |

**结论**: AI 完整诊断/巡检/报告链路质量高；**k8sgpt 工具调用仍不可靠**（A3 核心问题未修复），知识库 RAG 依赖措辞触发（路由脆弱）。建议按修复方案 A3 实施：显式工具关键词优先路由 + 前端渲染 tool_start/tool_end 事件。

---

## 五、结论与建议

**本轮新增 6 个问题（N1-N6）**，均为深度交互暴露，建议补充进修复方案：
- N1（服务详情数据缺失）→ 归入 A 系列（数据正确性）
- N2（运行记录为空）→ 归入 B10（工作流）
- N3（frontmatter 未剥离）→ 归入 B12（知识库）
- N4（SLO 校验无提示）→ 归入 B9（SLO）
- N5（变更表单静默失败）→ 归入 B12（变更）
- N6（加入知识库无反馈）→ 归入 B12（AI 对话）

**确认仍在的问题**: A1（容量 0.00%）、A3（k8sgpt 未调用）、B2（会话重载丢卡片）、C4（iframe 无 sandbox）——均未修复（方案尚未实施，符合预期）。

**确认已修复/未复现**: A4（严重度筛选）——当前版本行为正确。

**正向确认**: 工作流全链路、K8s 审批门控、报告闭环、平台健康、审计日志、通知/根因定位深链均正常。

*本报告与第一轮报告、第二轮报告、修复方案 v2 配套使用；N1-N6 需补充进修复方案。*