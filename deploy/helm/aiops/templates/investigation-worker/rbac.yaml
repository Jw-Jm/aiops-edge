{{- if .Values.investigationWorker.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-investigation-worker
  namespace: {{ .Values.namespace.observability }}
  labels:
    app: ai-investigation-worker
automountServiceAccountToken: false
{{- end }}
